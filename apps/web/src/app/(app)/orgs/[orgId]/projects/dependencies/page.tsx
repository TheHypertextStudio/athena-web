import type { JSX } from 'react';

import ProjectDependenciesClient from './dependencies-client';

/** Render the organization's Project dependencies canvas. */
export default function ProjectDependenciesPage(): JSX.Element {
  return <ProjectDependenciesClient />;
}
