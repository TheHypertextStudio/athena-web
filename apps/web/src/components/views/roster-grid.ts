/** The shared clipping and horizontal gutter contract for roster column headers. */
export const ROSTER_HEADER_CELL_CLASS =
  'text-label-medium min-w-0 overflow-hidden px-4 whitespace-nowrap';

/** The shared clipping and horizontal gutter contract for roster data cells. */
export const ROSTER_DATA_CELL_CLASS = 'flex min-w-0 items-center overflow-hidden px-4';
