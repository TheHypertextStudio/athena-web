/**
 * Marketing site — canonical brand copy.
 *
 * @remarks
 * One tagline, used everywhere (craft-rubric.md, dimension 1). Marketing pages, metadata,
 * the footer, and the auth screens all import from here instead of hand-writing variants.
 *
 * Voice rules these strings exist to hold the line on: no emotional adjectives standing in
 * for a claim ("calm", "effortless", "seamless"), no metaphor ("command center", "home",
 * "roof"), and no audience labels ("for people who run more than one thing"). Say what the
 * software does.
 */

/** The canonical tagline. The only one. */
export const TAGLINE = 'One tool for every kind of work.';

/** Short descriptor for metadata fragments where the tagline reads as a sentence. */
export const SHORT_DESCRIPTOR = 'One tool for planning, scheduling, and tracking work.';

/** Canonical meta description for marketing pages. */
export const META_DESCRIPTION =
  'Docket is one tool for planning, scheduling, and tracking every kind of work. Tasks carry a duration, land on your calendar, and log the hours they actually took — so the plan, the week, and the time are one record instead of three.';
