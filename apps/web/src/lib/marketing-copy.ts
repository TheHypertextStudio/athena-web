/**
 * Marketing site — canonical brand copy.
 *
 * @remarks
 * One tagline, used everywhere (craft-rubric.md, dimension 1). Marketing pages, metadata,
 * the footer, and the auth screens all import from here instead of hand-writing variants.
 */

/** The canonical tagline. The only one. */
export const TAGLINE = 'Run every organization from one calm place.';

/** Short descriptor for metadata fragments where the tagline reads as a sentence. */
export const SHORT_DESCRIPTOR = 'The calm command center for work.';

/** Canonical meta description for marketing pages. */
export const META_DESCRIPTION =
  'Docket is the calm command center for everything you run — your startup, your nonprofit, your side projects. Each one keeps its own space; your day comes together in a single view.';
