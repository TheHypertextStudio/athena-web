# Docket — Build Manifest (atomic, dependency-ordered)

> Generated from the specs + the frozen `DECISIONS.md`. The foundation (P0–P5) is **sequential**; **P6** is the parallel fan-out. Each ticket lists the files it owns, the governing spec section, what to build, and the acceptance gate that must pass before it's accepted.

## Summary

- **172 tickets** across 6 domains; **118 parallelizable**, 54 sequential.
- By phase: `P0`=2, `P1`=3, `P2`=12, `P3`=5, `P4`=5, `P5`=21, `P6`=124

## foundation (18 tickets)

### `FND-P0-01` — Turborepo skeleton: workspace, catalog, root config, Node/pnpm pins _(P0 · sequential)_

- **files:** `package.json`, `pnpm-workspace.yaml`, `.nvmrc`, `turbo.json`, `.gitignore`, `.npmrc`
- **spec:** build-sequence-and-slice.md#Phase 0, build-sequence-and-slice.md#Part 0, env-and-bootstrap.md#2.4, FROZEN:build-sequence-and-slice/turbo+node+pnpm pins, FROZEN:build-sequence-and-slice/pnpm catalog
- **build:** Fresh pnpm + Turborepo 2.9.x monorepo. Root package.json: "packageManager":"pnpm@11.9.x", "engines":{"node":">=24.15 <27"}, devDeps turbo@2.9.x + tsx; shared TypeScript/test/bundler/lint deps reference `catalog:`. Root scripts (build, dev, typecheck, lint, test, bootstrap='tsx scripts/bootstrap.ts', env:check). .nvmrc/.node-version='26'. pnpm-workspace.yaml: packages globs apps/_, packages/_, tooling/_, AND a `catalog:` block pinning the byte-identical cross-cut versions (hono, zod, better-auth=1.6.14 exact + every @better-auth/_ plugin =1.6.14, drizzle-orm, drizzle-kit, @modelcontextprotocol/sdk, stripe@^22, @t3-oss/env-core, @t3-oss/env-nextjs, ulid) plus shared toolchain versions (`@types/node`, typescript, vite, vitest, @vitejs/plugin-react, @vitest/coverage-v8, eslint, @eslint/js, typescript-eslint). turbo.json uses the 2.x `tasks` key (NEVER `pipeline`), strict env mode, globalEnv/per-task env wildcards per env-and-bootstrap §2.4 (NEXT*PUBLIC*_, BETTER*AUTH*_, STRIPE*\*, DOCKET_PRICE*\_, MCP\__, DATABASE*URL\*, GOOGLE*_, GITHUB\__, LINEAR*\*, ATHENA*_, CRON*SECRET, OIDC*_, API_URL, PORT, NODE_ENV, SKIP_ENV_VALIDATION), build/typecheck/lint/test/dev tasks with correct dependsOn (^build). .gitignore covers node_modules, dist, .next, apps/_/.env, .env.vercel.\_, playwright/.auth/\*, packages/db/drizzle is committed (do not ignore migrations).
- **accept:** `pnpm install` completes clean; `pnpm dlx turbo run build --dry=json` validates turbo.json against the 2.x schema with `tasks` key (no `pipeline`); `node -v` satisfies engines (>=24.15 <27); pnpm catalog resolves (`pnpm why hono` shows one version). `git check-ignore apps/api/.env` returns the path.

### `FND-P0-02` — Empty workspace members + tooling presets (tsconfig/eslint/tailwind) _(P0 · parallel)_

- **depends on:** FND-P0-01
- **files:** `tooling/tsconfig/base.json`, `tooling/tsconfig/nextjs.json`, `tooling/tsconfig/package.json`, `tooling/eslint-config/index.js`, `tooling/eslint-config/package.json`, `tooling/tailwind-config/package.json`, `apps/web/package.json`, `apps/marketing/package.json`, `apps/admin/package.json`, `apps/api/package.json`, `packages/db/package.json`, `packages/auth/package.json`, `packages/ui/package.json`, `packages/types/package.json`, `packages/env/package.json`, `packages/test-utils/package.json`
- **spec:** build-sequence-and-slice.md#Phase 0, build-sequence-and-slice.md#1.3, api-rpc-contract.md#1.2, FROZEN:build-sequence-and-slice/turbo+node+pnpm pins
- **build:** Create every workspace member as a folder with a package.json declaring its @docket/_ name and dependency edges (no logic yet, so the dependency graph is declarable). Compilation policy: @docket/db, @docket/auth, apps/api compiled (tsc → dist; package.json "main"/"types"/"exports" point at dist + a build script); @docket/ui, @docket/types, @docket/env are JIT (exports point at src/_.ts, no build). tooling/tsconfig/base.json: strict:true, moduleResolution 'bundler', target ES2022, declaration+composite for compiled pkgs; nextjs.json extends base for the Next apps. tooling/eslint-config: flat config (ESLint 9) shared preset. tooling/tailwind-config: package.json placeholder (CSS-first v4, no preset.ts — globals.css authored in FND-P5-01). All catalog versions referenced via `catalog:` in each package.json. Hono pinned via catalog in apps/api + @docket/types (RPC byte-identical requirement).
- **accept:** `pnpm -r exec true` enumerates all members without error; `pnpm turbo run build` succeeds across empty packages (compiled pkgs emit empty dist, JIT pkgs are no-op); `pnpm turbo run typecheck` passes (strict:true everywhere). Dependency graph: `pnpm why @docket/types` resolves the workspace link.

### `FND-P1-01` — @docket/env — t3-oss/env slices + per-app compositions + VAR*REGISTRY *(P1 · sequential)\_

- **depends on:** FND-P0-02
- **files:** `packages/env/src/registry.ts`, `packages/env/src/shared.ts`, `packages/env/src/db.ts`, `packages/env/src/auth.ts`, `packages/env/src/stripe.ts`, `packages/env/src/mcp.ts`, `packages/env/src/agent.ts`, `packages/env/src/ops.ts`, `packages/env/src/env.api.ts`, `packages/env/src/env.web.ts`, `packages/env/src/env.marketing.ts`, `packages/env/src/env.admin.ts`, `packages/env/package.json`, `scripts/env-check.ts`
- **spec:** env-and-bootstrap.md#1 (full var contract), env-and-bootstrap.md#2.1-2.5, env-and-bootstrap.md#4, FROZEN:env-and-bootstrap/single-source VAR_REGISTRY, FROZEN:env-and-bootstrap/MCP env names canonical, FROZEN:env-and-bootstrap/BILLING_ENABLED flag, FROZEN:env-and-bootstrap/agent endpoint+key pairing, FROZEN:env-and-bootstrap/BLOB_READ_WRITE_TOKEN
- **build:** Build @docket/env (JIT) as createEnv slices composed per deployable. registry.ts = the single typed VAR*REGISTRY array {name,slice,scope,targets,required,zod,where,generate?,sensitive} from which the slice schemas AND the bootstrap prompts are derived (one declaration site). Slices: shared (NODE_ENV, API_URL, NEXT_PUBLIC*_*URL passthrough), db (DATABASE_URL, DATABASE_URL_UNPOOLED), auth (BETTER_AUTH_SECRET min 32, BETTER_AUTH_URL, BETTER_AUTH_TRUSTED_ORIGINS csv, BETTER_AUTH_PASSKEY_RP_ID, BETTER_AUTH_PASSKEY_RP_NAME default 'Docket', GOOGLE/GITHUB/LINEAR id+secret), stripe (STRIPE_SECRET_KEY sk*, STRIPE*WEBHOOK_SECRET whsec*, DOCKET*PRICE_LOOKUP_TEAM, DOCKET_PRICE_LOOKUP_TEAM_ANNUAL?, STRIPE_PRICE_TEAM? price*, NEXT*PUBLIC_STRIPE_PUBLISHABLE_KEY pk*, STRIPE*BILLING_PORTAL_CONFIG_ID?, BILLING_ENABLED coerce-bool default false), mcp (MCP_ISSUER_URL=API_URL, MCP_RESOURCE_URL=${API_URL}/mcp, MCP_ALLOWED_ORIGINS csv, OIDC_LOGIN_PAGE_URL?, MCP_TASKS_ENABLED default false, MCP_CIMD_TRUST_ALLOWLIST? csv, MCP_CIMD_STRICT default true, MCP_SESSION_STORE_URL?), agent (ATHENA_AGENT_ENDPOINT?, ATHENA_AGENT_API_KEY?, ANTHROPIC_API_KEY?), ops (CRON_SECRET, SENTRY_DSN?, BLOB_READ_WRITE_TOKEN?, EXPORT_BUCKET_URL?, EXPORT_BUCKET_TOKEN?). superRefine cross-field rules at env.api composition: agent endpoint↔key all-or-nothing; BILLING_ENABLED=true ⇒ STRIPE_SECRET_KEY + price required; STRIPE_PRICE_TEAM-or-lookup; EXPORT_BUCKET_URL↔token; MCP_TASKS_ENABLED=true ⇒ MCP_SESSION_STORE_URL. env.api uses @t3-oss/env-core extends [shared,db,auth,stripe,mcp,agent,ops] + PORT; env.web/.marketing/.admin use @t3-oss/env-nextjs with NEXT_PUBLIC*_ referenced LITERALLY in runtimeEnv. emptyStringAsUndefined:true, skipValidation gated on SKIP_ENV_VALIDATION. package.json exports map: ./api, ./web, ./marketing, ./admin, ./registry. scripts/env-check.ts (pnpm env:check) runs each composition and prints the first failing var + its `where` hint from the registry.
- **accept:** `pnpm --filter @docket/env typecheck` passes. A unit/smoke test: importing env.api with DATABASE*URL unset throws at module load with a message naming DATABASE_URL + its where-hint (proves fail-fast 12-factor). `pnpm env:check` with a complete dev env exits 0; with a missing required var exits non-zero naming the var. NEXT_PUBLIC*\* appear literally in env.web runtimeEnv (grep confirms no `...process.env` spread).

### `FND-P2-01` — @docket/db scaffold: drizzle deps, id generator, drizzle.config, db client, package compile _(P2 · sequential)_

- **depends on:** FND-P1-01
- **files:** `packages/db/src/id.ts`, `packages/db/drizzle.config.ts`, `packages/db/src/client.ts`, `packages/db/src/index.ts`, `packages/db/package.json`, `packages/db/tsconfig.json`
- **spec:** data-model.md#0.1, data-model.md#9, build-sequence-and-slice.md#Phase 2, RECONCILIATION.md#1 (text ULIDs), FROZEN:data-model/ULID single generator, env-and-bootstrap.md#3.3 (unpooled for migrate)
- **build:** Compiled package (tsc → dist). id.ts: `import { ulid } from 'ulid'; export const genId = () => ulid();` — the single repo-wide ULID generator (no uuid, no cuid2). drizzle.config.ts: { schema:'./src/schema', dialect:'postgresql', out:'./drizzle', dbCredentials:{ url: process.env.DATABASE_URL_UNPOOLED ?? process.env.DATABASE_URL } } (migrations run over the UNPOOLED string). client.ts: construct the drizzle db client — Neon serverless driver in prod / node-postgres locally, both from DATABASE_URL (pooled) — and export `db`. index.ts: re-export genId, db, and (later) the `schema` namespace + inferred row types. package.json scripts: db:generate (drizzle-kit generate), db:migrate (drizzle-kit migrate), db:studio (drizzle-kit studio), db:auth:generate (placeholder wired in FND-P3-01); deps drizzle-orm + drizzle-kit + postgres + @neondatabase/serverless + ulid via catalog; main/types → dist. Do NOT define tables here (subsequent tickets) — this is the owner-package skeleton so it is the single SQL owner.
- **accept:** `pnpm --filter @docket/db build` emits dist with genId typed `() => string`; `pnpm --filter @docket/db exec drizzle-kit --help` resolves drizzle-kit. A unit assertion: genId() returns a 26-char Crockford ULID matching /^[0-9A-HJKMNP-TV-Z]{26}$/. `drizzle.config.ts` references DATABASE_URL_UNPOOLED first.

### `FND-P2-02` — @docket/db enums + jsonb $type interfaces (enums.ts, types.ts) _(P2 · sequential)_

- **depends on:** FND-P2-01
- **files:** `packages/db/src/enums.ts`, `packages/db/src/types.ts`
- **spec:** data-model.md#1 (all pgEnum), data-model.md#0.5, RECONCILIATION.md#4 (resource_kind, grant_subject_kind), RECONCILIATION.md#2 (grant_effect), FROZEN:data-model/grant_effect enum, FROZEN:data-model/invitation_status enum, FROZEN:data-model/idempotency_status enum, FROZEN:data-model/view_scope enum, FROZEN:data-model/audit_subject_type adds agent, FROZEN:design-system/VocabularySkin shape, FROZEN:design-system/HubPreferences shape
- **build:** enums.ts: declare ALL pgEnum() exactly as data-model §1 (actorKind, actorStatus, initiativeStatus, programStatus, projectStatus, cycleStatus, health, taskPriority, provenanceSource, syncMode, sessionTrigger, sessionStatus, sessionActivityType, approvalStatus, approvalPolicy, grantCapability, grantSubjectKind, resourceKind, visibility, updateSubjectType, commentSubjectType, notificationType, auditSubjectType WITH 'agent' added per frozen decision, auditEventType, integrationPattern, integrationRole, integrationStatus, dailyPlanItemStatus, orgLifecycleState, staffRole) PLUS the frozen-added enums: grantEffect=['allow','deny'], invitationStatus=['pending','accepted','revoked','expired'], idempotencyStatus=['in_progress','completed'], viewScope=['personal','team','organization']. NO resource_type (use resourceKind). types.ts: Zod-derived $type<> interfaces for every jsonb column — HubPreferences (frozen shape), VocabularySkin ({preset:'startup'|'nonprofit'|'agency', overrides?}), WorkflowState ({key,name,type:'backlog'|'unstarted'|'started'|'completed'|'canceled',position}) + defaultWorkflowStates const (first state key='backlog'), presetStartup const, AgentConnection, ApprovalRouting, IntegrationConnection, NotificationBody, SessionActivityBody, ViewFilter/ViewGrouping/ViewSort, GrantCapability[] type. These are db-internal $type shapes; the canonical Zod source lives in @docket/types (FND-P4-types) and is re-derived here.
- **accept:** `pnpm --filter @docket/db typecheck` passes; grep confirms grant_effect, invitation_status, idempotency_status, view_scope pgEnums exist and audit_subject_type includes 'agent'; defaultWorkflowStates[0].key === 'backlog'; no occurrence of `resource_type` or `pgEnum("resource_type"` in the file.

### `FND-P2-03` — @docket/db identity schema island (hub, organization, actor, team, teamMember, invitation) _(P2 · sequential)_

- **depends on:** FND-P2-02
- **files:** `packages/db/src/schema/identity.ts`
- **spec:** data-model.md#3.2-3.5, data-model.md#0.2 (org_id leading key), data-model.md#0.3 (auditColumns), RECONCILIATION.md#5 (visibility + ancestor_path), FROZEN:data-model/team+org approval_routing columns, FROZEN:data-model/invitation table, FROZEN:data-model/actor.user_id cascade direction, FROZEN:data-model/team_member retains org_id, no audit cols, FROZEN:data-model/personal-space app-guarded
- **build:** schema/identity.ts: define `auditColumns()` helper (id text PK $defaultFn(genId), organizationId NOT NULL → organization CASCADE, createdBy → actor SET NULL, createdAt, updatedAt $onUpdate, archivedAt nullable). Tables: `hub` (id, userId unique → user CASCADE, name?, preferences jsonb HubPreferences default {}, ts; uniqueIndex hub_user_id_uq); `organization` (id, name, slug, avatar?, isPersonal default false, vocabulary jsonb VocabularySkin default presetStartup, agentGuidance?, approvalRouting jsonb ApprovalRouting nullable [FROZEN], lifecycleState orgLifecycleState default trialing, exportReadyAt?, deleteAfterAt?, ts, archivedAt; uniqueIndex organization_slug_uq, index organization_lifecycle_idx); `actor` (id, organizationId NOT NULL CASCADE, kind, displayName, avatar?, status default active, userId? → user CASCADE [cascade fires only on user hard-delete], roleId? → role SET NULL, ts, archivedAt; index actor_org_idx, partial uniqueIndex actor_org_user_uq WHERE user_id IS NOT NULL); `team` (id, organizationId CASCADE, name, key, description?, workflowStates jsonb default defaultWorkflowStates, triageEnabled default true, agentGuidance?, approvalRouting jsonb ApprovalRouting nullable [FROZEN], visibility default public [FROZEN], ancestorPath text[] default '{}' [FROZEN], ts, archivedAt; index team_org_idx, uniqueIndex team_org_key_uq, GIN index team_ancestor_path_gin); `teamMember` (teamId CASCADE, actorId CASCADE, organizationId CASCADE; PK(teamId,actorId), index team_member_actor_idx — NO audit cols, org_id retained per FROZEN); `invitation` (id, organizationId CASCADE, email, roleId NOT NULL → role RESTRICT, asGuest default false, token, status invitationStatus default pending, invitedBy? → actor SET NULL, expiresAt NOT NULL, createdAt, acceptedAt?; uniqueIndex invitation_token_uq, partial uniqueIndex invitation_org_email_pending_uq WHERE status='pending', index invitation_org_idx). Import user from schema/auth (FK targets; auth.ts generated in P3 — use a forward type ref or a thin re-export shim so this compiles before auth gen, then unify).
- **accept:** `pnpm --filter @docket/db typecheck` passes after auth shim; grep confirms organization.approval_routing, team.approval_routing, team.visibility, team.ancestor_path, invitation table + invitation_org_email_pending_uq partial index, actor_org_user_uq partial index. organization_id is the leading column on team_org_idx / actor_org_idx.

### `FND-P2-04` — @docket/db work + crosscutting + joins + agents + admin + infra schema islands _(P2 · sequential)_

- **depends on:** FND-P2-03
- **files:** `packages/db/src/schema/work.ts`, `packages/db/src/schema/crosscutting.ts`, `packages/db/src/schema/joins.ts`, `packages/db/src/schema/agents.ts`, `packages/db/src/schema/admin.ts`, `packages/db/src/schema/infra.ts`, `packages/db/src/relations.ts`
- **spec:** data-model.md#4,#5,#6,#7,#8, RECONCILIATION.md#2,#3,#5, FROZEN:data-model/canonical grant columns + grant_effect + grant_subject_resource_effect_uq, FROZEN:data-model/canonical role columns (key,is_system,base_capability,default_visibility), FROZEN:data-model/delete permission_grant, FROZEN:data-model/visibility+ancestor_path on program/project/task, FROZEN:data-model/idempotency_key table, FROZEN:data-model/saved_view table, FROZEN:data-model/join tables retain org_id
- **build:** Define the rest of the §5 schema. work.ts: initiative, program (NO completed status), project (+ visibility, ancestor_path GIN [FROZEN]), cycle, task (+ visibility, ancestor_path GIN [FROZEN]; state text key, provenance inline triple, task_source_uq partial index, estimate unitless integer), milestone. crosscutting.ts: `role` with CANONICAL columns (id, organizationId CASCADE, key NOT NULL, name NOT NULL, isSystem default false, capabilities jsonb GrantCapability[] default [], baseCapability grantCapability nullable, defaultVisibility visibility default public; uniqueIndex role_org_key_uq + role_org_name_uq); `grant` with CANONICAL folded columns (id, organizationId CASCADE, subjectKind, subjectId text, resourceKind, resourceId text, capabilities jsonb GrantCapability[] NOT NULL, effect grantEffect default allow [FROZEN], cascades bool default true, visibilityOverride visibility nullable, expiresAt?, visibility default public, createdBy? → actor SET NULL, createdAt; indexes grant_org_idx, grant_subject_idx, grant_resource_idx, uniqueIndex grant_subject_resource_effect_uq); update, dailyPlanItem (hubId, no org_id, refOrganizationId, refTaskId), notification, integration, label (partial uniqueIndexes label_org_name_global_uq WHERE team_id IS NULL + label_team_name_uq WHERE team_id IS NOT NULL [FROZEN]), comment, auditEvent, savedView [FROZEN]. joins.ts: initiativeProject, initiativeProgram, taskLabel, taskDependency (PK + no-self CHECK + blocked_idx), each retaining organization_id [FROZEN]. agents.ts: agent (+ approvalRouting already on table), agentSession (table name agent_session, TS agentSession), sessionActivity. admin.ts: staffUser, impersonationSession, lifecycleHold, operatorAuditEvent. infra.ts: idempotency_key (PK(user_id,key), org_id nullable CASCADE, method/path/request_hash, response_status/body, status idempotencyStatus, expires_at, idempotency_expires_idx) [FROZEN]. relations.ts: drizzle relations() for all. DELETE any permission_grant table [FROZEN]. Then update packages/db/src/index.ts to export the `schema` namespace object (for drizzle()) + inferred row types.
- **accept:** `pnpm --filter @docket/db build` succeeds; grep confirms exactly one `grant` table with effect/cascades/visibility_override/expires_at and NO permission_grant; role has key/is_system/base_capability/default_visibility; idempotency_key + saved_view tables exist; program has no `completed` enum value; task + project + program + team carry visibility + ancestor_path; label has the two partial unique indexes. `schema` object exported from index.ts.

### `FND-P2-05` — @docket/db initial migration generated + applied (slice + full schema) _(P2 · sequential)_

- **depends on:** FND-P2-04
- **files:** `packages/db/drizzle/0000_init.sql`, `packages/db/drizzle/meta/_journal.json`, `packages/db/drizzle/meta/0000_snapshot.json`
- **spec:** data-model.md#9 (single migration owner), build-sequence-and-slice.md#Phase 2 exit gate, env-and-bootstrap.md#3.3 (migrate over unpooled), FROZEN:env-and-bootstrap/Neon dev branch
- **build:** Run `pnpm --filter @docket/db db:generate` to emit the first Drizzle migration covering every app table from FND-P2-03/04 (this runs BEFORE Better Auth gen; auth tables are added in FND-P3-01's follow-up migration, OR if auth.ts already exists the migration is unified — coordinate via depends_on). Commit the generated SQL + meta journal/snapshot (migrations are checked in; do not gitignore drizzle/). Apply against a real Neon `dev` branch over DATABASE_URL_UNPOOLED via `pnpm --filter @docket/db db:migrate`. Do NOT hand-edit generated SQL.
- **accept:** `pnpm --filter @docket/db db:migrate` against the Neon dev branch applies cleanly; `pnpm --filter @docket/db db:studio` (or a `SELECT to_regclass('public.organization')` smoke query via test-utils) shows the organization/actor/team/task/role/grant tables exist; the GIN indexes on task.ancestor_path and partial unique indexes (actor_org_user_uq, invitation_org_email_pending_uq) are present (`\d+ actor` / information_schema check).

### `FND-P3-01` — @docket/auth betterAuth() config (passkey-first + resolveUser) + Better Auth schema gen into @docket/db _(P3 · sequential)_

- **depends on:** FND-P2-05
- **files:** `packages/auth/src/index.ts`, `packages/auth/src/passkey-intent.ts`, `packages/db/src/schema/auth.ts`, `packages/auth/package.json`, `packages/auth/tsconfig.json`
- **spec:** build-sequence-and-slice.md#Phase 3, build-sequence-and-slice.md#Part 0 (passkey-first facts), data-model.md#2 (Better Auth tables owned by @docket/db), RECONCILIATION.md#1 (advanced.database.generateId), #21 (org plugin OFF), FROZEN:data-model/advanced.database.generateId VERIFIED + usePlural off + useNumberId not set, FROZEN:build-sequence-and-slice/passkey-intent HMAC context + transactional user+hub
- **build:** Compiled package. index.ts: one betterAuth() config — drizzleAdapter(db,{provider:'pg'}) WITHOUT usePlural (singular table names) and WITHOUT useNumberId; advanced.database.generateId = () => genId() (shares the ULID generator so user/session/account ids are 26-char text ULIDs lining up with actor.user_id FK); plugins minimal for the slice: passkey({ registration:{ requireSession:false, resolveUser } }) + nextCookies() LAST. resolveUser({context}) verifies the HMAC-signed context (FND passkey-intent) and in ONE Drizzle transaction inserts the `user` row (id via generateId) AND its 1:1 `hub` row (guards the partial-identity failure mode); reject missing/invalid/expired context → 400. RP id/name + secret from @docket/env/api authEnv constants. (Google/GitHub/Linear social, sso, scim, oidcProvider, mcp, stripe are Phase-6 lanes — NOT added here.) passkey-intent.ts: HMAC sign/verify of {name,email,nonce,exp} over BETTER_AUTH_SECRET (5-min TTL) for the pre-registration POST /v1/auth/passkey-intent handler (route wired in apps/api FND-P4-03). Then run `npx @better-auth/cli@1.6.14 generate` to emit schema/auth.ts INTO @docket/db (user/session/account/verification/passkey; oauthApplication/oauthAccessToken/oauthConsent/subscription/sso/scim/jwks only when those plugins enable later — for the slice plugin set, generate just core+passkey). Unify identity.ts's user FK shim to import the real generated user table. Regenerate + apply the migration (extends FND-P2-05's journal). Add db:auth:generate script to @docket/db.
- **accept:** `pnpm --filter @docket/auth build` succeeds; `pnpm --filter @docket/db db:migrate` now creates user/session/account/verification/passkey. A unit test (CDP/virtual-authenticator or the better-auth test harness) registers + verifies a passkey with no prior session and receives a session cookie; a row assertion confirms BOTH a `user` AND a 1:1 `hub` row were created in the same tx, and user.id matches /^[0-9A-HJKMNP-TV-Z]{26}$/ (ULID, proving advanced.database.generateId is honored). resolveUser rejects an expired context with 400.

### `FND-P4-01` — @docket/types: branded Id primitive, Capability/enums, Problem, ListQuery/Page, and AppType re-export _(P4 · sequential)_

- **depends on:** FND-P3-01
- **files:** `packages/types/src/primitives.ts`, `packages/types/src/capability.ts`, `packages/types/src/errors.ts`, `packages/types/src/pagination.ts`, `packages/types/src/vocabulary.ts`, `packages/types/src/hub-preferences.ts`, `packages/types/src/index.ts`, `packages/types/src/api.ts`, `packages/types/package.json`
- **spec:** api-rpc-contract.md#1.7,#5, permissions.md#1 (CAPABILITIES, satisfies), RECONCILIATION.md#6,#7 (flat Capability literal, one branded Id), FROZEN:api-rpc-contract/one shared branded Id ULID regex + per-entity type brands, FROZEN:api-rpc-contract/Problem RFC9457 + closed code enum, FROZEN:api-rpc-contract/delete CapabilityGrid (flat), FROZEN:api-rpc-contract/@docket/types/api re-export path, FROZEN:design-system/VocabularySkin + HubPreferences single source in @docket/types
- **build:** @docket/types (JIT, Zod v4). primitives.ts: ONE branded `Id = z.string().regex(/^[0-9A-HJKMNP-TV-Z]{26}$/).brand('Id')`; per-entity ids (OrganizationId, TaskId, TeamId, ActorId, RoleId, ProjectId, GrantId, …) are TYPE-LEVEL brands over the SAME runtime ULID validator (an `id<'TaskId'>()` factory). NO z.string().uuid() anywhere. Also DateString (z.iso.date()). capability.ts: `Capability = z.enum(['view','comment','contribute','assign','manage'])`, CAPABILITY_RANK, `satisfies(held,required)`, `GrantCapability` alias; export Visibility, Health, Priority enums. NO CapabilityGrid (deleted per frozen decision — flat Capability[] only). errors.ts: `Problem` Zod schema (RFC 9457: type, title, status, detail?, code, fieldErrors?) + a CLOSED `ProblemCode` enum (task_already_linked, idempotency_key_reuse, dependency_cycle, last_owner_guard, self_escalation, personal_org_no_invites, card_required, …) for client switch. pagination.ts: ListQuery (cursor?, limit coerce int 1..100 default 50, order asc|desc) + Page<T> ({items,nextCursor,total?}). vocabulary.ts + hub-preferences.ts: canonical Zod source for VocabularySkin and HubPreferences (re-derived by @docket/db $type and consumed by @docket/ui). index.ts re-exports all. api.ts: `export type { AppType } from '@docket/api'` (re-exported as the @docket/types/api subpath). package.json exports map adds './api' → ./src/api.ts.
- **accept:** `pnpm --filter @docket/types typecheck` passes; a type test asserts `OrganizationId` and `TaskId` are NOT mutually assignable (distinct brands) yet share the ULID runtime schema; `Id.parse('0'.repeat(26))`-style valid ULID parses and a uuid string rejects; grep confirms NO `z.string().uuid(` and NO `CapabilityGrid` in the package; `import type { AppType } from '@docket/types/api'` resolves once apps/api compiles.

### `FND-P4-02` — @docket/types: slice DTO schemas (OrgCreate/ProjectCreate/TaskCreate + \*Out) _(P4 · parallel)_

- **depends on:** FND-P4-01
- **files:** `packages/types/src/organization.ts`, `packages/types/src/project.ts`, `packages/types/src/task.ts`, `packages/types/src/actor.ts`, `packages/types/src/team.ts`
- **spec:** api-rpc-contract.md#3.1,#3.5,#3.7, RECONCILIATION.md#9,#10 (PascalCase \*Create; state from workflow_states[0]), FROZEN:build-sequence-and-slice/OrgCreate+ProjectCreate+TaskCreate full §3 shapes, FROZEN:api-rpc-contract/TaskUpdate field set + reparent via /move, data-model.md#4.5 (task state key)
- **build:** Author the slice DTOs as Zod schemas with .meta({id,description}) for clean OpenAPI $refs, PascalCase canonical names (NOT create\*Input). OrgCreate { name min1, slug?, vocabulary z.enum(['startup','nonprofit','agency']).default('startup'), isPersonal forced false, intent? } (full §3 shape so feature lanes extend without re-cut); OrgOut/OrgSummary. ProjectCreate { name, description?, leadId?:ActorId, teamId?:TeamId, startDate?, targetDate?, initiativeIds?:InitiativeId[] } — organizationId comes from the path, NEVER the body; ProjectOut. TaskCreate { title, description?, teamId:TeamId, state?:WorkflowStateRef, priority?:Priority, assigneeId?:ActorId, projectId?:ProjectId, milestoneId?, cycleId?, parentTaskId?, estimate?, dueDate?, labels? } — when state omitted, app sets team.workflow_states[0].key (no DB default); TaskOut (with provenance single triple, NOT externalLinks[]); TaskUpdate { title?, description?, state?, priority?, estimate?, dueDate?, labels? } (reparent fields REMOVED — go through /move). ActorOut, TeamOut as needed by the slice GET routes. All ids use the branded Id primitives from FND-P4-01.
- **accept:** `pnpm --filter @docket/types typecheck` passes; OrgCreate.parse({name:'Acme'}) yields vocabulary:'startup' (default); TaskCreate rejects a missing teamId; grep confirms schema export names are OrgCreate/ProjectCreate/TaskCreate (no create\*Input) and TaskUpdate has no projectId/programId/cycleId/parentTaskId field. ProjectCreate has no organizationId field.

### `FND-P4-03` — apps/api: app composition root (CORS → session mw → /api/auth/\* mount → /v1 basePath), error handler, OpenAPI/Scalar, health _(P4 · sequential)_

- **depends on:** FND-P4-01, FND-P3-01
- **files:** `apps/api/src/index.ts`, `apps/api/src/app.ts`, `apps/api/src/env.ts`, `apps/api/src/error.ts`, `apps/api/src/lib/ok.ts`, `apps/api/src/auth/session-middleware.ts`, `apps/api/src/openapi.ts`, `apps/api/package.json`, `apps/api/tsconfig.json`
- **spec:** api-rpc-contract.md#1.1-1.6, build-sequence-and-slice.md#Phase 4, RECONCILIATION.md#8,#22 (/v1/orgs nesting, same-origin), FROZEN:api-rpc-contract/canonical composition (orgs.use middleware placement, auth/mcp/docs outside AppType), FROZEN:api-rpc-contract/Problem onError status map + validator hook, FROZEN:api-rpc-contract/ok() output validation helper, env-and-bootstrap.md#2.5 (import env at top)
- **build:** Compiled Hono 4.x service. env.ts: `import { env } from '@docket/env/api'` at top so process refuses bad contract; define AppEnv ({ Variables: { session, actorCtx } }). index.ts ORDER: (1) cors({ origin: env BETTER*AUTH_TRUSTED_ORIGINS, credentials:true, allowHeaders:['Content-Type','Authorization'], exposeHeaders:['Authorization','WWW-Authenticate'] }) FIRST; (2) app.use('*', sessionMiddleware) setting c.var.session via auth.api.getSession({headers}); (3) app.on(['POST','GET'],'/api/auth/\_', c=>auth.handler(c.req.raw)) — OUTSIDE /v1, NOT in AppType; (4) mount the /v1 routes const from app.ts; (5) GET /v1/health, GET /v1/openapi.json (openAPIRouteHandler, OpenAPI 3.1, env-driven servers, bearerAuth+mcpOAuth schemes), GET /v1/docs (@scalar/hono-api-reference) — all OUTSIDE the AppType routes const. app.ts: `const app = new Hono<AppEnv>().basePath('/v1'); const routes = app.route('/orgs', orgs)...; export type AppType = typeof routes; export { app };` (chained, never broken; orgs.use('/:orgId/\*', orgContextMiddleware) placement reserved for FND-P4.5). error.ts: onError mapping ZodError→422, AuthError→401, CapabilityError→403, NotFoundError→404, ConflictError→409, CycleError→409, IdempotencyConflictError→422, BillingFrozenError→402, else→500, all emitting the @docket/types Problem shape with Content-Type application/problem+json. lib/ok.ts: ok(c,schema,data) that schema.parse()es in dev/test then c.json. session-middleware.ts as above. Use hono-openapi validator/describeRoute/resolver (NOT @hono/zod-openapi). Package compiled to dist; export `type AppType` from package entry. ALSO mount POST /v1/auth/passkey-intent (public) calling the FND-P3-01 passkey-intent signer — wait: passkey-intent is auth-adjacent; per frozen decision it lives at POST /v1/auth/passkey-intent (public) — register it on app OUTSIDE the AppType routes (it is auth pre-registration). Add scratch consumer typecheck of hc<AppType>.
- **accept:** `pnpm --filter @docket/api build` emits dist exporting `type AppType`; `pnpm dev`-style boot of apps/api refuses to start with a missing required env var (proves env.ts top-import). `GET /v1/health` returns ok; `GET /v1/openapi.json` returns a 3.1 doc with bearerAuth scheme + env-driven servers; `GET /v1/docs` renders Scalar. A scratch `hc<AppType>` consumer typechecks (RPC seam alive). An unauthenticated request to a protected stub returns the Problem JSON with application/problem+json.

### `FND-P4-04` — apps/api: chained org/project/task routers (slice routes, /v1/orgs/:orgId nesting) + org-create transaction _(P4 · sequential)_

- **depends on:** FND-P4-03, FND-P4-02
- **files:** `apps/api/src/routes/orgs.ts`, `apps/api/src/routes/projects.ts`, `apps/api/src/routes/tasks.ts`
- **spec:** api-rpc-contract.md#1.3,#3.1,#3.5,#3.7, build-sequence-and-slice.md#2.3, RECONCILIATION.md#8,#10, FROZEN:build-sequence-and-slice/nested route set + org-create exception, FROZEN:build-sequence-and-slice/org-create tx seeds team_member + 4 roles + owner actor, FROZEN:api-rpc-contract/state from team.workflow_states[0].key, permissions.md#4.1 (seeded role defaults)
- **build:** Method-chained Hono routers (NEVER break the chain). orgs.ts: `const orgs = new Hono<AppEnv>().get('/', listMemberships).post('/', validator('json',OrgCreate), createOrg).get('/:orgId', getOrg).route('/:orgId/projects', projects).route('/:orgId/tasks', tasks); export default orgs;`. POST /v1/orgs (auth=authenticated, the single un-nested create; org doesn't exist yet) runs ONE Drizzle transaction creating, in order: organization row (isPersonal=false); 4 seeded `role` rows (owner/admin/member/guest, is_system=true, with base_capability owner=manage/admin=manage/member=contribute/guest=null, default_visibility per permissions §4.1); caller's human `actor` (role=owner) using actorCtx user; default `team` row + its `actor{kind:'team'}`; a `team_member` row binding the creator's human actor; the owner/admin/member role-base materialized as grant rows at org root. Returns OrgOut + defaultTeam via ok(). GET /v1/orgs returns the caller's memberships (Page<OrgSummary>). projects.ts mounted at /:orgId/projects: POST (validator ProjectCreate; organizationId from path actorCtx, never body; status=planned) + GET (list, org-scoped). tasks.ts mounted at /:orgId/tasks: POST (validator TaskCreate; teamId required; state defaults to team.workflow_states[0].key='backlog'; provenance native; org_id from path) + GET (?projectId= for grouping). All nested handlers read organization_id from c.var.actorCtx ONLY. Output through ok(c, \*Out, data).
- **accept:** Routers compile into the AppType chain (`hc<AppType>` autocompletes client.orgs.$post / client.orgs[':orgId'].tasks.$post with correct body/response types). Integration: POST /v1/orgs creates org + default team + 4 roles + owner actor + team_member in one tx (DB read confirms all rows + grant rows at org root); POST a task with no `state` lands in 'backlog'; a task created with no projectId is returned (groups under No-project/Triage). organization_id never read from body (grep handlers).

### `FND-P45-01` — Permissions core: @docket/authz canActor + satisfies + write-guards; orgContextMiddleware + requireOrgMember/capabilityGuard _(P5 · sequential)_

- **depends on:** FND-P4-04
- **files:** `packages/authz/src/index.ts`, `packages/authz/src/can-actor.ts`, `packages/authz/src/ancestor-chain.ts`, `packages/authz/src/visibility.ts`, `packages/authz/src/write-guards.ts`, `packages/authz/package.json`, `apps/api/src/permissions/org-context-middleware.ts`, `apps/api/src/permissions/capability-guard.ts`
- **spec:** build-sequence-and-slice.md#Phase 4.5, permissions.md#1,#2,#4,#5,#6,#7, api-rpc-contract.md#1.6, RECONCILIATION.md#27 (deny deferred), FROZEN:permissions/DENY deferred (DENY_ENABLED=false fast path), FROZEN:permissions/canActor returns effectiveCapability for 404-vs-403, FROZEN:permissions/cross-org+suspended pre-checks, FROZEN:permissions/last-owner + no-self-escalation write-guards, FROZEN:permissions/canCapabilityGuard label→literal, FROZEN:permissions/MVP cut: foundation needs requireOrgMember + 4 roles + creator-Owner
- **build:** @docket/authz (compiled; pure engine, unit-tested). can-actor.ts: `canActor(actorId, required:Capability, target:ResourceRef, db): ResolveResult{allow, reason, effectiveCapability}` — step0 pre-checks (load actor; cross-org or unknown → deny; status!=='active' → deny); build ancestor chain (FK walk, ≤5 hops, single CTE or ancestor_path); collect applicable non-expired grants (subject ∈ {actor} ∪ {actor.roleId}, resource ∈ chain, cascades); precedence walk (specificity → max-rank allow); ALLOW-ONLY fast path with effect='deny' rows ignored behind compile-dead DENY_ENABLED=false (forward-compatible). visibility.ts: effectiveVisibility (visibilityOverride nearest-ancestor → resource.visibility → public) + visibilityGrantsView (members see public, guests grant-only). ancestor-chain.ts: the FK-walk/ancestor_path resolver. write-guards.ts: lastOwnerGuard (SERIALIZABLE + FOR UPDATE count of active owner actors stays ≥1, 409 last_owner_guard) + noSelfEscalation (granted base ≤ writer's effective manage rank, 403 self_escalation) — imported by member/role/grant mutation endpoints. apps/api/src/permissions/org-context-middleware.ts: orgContextMiddleware applied via `orgs.use('/:orgId/*', orgContextMiddleware)` BEFORE child .route() chains (the only correct placement) — loads the human Actor for (session.user.id, orgId), 404 if no membership, sets c.var.actorCtx={orgId, actorId, role, capabilities}. capability-guard.ts: capabilityGuard(required, locate) parses the org:<cap> route label → bare Capability literal and calls canActor; derives 404-vs-403 from the SINGLE result's effectiveCapability (no second DB call); 403 on capability failure, 404/existence-hiding when effectiveCapability null or below view. Wire requireOrgMember (the foundation slice's at-least-view/contribute guard) onto the slice routers from FND-P4-04 so cross-tenant access is impossible. The full cascade/visibility grid is exercised by unit tests now; the UI grid is a Phase-6 lane.
- **accept:** `pnpm --filter @docket/authz build` + unit tests: canActor allows an Owner `manage` org-wide, allows a Member `contribute`, denies a Guest `view` without a grant, and denies a cross-org actor (effectiveCapability null → 404 path). Integration on the slice: GET /v1/orgs/:otherOrgId/tasks with a session that has no Actor in that org returns 404/403 (NOT the rows) — the multi-tenant isolation gate. lastOwnerGuard rejects downgrading the sole owner with 409 last_owner_guard. orgContextMiddleware is registered via orgs.use before child routes (grep apps/api/src/routes/orgs.ts).

### `FND-P5-01` — @docket/ui foundation: tokens (globals.css OKLCH), Tailwind v4 setup, cn() util, contrast CI test, components.json per app _(P5 · sequential)_

- **depends on:** FND-P45-01
- **files:** `packages/ui/src/styles/globals.css`, `packages/ui/src/lib/utils.ts`, `packages/ui/src/lib/org-accent.ts`, `packages/ui/package.json`, `apps/web/components.json`, `apps/admin/components.json`, `apps/marketing/components.json`, `apps/web/app/globals.css`
- **spec:** design-system.md#0,#1,#2,#8, FROZEN:design-system/tw-animate-css (no tailwindcss-animate), FROZEN:design-system/state tokens keyed by WorkflowState.type, FROZEN:design-system/org accent hash from org.id palette, FROZEN:design-system/marketing consumes @docket/ui rsc:true, FROZEN:design-system/list engine @tanstack/react-virtual (drop react-arborist)
- **build:** @docket/ui (JIT, transpilePackages). globals.css: the full §1 token file — @import 'tailwindcss'; @import 'tw-animate-css'; :root OKLCH tokens + `@media (prefers-color-scheme: dark)` overrides; @theme inline mapping to color/radius/font utilities. RENAME the work-state tokens to the WorkflowState.type taxonomy: --state-backlog/--state-unstarted/--state-started/--state-completed/--state-canceled (was todo/in-progress/done) [FROZEN], wired through @theme inline as --color-state-_. lib/utils.ts: cn() (clsx + tailwind-merge). lib/org-accent.ts: ORG_ACCENT_PALETTE (8 OKLCH entries, contrast-tuned) + getOrgAccent(org)=palette[hash(org.id)%8] (deterministic, no DB column) [FROZEN]. Do not add CSS-token parser tests. package.json deps (pinned, via catalog where cross-cut): tailwindcss@^4, tw-animate-css, class-variance-authority, clsx, tailwind-merge, lucide-react, cmdk, sonner, vaul, @dnd-kit/_, @tanstack/react-virtual (NO react-arborist, NO tailwindcss-animate, NO next-themes) [FROZEN]; exports map for ./styles/globals.css, ./lib/utils, ./primitives, ./components, ./hooks, ./icons. Per-app components.json (style 'new-york', rsc:true incl. marketing [FROZEN], cssVariables:true, baseColor 'neutral', config:'', aliases ui→@docket/ui/primitives etc., iconLibrary 'lucide'). apps/web/app/globals.css: @import '@docket/ui/styles/globals.css'; @source the workspace ui src.
- **accept:** `pnpm --filter @docket/ui typecheck` passes; grep confirms --state-backlog/--state-started/--state-completed (NOT --state-todo/--state-in-progress/--state-done) and `@import "tw-animate-css"` (no tailwindcss-animate); package.json has @tanstack/react-virtual and NO react-arborist. cn() exists; getOrgAccent returns a stable palette entry for a given org id.

### `FND-P5-02` — @docket/ui: vendored shadcn primitives + slice atoms (Button/Card/Input/Badge/Avatar/Skeleton/DropdownMenu) _(P5 · parallel)_

- **depends on:** FND-P5-01
- **files:** `packages/ui/src/primitives/button.tsx`, `packages/ui/src/primitives/input.tsx`, `packages/ui/src/primitives/card.tsx`, `packages/ui/src/primitives/badge.tsx`, `packages/ui/src/primitives/avatar.tsx`, `packages/ui/src/primitives/skeleton.tsx`, `packages/ui/src/primitives/dropdown-menu.tsx`, `packages/ui/src/primitives/separator.tsx`, `packages/ui/src/icons/index.ts`
- **spec:** design-system.md#3.1,#3.2, FROZEN:design-system/normative component names, FROZEN:design-system/canonical names win over slice aliases
- **build:** Vendor the shadcn 'new-york' primitives the slice needs via `npx shadcn@latest add` into @docket/ui/src/primitives (committed, not a dependency): Button, Input, Card, Badge, Avatar, Skeleton, DropdownMenu, Separator (the slice subset of §3.1; do not over-vendor). Each consumes the semantic tokens from FND-P5-01 (cssVariables:true) — do NOT fork, extend via composition. icons/index.ts re-exports the lucide icons used by the shell/slice. These are the building blocks the slice screens and the shell components compose from.
- **accept:** `pnpm --filter @docket/ui typecheck` passes; each primitive imports cn() from @docket/ui/lib/utils and renders token utility classes (e.g. Button variant default uses bg-primary text-primary-foreground); a render smoke test (Vitest + @testing-library) mounts Button/Badge/Avatar without error; grep confirms primitives live under src/primitives (the components.json `ui` alias target).

### `FND-P5-03` — @docket/ui: app shell (AppShell, GlobalRail, ContextSidebar, ContextProvider, VocabularyProvider) + useVocabulary _(P5 · sequential)_

- **depends on:** FND-P5-02
- **files:** `packages/ui/src/components/shell/AppShell.tsx`, `packages/ui/src/components/shell/GlobalRail.tsx`, `packages/ui/src/components/shell/RailOrgAvatar.tsx`, `packages/ui/src/components/shell/ContextSidebar.tsx`, `packages/ui/src/components/shell/SidebarNavItem.tsx`, `packages/ui/src/components/shell/ContextProvider.tsx`, `packages/ui/src/hooks/useVocabulary.tsx`, `packages/ui/src/vocabulary/presets.ts`
- **spec:** design-system.md#4.1-4.3,#6, build-sequence-and-slice.md#2.4 (app shell), RECONCILIATION.md#32 (list engine), FROZEN:design-system/canonical names (OrgRail→GlobalRail, OrgSidebar→ContextSidebar), FROZEN:design-system/program skinnable + startup/nonprofit/agency presets, FROZEN:design-system/VocabularySkin resolution order, FROZEN:design-system/org accent on AppShell rebind
- **build:** Build the app shell the slice needs (canonical names — the slice's OrgRail/OrgSidebar are aliases that resolve to these). AppShell.tsx: client component holding ContextProvider (active context = Hub | org id), density, org-accent; on org rebind sets `--org-accent` inline (from getOrgAccent) + data-density on root; region layout (GlobalRail + ContextSidebar + MAIN) using shadcn Resizable. GlobalRail.tsx: persistent, never rebinds; Hub button + one RailOrgAvatar per org (from GET /v1/orgs) + AddOrgButton; selecting an avatar rebinds ContextProvider (240ms cross-fade). ContextSidebar.tsx + SidebarNavItem.tsx: org-scoped nav (My Work · Triage · Projects per IA §7) routed through useVocabulary. ContextProvider.tsx: rebind logic (active context, swaps sidebar/MAIN/org-accent). vocabulary/presets.ts: presetStartup/presetNonprofit/presetAgency full Record<VocabKey,{singular,plural}> (program skinnable: agency='Retainer'). useVocabulary.tsx: VocabularyProvider (scoped to active org; Hub uses presetStartup) + useVocabulary(key,{count}) resolver (org.vocabulary.overrides[key] → preset[key] → presetStartup[key]). Components NEVER hardcode entity labels.
- **accept:** `pnpm --filter @docket/ui typecheck` passes; a Vitest render of AppShell with a mock context renders GlobalRail + ContextSidebar; switching the active org in ContextProvider updates the `--org-accent` CSS var (assert via computed style); useVocabulary('program') returns 'Program' under startup and 'Retainer' under agency; grep confirms no hardcoded 'Project'/'Program' strings in SidebarNavItem (all via useVocabulary).

### `FND-P5-04` — @docket/ui: ListView family (custom virtualized flattened tree) + ListGroup/ListSubGroup/ListRow + useListKeyboard _(P5 · sequential)_

- **depends on:** FND-P5-03
- **files:** `packages/ui/src/components/views/ListView.tsx`, `packages/ui/src/components/views/ListGroup.tsx`, `packages/ui/src/components/views/ListSubGroup.tsx`, `packages/ui/src/components/views/ListRow.tsx`, `packages/ui/src/components/views/GroupHeader.tsx`, `packages/ui/src/components/atoms/StatusIcon.tsx`, `packages/ui/src/components/atoms/ActorAvatar.tsx`, `packages/ui/src/hooks/useListKeyboard.tsx`
- **spec:** design-system.md#5.1,#3.2,#8, RECONCILIATION.md#32 (custom virtualized tree), build-sequence-and-slice.md#2.4 (grouped List view), FROZEN:design-system/list engine @tanstack/react-virtual flattened tree, FROZEN:design-system/StatusIcon keys off WorkflowState.type, FROZEN:design-system/canonical names (TaskListView→ListView, TaskRow→ListRow)
- **build:** Build the workhorse ListView as a CUSTOM virtualized flattened tree on @tanstack/react-virtual (NO react-arborist [FROZEN]). ListView flattens (group-header + sub-group-header + row) into one virtual list; collapsed groups omit descendants; selection/keyboard via useListKeyboard; reorder via @dnd-kit (disabled under computed sort). ListGroup/ListSubGroup/GroupHeader render group boundaries with aria-expanded; ListRow is the row primitive with a Task-cell preset (the slice's TaskRow alias). atoms/StatusIcon.tsx: workflow-state ring/check colored by workflowState.TYPE (backlog/unstarted/started/completed/canceled) using --color-state-\* tokens [FROZEN] — keys off type, not the free-form state key. atoms/ActorAvatar.tsx: human/agent/team shape + ring rules. useListKeyboard: arrow/Enter/Esc grid navigation. ListView uses role='grid'/'row'/'gridcell', aria-selected, aria-busy. This is the component the slice's My-Work List view composes (groupBy='project' subGroupBy='state').
- **accept:** `pnpm --filter @docket/ui typecheck` + a Vitest render: ListView given mock tasks grouped by project → sub-grouped by state renders group headers + rows; a task with no project renders under a 'No project / Triage' group; StatusIcon for a state whose workflow type is 'backlog' uses the --state-backlog token and 'started' uses --state-started (assert class); collapsing a group hides its rows in the flattened set. grep confirms import from @tanstack/react-virtual and NO react-arborist.

## data-and-api (33 tickets)

### `DA-shared-01` — @docket/types primitives: branded ULID Id, per-entity ids, ListQuery/Page/IdParam, Capability, Problem, enums _(P6 · parallel)_

- **files:** `packages/types/src/primitives.ts`, `packages/types/src/query.ts`, `packages/types/src/enums.ts`, `packages/types/src/problem.ts`, `packages/types/src/index.ts`
- **spec:** api-rpc-contract.md §1.7, api-rpc-contract.md §5, data-model.md §0.1, RECONCILIATION.md #1/#6/#7/#13 (FROZEN decisions: ULID branded Id, flat Capability)
- **build:** Author the shared Zod foundation every per-entity schema depends on. (1) primitives.ts: `Id = z.string().regex(/^[0-9A-HJKMNP-TV-Z]{26}$/).brand('Id')` (Crockford ULID, 26 char) plus an `id<'Name'>()` factory returning the SAME runtime ULID validator with a phantom brand; export type-level brands OrganizationId, TeamId, InitiativeId, ProgramId, ProjectId, CycleId, TaskId, MilestoneId, UpdateId, CommentId, AgentId, SessionId, IntegrationId, NotificationId, ActorId, RoleId, GrantId, LabelId, InvitationId, SavedViewId; `DateString = z.iso.date()`. NO z.string().uuid() anywhere. (2) enums.ts: export Zod enums matching data-model pgEnums used in DTOs: Capability/GrantCapability = z.enum(['view','comment','contribute','assign','manage']) (GrantCapability is an alias), Health, Priority, Visibility, plus status enums referenced by DTOs (initiativeStatus, programStatus, projectStatus, cycleStatus, etc.). (3) query.ts: `ListQuery = { cursor?: string, limit: z.coerce.number().int().min(1).max(100).default(50), order?: 'asc'|'desc' }`, `page(schema)` factory producing `{ items: T[], nextCursor: string|null, total?: number }`, `IdParam(extra)` factory producing `z.object({ orgId: OrganizationId, ...extra })`. (4) problem.ts: RFC 9457 `Problem = { type, title, status, detail?, code, fieldErrors? }` plus a closed `code` enum exported for client switch. All schemas use `.meta({ id, description })`. Per FROZEN api-rpc decisions: drop CapabilityGrid entirely; capabilities are flat Capability[]. Mirror NO CapabilityGrid anywhere.
- **accept:** `pnpm --filter @docket/types typecheck` (tsc --noEmit) passes; `pnpm --filter @docket/types test` passes a unit test asserting Id accepts a 26-char Crockford ULID and rejects a UUID; `import { Id, ListQuery, page, Capability, Problem } from '@docket/types'` resolves.

### `DA-enums-01` — @docket/db enums.ts: complete pgEnum set incl. frozen additions (grant*effect, invitation_status, idempotency_status, view_scope, audit agent) *(P6 · parallel)\_

- **files:** `packages/db/src/enums.ts`
- **spec:** data-model.md §1, RECONCILIATION.md (FROZEN data-model decisions: grant_effect, invitation, idempotency, saved_view, audit agent)
- **build:** Declare every pgEnum from data-model §1 plus the FROZEN-decision additions, all in one file (single declaration site): the §1 list verbatim (actor_kind, actor_status, initiative_status, program_status, project_status, cycle_status, health, task_priority, provenance_source, sync_mode, session_trigger, session_status, session_activity_type, approval_status, approval_policy, grant_capability, grant_subject_kind, resource_kind, visibility, update_subject_type, comment_subject_type, notification_type, audit_subject_type, audit_event_type, integration_pattern, integration_role, integration_status, daily_plan_item_status, org_lifecycle_state, staff_role) AND additions: `grant_effect = pgEnum('grant_effect',['allow','deny'])`, `invitation_status = pgEnum('invitation_status',['pending','accepted','revoked','expired'])`, `idempotency_status = pgEnum('idempotency_status',['in_progress','completed'])`, `view_scope = pgEnum('view_scope',['personal','team','organization'])`. Per FROZEN data-model decision, ADD 'agent' to audit_subject_type giving [task,project,program,initiative,cycle,team,actor,integration,session,update,comment,agent]. Do NOT add daily_plan_item or grant_request to any subject enum.
- **accept:** `pnpm --filter @docket/db typecheck` passes; `pnpm --filter @docket/db db:generate` emits a migration referencing all listed enums including grant_effect/invitation_status/idempotency_status/view_scope and audit_subject_type containing 'agent'.

### `DA-types-jsonb-01` — @docket/types jsonb shapes: VocabularySkin, HubPreferences, ApprovalRouting, AgentConnection, IntegrationConnection, WorkflowState, ViewFilter/Grouping/Sort, NotificationBody, SessionActivityBody _(P6 · parallel)_

- **depends on:** DA-shared-01
- **files:** `packages/types/src/jsonb.ts`, `packages/types/src/index.ts`
- **spec:** data-model.md §0.5, RECONCILIATION.md (FROZEN design-system: VocabularySkin/HubPreferences shapes; FROZEN data-model: ApprovalRouting, ViewFilter/Grouping/Sort)
- **build:** Author the canonical Zod source for every $type<T> jsonb blob the DB schema and DTOs reference (data-model §0.5; design-system owns VocabularySkin/HubPreferences). Export: WorkflowState = { key, name, type: z.enum(['backlog','unstarted','started','completed','canceled']), position } and defaultWorkflowStates const (first key 'backlog'); VocabularySkin = { preset: z.enum(['startup','nonprofit','agency']), overrides?: Partial<Record<VocabKey,{singular,plural}>> }; HubPreferences (all optional, theme/density/railOrder/railPins/sidebar/properties collapse+width/landing/viewState per FROZEN design-system decision); ApprovalRouting = { mode: 'assigner'|'specific_actor'|'team_assignees', actorId?: Id, teamId?: Id }; AgentConnection = { endpoint, protocol, credentialsRef }; IntegrationConnection = { credentialsRef, account? }; ViewFilter/ViewGrouping/ViewSort (for saved_view); NotificationBody; SessionActivityBody; GrantCapability[] alias from DA-shared-01. These are imported by @docket/db for `$type<>` (re-derived) and by DTO tickets. Use .meta({id}).
- **accept:** `pnpm --filter @docket/types typecheck` passes; a unit test asserts VocabularySkin defaults preset='startup' and WorkflowState[0] key='backlog'; `pnpm --filter @docket/types test` green.

### `DA-org-ext-01` — organization table extension (approval*routing column) + orgs router non-slice routes (get/patch/delete/export) *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01
- **files:** `packages/db/src/schema/identity.ts`, `packages/types/src/org.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** api-rpc-contract.md §3.1, data-model.md §3.3, RECONCILIATION.md (FROZEN: org.approval_routing column; v1 export contract)
- **build:** Slice builds POST/GET /orgs and the organization table base. Add: (1) DB column `approvalRouting: jsonb('approval_routing').$type<ApprovalRouting>()` (nullable) on organization per FROZEN three-level approval-routing decision. (2) DTOs in @docket/types: OrgOut{ id, name, slug, avatar?, isPersonal, vocabulary, agentGuidance, lifecycleState }, OrgUpdate{ name?, slug?, avatar?, vocabulary?, agentGuidance?, approvalRouting? }, OrgSummary (for GET /orgs list, attention badge). (3) Routes on the existing chained `orgs` Hono router (do not break the chain): `GET /:orgId` (org:view → OrgOut), `PATCH /:orgId` (org:manage → OrgUpdate→OrgOut), `DELETE /:orgId` (org:manage Owner-only → {id, lifecycleState:'pending_deletion', deleteAfterAt} sets lifecycle pipeline), `GET /:orgId/export?format=json|zip` (org:manage → {downloadUrl, expiresAt}, real work-layer JSON export per FROZEN api decision, signed short-TTL URL, secrets/Stripe/other tenants excluded). Each route: validator(input) + describeRoute(resolver(OutSchema), security:[{bearerAuth:[]}], x-docket-capability) + ok(c,Out,data). POST /orgs accepts optional Idempotency-Key (DA-infra-idem-01).
- **accept:** `pnpm --filter apps/api typecheck` passes; `pnpm --filter @docket/db db:generate` emits a migration adding organization.approval_routing; a scratch `hc<AppType>` consumer autocompletes `client.orgs[':orgId'].$get` and `client.orgs[':orgId'].export.$get`; `GET /v1/openapi.json` lists the new org routes with capability annotations.

### `DA-members-01` — Membership routes (orgs/:orgId/members) + invitation table + members CRUD + invite _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01
- **files:** `packages/db/src/schema/identity.ts`, `packages/types/src/member.ts`, `packages/types/src/invitation.ts`, `apps/api/src/routes/members.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** api-rpc-contract.md §3.1 (members rows), RECONCILIATION.md (FROZEN: invitation table, personal-org guard, card-gate env flag, last-owner guard)
- **build:** The slice builds the actor table but no membership routes and no invitation table. (1) DB: add the hand-built `invitation` table to identity.ts per FROZEN data-model decision: id PK $defaultFn(genId), organization_id NOT NULL→organization CASCADE, email NOT NULL, role_id NOT NULL→role RESTRICT, as_guest boolean default false, token NOT NULL, status invitation_status default 'pending', invited_by→actor SET NULL, expires_at NOT NULL, created_at default now(), accepted_at; indexes uniqueIndex(token), partial uniqueIndex(organizationId,email) WHERE status='pending', index(organizationId). Token is a random 32-byte url-safe string (app-generated, not ULID). (2) DTOs: ActorOut{ id, kind, displayName, avatar?, status, role?, userId?, isGuest }, InvitationOut{ invitationId, email, role, status:'pending' }, MemberInvite{ email, role: RoleRef, asGuest? }, MemberUpdate{ role?: RoleRef, status?:'active'|'suspended' }. (3) Routes (mounted via orgs.route('/:orgId/members', members)): GET /members (org:view, ListQuery & {kind?}→Page<ActorOut>), POST /members/invite (org:manage→InvitationOut; rejects 403 personal_org_no_invites when organization.is_personal; data-driven card-gate via DOCKET_REQUIRE_CARD_FOR_INVITE→402 card_required), PATCH /members/:actorId (org:manage→ActorOut; last-owner guard), DELETE /members/:actorId (org:manage→{id, removed:true}). Wire into orgs router chain.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the invitation table with the partial-pending unique index; `client.orgs[':orgId'].members.invite.$post` autocompletes with {email, role, asGuest?}; openapi.json shows members routes tagged.

### `DA-role-01` — role table extension (key/is*system/base_capability/default_visibility) + roles CRUD routes *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/role.ts`, `apps/api/src/routes/roles.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** api-rpc-contract.md §3.1 (roles), data-model.md §6.1, RECONCILIATION.md #3 (FROZEN: flat capabilities, role key/is_system/base_capability/default_visibility, drop CapabilityGrid)
- **build:** Slice seeds 4 system roles but builds no role-mgmt routes; the role table needs the FROZEN columns. (1) DB: canonical role table per FROZEN data-model/permissions decision: id, organization_id NOT NULL CASCADE, key text NOT NULL, name text NOT NULL, is_system boolean default false, capabilities jsonb $type<GrantCapability[]> default [], base_capability grant_capability (nullable), default_visibility visibility NOT NULL default 'public'; uniqueIndex(organization_id, key) AND keep (organization_id, name). (2) DTOs (flat capabilities, NO CapabilityGrid): RoleOut{ id, key, name, isSystem, baseCapability: Capability|null, defaultVisibility, capabilities: Capability[] }, RoleCreate{ key?, name, baseCapability: Capability|null, defaultVisibility }, RoleUpdate{ name?, baseCapability?, defaultVisibility? } (key immutable). (3) Routes (orgs.route('/:orgId/roles', roles)): GET /roles (org:view→RoleOut[]), POST /roles (org:manage→RoleOut; reject reserved keys owner/admin/member/guest), PATCH /roles/:roleId (org:manage→RoleOut), DELETE /roles/:roleId (org:manage→{id, removed:true}; 409 on is_system). Wire into orgs chain.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits role with key/is_system/base_capability/default_visibility and uniqueIndex(organization_id,key); a unit/type check confirms RoleOut.capabilities is Capability[] (flat, not a Record); `client.orgs[':orgId'].roles.$post` autocompletes flat capabilities; openapi shows roles routes.

### `DA-grant-01` — grant table (folded permissions columns) + grants routes (GET/PUT/DELETE) _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/grant.ts`, `apps/api/src/routes/grants.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** data-model.md §6.1, api-rpc-contract.md §3.1 (grants), RECONCILIATION.md #2/#3/#4 (FROZEN: one grant table, folded columns, delete permission_grant, deny deferred)
- **build:** New table + routes (not in slice). (1) DB: single canonical `grant` table per FROZEN decision (delete permission_grant entirely): id, organization_id NOT NULL CASCADE, subject_kind grant_subject_kind {actor,role}, subject_id text (polymorphic, app-enforced), resource_kind resource_kind, resource_id text (polymorphic), capabilities jsonb $type<GrantCapability[]> NOT NULL, effect grant_effect default 'allow', cascades boolean NOT NULL default true, visibility_override visibility (nullable), expires_at timestamptz (nullable), visibility visibility NOT NULL default 'public', created_by→actor SET NULL, created_at default now(). Indexes: grant_org_idx, grant_subject_idx(org,subject_kind,subject_id), grant_resource_idx(org,resource_kind,resource_id), uniqueIndex('grant_subject_resource_effect_uq').on(subject_kind,subject_id,resource_kind,resource_id,effect). (2) DTOs against branded Id: ActorRef={kind:'actor',id:Id}, RoleRef={kind:'role',id:Id}, ResourceRef={kind:ResourceKind,id:Id}, GrantOut{ id, subject, resource, capabilities: Capability[], visibility }, GrantUpsert{ subject: ActorRef|RoleRef, resource: ResourceRef, capabilities: Capability[] (min 1), visibility default 'public', cascades?, expiresAt? } (NO effect field in v1 — server writes allow). (3) Routes (orgs.route('/:orgId/grants', grants)): GET /grants (org:manage, {subjectActorId?, resourceType?, resourceId?}→GrantOut[]), PUT /grants (org:manage→GrantOut upsert; self-escalation guard), DELETE /grants/:grantId (org:manage→{id, removed:true}). Wire into orgs chain.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits grant with effect/cascades/visibility_override/expires_at and the 5-column unique index, and NO permission_grant table exists; `client.orgs[':orgId'].grants.$put` autocompletes with tagged subject/resource refs; openapi shows grants routes (org:manage).

### `DA-billing-01` — Billing routes (orgs/:orgId/billing + checkout + portal) over Better Auth Stripe subscription _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/types/src/billing.ts`, `apps/api/src/routes/billing.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** api-rpc-contract.md §3.1 (billing), data-model.md §2 (subscription), §3.3 (lifecycle cols), RECONCILIATION.md (FROZEN env: BILLING_ENABLED gate, no-card trial)
- **build:** New routes (no new table — subscription is the Better Auth Stripe CLI table keyed reference_id=organization.id; lifecycle cols live on organization). (1) DTOs: BillingOut{ lifecycleState, plan, status, currentPeriodEnd?, trialEndsAt?, exportReadyAt?, deleteAfterAt? }, CheckoutCreate{ plan, returnUrl }, CheckoutOut{ checkoutClientSecret }, PortalCreate{ returnUrl }, PortalOut{ url }. (2) Routes (orgs.route('/:orgId/billing', billing)): GET /billing (org:manage→BillingOut reading organization lifecycle cols + subscription row), POST /billing/checkout (org:manage→CheckoutOut embedded Checkout), POST /billing/portal (org:manage→PortalOut). All gated by BILLING_ENABLED env (when false, return a typed 402 Problem per FROZEN env decision — never a stub). Use describeRoute + resolver + ok helper. Reads must not invent subscription columns beyond the CLI-generated ones.
- **accept:** `pnpm --filter apps/api typecheck` passes; `client.orgs[':orgId'].billing.$get` and `.billing.checkout.$post` autocomplete; openapi shows billing routes tagged org:manage; with BILLING_ENABLED unset the checkout route returns a typed Problem (not a thrown stub).

### `DA-team-ext-01` — team table extension (approval*routing) + team_member join (non-slice routes) + teams sub-routes (members) *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01
- **files:** `packages/db/src/schema/identity.ts`, `packages/types/src/team.ts`, `apps/api/src/routes/teams.ts`
- **spec:** api-rpc-contract.md §3.2, data-model.md §3.5, RECONCILIATION.md (FROZEN: team.approval_routing column; team_member retains organization_id)
- **build:** Slice builds team table + team CRUD core and inserts a creator team_member row; this ticket adds the non-slice surface. (1) DB: add `approvalRouting: jsonb('approval_routing').$type<ApprovalRouting>()` (nullable) to team per FROZEN approval-routing decision; confirm team_member is the thin join {team_id→team CASCADE, actor_id→actor CASCADE, organization_id→organization CASCADE, primaryKey(team_id,actor_id), index(actor_id)} with NO audit columns (retain organization_id per §0.2 — do not remove). (2) DTOs: TeamOut{ id, name, key, description?, workflowStates[], triageEnabled, agentGuidance? }, TeamCreate, TeamUpdate (full §3.2 field set). (3) Routes (the teams Hono router, mounted orgs.route('/:orgId/teams', teams)): GET / (org:view→Page<TeamOut>), POST / (org:manage→TeamOut), GET /:teamId (org:view), PATCH /:teamId (org:manage), DELETE /:teamId (org:manage→{id, archivedAt} soft-delete), GET /:teamId/members (org:view→Page<ActorOut> via team_member join), PUT /:teamId/members/:actorId (org:manage→{teamId, actorId, added:true} inserts team_member). Note: if the slice already shipped basic teams CRUD, extend the same router file without duplicating; this ticket owns members sub-routes + approval_routing column regardless.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits team.approval_routing and confirms team_member carries organization_id; `client.orgs[':orgId'].teams[':teamId'].members.$get` autocompletes; openapi shows team members routes.

### `DA-initiative-01` — initiative entity: table + DTOs + initiatives CRUD routes (incl. members rollup + timeline) _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/work.ts`, `packages/types/src/initiative.ts`, `apps/api/src/routes/initiatives.ts`
- **spec:** data-model.md §4.1, api-rpc-contract.md §3.3, RECONCILIATION.md (FROZEN: initiative not grantable — no visibility/ancestor_path)
- **build:** New entity. (1) DB: `initiative` per data-model §4.1 with auditColumns() + name, description?, owner_id→actor SET NULL, status initiative_status default 'active', target_date?, health?. Note: initiative does NOT get visibility/ancestor_path (FROZEN: only team/program/project/task are grantable containment tables). Indexes: index('initiative_org_idx'). (2) DTOs: InitiativeOut{ id, name, description, ownerId, status, targetDate?, health?, childMix{programs,projects} }, InitiativeCreate{ name, description?, ownerId?, targetDate? }, InitiativeUpdate{ name?, description?, ownerId?, status?, targetDate? }. (3) initiatives Hono router (orgs.route('/:orgId/initiatives', initiatives)): GET / (org:view, ListQuery & {status?,health?}→Page<InitiativeOut>), POST / (org:contribute→InitiativeOut, accepts Idempotency-Key), GET /:initiativeId (org:view), PATCH /:initiativeId (org:contribute), DELETE /:initiativeId (org:manage→{id, archivedAt}), GET /:initiativeId/members (org:view→Page<{kind:'program'|'project',id,name,health?}> m2m rollup), GET /:initiativeId/timeline (org:view→{programs:Lane[],projects:Bar[]}). The PUT/DELETE link routes for projects/programs live in DA-initiative-links-01.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the initiative table; `client.orgs[':orgId'].initiatives.$post` autocompletes; openapi lists initiatives routes with capability annotations.

### `DA-initiative-links-01` — initiative*project + initiative_program join tables + initiative m2m link routes *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-initiative-01
- **files:** `packages/db/src/schema/joins.ts`, `apps/api/src/routes/initiatives.ts`
- **spec:** data-model.md §7.1, §7.2, api-rpc-contract.md §3.3, RECONCILIATION.md (FROZEN §0.2: joins retain organization_id)
- **build:** New join tables + the link/unlink routes (extend the initiatives router from DA-initiative-01). (1) DB: initiative_project { initiative_id→initiative CASCADE, project_id→project CASCADE, organization_id→organization CASCADE, primaryKey(initiative_id,project_id), index(project_id) } and initiative_program { initiative_id→initiative CASCADE, program_id→program CASCADE, organization_id→organization CASCADE, primaryKey(initiative_id,program_id), index(program_id) }; each retains organization_id per §0.2 (do not optimize away). (2) Routes added to the same chained initiatives router: PUT /:initiativeId/projects/:projectId (org:contribute→{initiativeId,projectId,linked:true} writes initiative_project), DELETE /:initiativeId/projects/:projectId (org:contribute→{unlinked:true}), PUT /:initiativeId/programs/:programId (org:contribute→{initiativeId,programId,linked:true}), DELETE /:initiativeId/programs/:programId (org:contribute→{unlinked:true}). Method-chained, no const break.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits both join tables with organization_id columns; `client.orgs[':orgId'].initiatives[':initiativeId'].projects[':projectId'].$put` autocompletes; openapi lists the four link routes.

### `DA-program-01` — program entity: table + visibility/ancestor*path columns + DTOs + programs CRUD routes (incl. work + updates rollups) *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/work.ts`, `packages/types/src/program.ts`, `apps/api/src/routes/programs.ts`
- **spec:** data-model.md §4.2, api-rpc-contract.md §3.4, RECONCILIATION.md #5 (FROZEN: program gets visibility + ancestor_path + GIN)
- **build:** New entity (no 'completed' status). (1) DB: `program` per §4.2 with auditColumns() + name, description?, owner_id→actor SET NULL, status program_status default 'active' (active|paused|archived), health?; PLUS FROZEN grantable-container columns: visibility visibility NOT NULL default 'public' and ancestor_path text[] NOT NULL default '{}' with GIN index program_ancestor_path_gin. Index program_org_idx. (2) DTOs: ProgramOut{ id, name, description, ownerId, status, health?, flow{throughput,wip} }, ProgramCreate{ name, description?, ownerId? }, ProgramUpdate{ name?, description?, ownerId?, status? } (status restricted to active/paused/archived). (3) programs router (orgs.route('/:orgId/programs', programs)): GET / (org:view, {status?,health?}→Page<ProgramOut>), POST / (org:contribute→ProgramOut, Idempotency-Key), GET /:programId (org:view), PATCH /:programId (org:contribute), DELETE /:programId (org:manage→{id, archivedAt}), GET /:programId/work (org:view, {cycleId?,projectId?}→grouped-by-cycle/segmented-by-project), GET /:programId/updates (org:view→Page<UpdateOut> — reads update entity, depends on DA-update-01 type only).
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits program with visibility + ancestor_path (GIN index) and program_status has no 'completed'; `client.orgs[':orgId'].programs.$post` autocompletes; openapi lists programs routes.

### `DA-project-ext-01` — project table extension (visibility/ancestor*path) + projects non-slice routes (get/patch/delete/tasks/updates/agents) + project DTOs *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/work.ts`, `packages/types/src/project.ts`, `apps/api/src/routes/projects.ts`
- **spec:** data-model.md §4.3, api-rpc-contract.md §3.5, RECONCILIATION.md #5 (FROZEN: project gets visibility + ancestor_path + GIN)
- **build:** Slice builds project table base + POST/GET /projects. Add: (1) DB FROZEN grantable-container columns on project: visibility visibility NOT NULL default 'public', ancestor_path text[] NOT NULL default '{}' with GIN index project_ancestor_path_gin (keep existing org/program/team indexes). (2) DTOs: ProjectOut{ id, name, description, leadId?, programId?, status, health?, startDate?, targetDate?, teamId?, progress{weighted,completed,total} }, ProjectCreate{ name, description?, leadId?, programId?, teamId?, startDate?, targetDate?, initiativeIds? } (organizationId from path), ProjectUpdate{ name?, description?, leadId?, programId?, status?, startDate?, targetDate?, teamId? }. (3) Routes on the existing projects router (extend chain): GET /:projectId (org:view→ProjectOut), PATCH /:projectId (org:contribute→ProjectOut), DELETE /:projectId (org:manage→{id, archivedAt}), GET /:projectId/tasks (org:view, {groupBy?:'status'|'milestone', state?, assigneeId?}→{groups:TaskGroup[]} default group-by-milestone), GET /:projectId/updates (org:view→Page<UpdateOut>), GET /:projectId/agents (org:view→{agents:AgentRef[], lastSession?, pendingApprovals:number}). POST /projects (slice) extended to honor initiativeIds (writes initiative_project) and Idempotency-Key.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits project.visibility + project.ancestor_path with GIN index; `client.orgs[':orgId'].projects[':projectId'].tasks.$get` autocompletes with groupBy; openapi lists the new project sub-routes.

### `DA-milestone-01` — milestone entity: table + DTOs + nested milestones routes under projects _(P6 · parallel)_

- **depends on:** DA-shared-01
- **files:** `packages/db/src/schema/work.ts`, `packages/types/src/milestone.ts`, `apps/api/src/routes/milestones.ts`, `apps/api/src/routes/projects.ts`
- **spec:** data-model.md §4.6, api-rpc-contract.md §3.5, RECONCILIATION.md (FROZEN: milestone hard-delete returns {id,removed:true}, nulls task.milestone_id)
- **build:** New entity (nested under project). (1) DB: `milestone` per §4.6: id PK, organization_id NOT NULL CASCADE, project_id NOT NULL→project CASCADE, name NOT NULL, target_date?, sort integer default 0, created_at default now(); index milestone_project_idx. (2) DTOs: Milestone{ id, projectId, name, targetDate?, sort }, MilestoneCreate{ name, targetDate?, sort? }, MilestoneUpdate{ name?, targetDate?, sort? }. (3) Routes: a milestones Hono router mounted under projects as projects.route('/:projectId/milestones', milestones) (keep chaining intact): GET / (org:view→Milestone[]), POST / (org:contribute→Milestone, Idempotency-Key), PATCH /:milestoneId (org:contribute→Milestone), DELETE /:milestoneId (org:contribute→{id, removed:true}; FROZEN hard-delete and additionally nulls dependent tasks' milestone_id). Use describeRoute + resolver + ok.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the milestone table; `client.orgs[':orgId'].projects[':projectId'].milestones.$post` autocompletes; openapi lists milestone routes; DELETE returns {id, removed:true} shape per spec.

### `DA-cycle-01` — cycle entity: table + DTOs + cycles CRUD routes (incl. burnup/tasks/close) _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/work.ts`, `packages/types/src/cycle.ts`, `apps/api/src/routes/cycles.ts`
- **spec:** data-model.md §4.4, api-rpc-contract.md §3.6, RECONCILIATION.md (FROZEN: cycle stats/burnup computed, close SERIALIZABLE)
- **build:** New entity (team-scoped). (1) DB: `cycle` per §4.4 with auditColumns() + team_id NOT NULL→team CASCADE, number integer NOT NULL, name?, starts_at NOT NULL, ends_at NOT NULL, status cycle_status default 'upcoming'; indexes cycle_team_idx, uniqueIndex cycle_team_number_uq(team_id, number). (2) DTOs (stats COMPUTED, not stored): CycleOut{ id, teamId, number, name?, startsAt, endsAt, status, stats{committed,completed,capacity,scopeChange,carryover} }, CycleCreate{ teamId, name?, startsAt, endsAt } (number auto-assigned), CycleUpdate{ name?, startsAt?, endsAt?, status? }, CycleClose{ carryover:{taskId, action:'keep'|'move'|'triage', targetCycleId?}[] }. (3) cycles router (orgs.route('/:orgId/cycles', cycles)): GET / (org:view, {teamId:TeamId required, status?}→Page<CycleOut>), POST / (org:contribute→CycleOut, Idempotency-Key), GET /:cycleId (org:view), PATCH /:cycleId (org:contribute), GET /:cycleId/burnup (org:view→{series,scopeChanges} computed from audit_event), GET /:cycleId/tasks (org:view, {groupBy?:'project'|'program'}→{groups:TaskGroup[]}), POST /:cycleId/close (org:contribute→{closed:true,movedCount,triagedCount}; SERIALIZABLE tx; 409 cycle_already_closed; same-team targetCycle else 422).
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the cycle table with uniqueIndex(team_id,number); `client.orgs[':orgId'].cycles[':cycleId'].close.$post` autocompletes; openapi lists cycle routes including burnup and close.

### `DA-task-ext-01` — task table extension (visibility/ancestor*path) + tasks non-slice routes (get-detail/patch/assign/move/state/delete/subtasks/triage) + task DTOs *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/work.ts`, `packages/types/src/task.ts`, `apps/api/src/routes/tasks.ts`
- **spec:** data-model.md §4.5, api-rpc-contract.md §3.7, RECONCILIATION.md #5 + FROZEN api decisions (task visibility/ancestor_path; single provenance, no externalLinks; reparent via /move; estimate unitless app-enforced kinds)
- **build:** Slice builds task table base + POST/GET /tasks. Add: (1) DB FROZEN grantable-container columns on task: visibility visibility NOT NULL default 'public', ancestor_path text[] NOT NULL default '{}' with GIN index task_ancestor_path_gin (keep all existing task indexes incl. task_source_uq partial unique). estimate stays a unitless nullable integer (no unit column). (2) DTOs: TaskOut{ ...all columns, provenance:{source,sourceIntegrationId?,externalId?,externalUrl?,syncMode?}, blocking:TaskRef[], blockedBy:TaskRef[], subtasks:TaskRef[] } (FROZEN: externalLinks REMOVED for v1, provenance inline single), TaskUpdate{ title?, description?, state?, priority?, estimate?, dueDate?, labels? } (FROZEN: reparenting fields REMOVED — go through /move). (3) Routes on the existing tasks router (extend chain): GET /:taskId (org:view→TaskOut detail), PATCH /:taskId (org:contribute→TaskOut; state change runs same side-effects as /state; 409 on linked non-link fields), POST /:taskId/assign (org:assign, {assigneeId?, delegateId?}; app-enforced actor-kind), POST /:taskId/move (org:contribute, {toProjectId?,toProgramId?,toMilestoneId?,toCycleId?,toTriage?}; recomputes ancestor_path transactionally), POST /:taskId/state (org:contribute, validated vs team.workflow_states; sets completed_at/canceled_at on terminal; audit state_changed), DELETE /:taskId (org:contribute→{id, archivedAt}), GET /:taskId/subtasks (org:view→Page<TaskOut> where parent_task_id), GET /triage (org:view, {teamId?}→Page<TaskOut>). GET / extended with full TaskFilter.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits task.visibility + task.ancestor_path with GIN index; `client.orgs[':orgId'].tasks[':taskId'].move.$post` autocompletes; openapi lists task sub-routes; a type check confirms TaskUpdate has no projectId/parentTaskId fields.

### `DA-task-deps-01` — task*dependency table (acyclic) + dependency routes + link-external route (single inline provenance) *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-task-ext-01
- **files:** `packages/db/src/schema/joins.ts`, `apps/api/src/routes/tasks.ts`, `packages/types/src/task-dependency.ts`
- **spec:** data-model.md §7.4, api-rpc-contract.md §3.7, RECONCILIATION.md (FROZEN: single inline provenance, link-external singular, dependency SERIALIZABLE acyclicity)
- **build:** New join table + the dependency/link sub-routes (extend the tasks router). (1) DB: task_dependency per §7.4: blocking_task_id→task CASCADE, blocked_task_id→task CASCADE, organization_id→organization CASCADE, created_at default now(); primaryKey(blocking_task_id,blocked_task_id), index(blocked_task_id), check('task_dependency_no_self', blocking<>blocked). (2) DTOs: DependencyCreate{ blockedTaskId: TaskId, relation:'blocks' }, dependency read shape {blocking:TaskRef[], blockedBy:TaskRef[]}. (3) Routes added to the chained tasks router: GET /:taskId/dependencies (org:view→{blocking,blockedBy} each ref carries project), POST /:taskId/dependencies (org:contribute→{created:true}; SERIALIZABLE tx running recursive-CTE reachability per §7.4; 409 CycleError code dependency_cycle if it would cycle; both tasks must share organization_id), DELETE /:taskId/dependencies/:edgeId (org:contribute→{removed:true}). PLUS the renamed single-provenance link routes per FROZEN api decision: POST /:taskId/link-external (org:contribute, {integrationId, externalId, externalUrl, role}→TaskOut; sets inline provenance source='linked' sync_mode='mirror'; 409 task_already_linked) and DELETE /:taskId/link-external (org:contribute→TaskOut; clears provenance back to native). No external_link collection table.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits task_dependency with the self-edge check and composite PK; `client.orgs[':orgId'].tasks[':taskId'].dependencies.$post` autocompletes; openapi lists dependency + link-external routes; a unit test for the cycle check rejects a back-edge with code dependency_cycle.

### `DA-label-01` — label entity: table (scope-aware unique) + task*label join + DTOs + labels routes *(P6 · parallel)\_

- **depends on:** DA-shared-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/db/src/schema/joins.ts`, `packages/types/src/label.ts`, `apps/api/src/routes/labels.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** data-model.md §6.6, §7.3, RECONCILIATION.md (FROZEN: per-org-global + per-team partial unique indexes, #RRGGBB color)
- **build:** New entity + its m2m join. (1) DB: `label` per §6.6: id PK, organization_id NOT NULL CASCADE, name NOT NULL, color text NOT NULL (#RRGGBB validated in Zod), group text?, team_id→team CASCADE (nullable scope). Per FROZEN decision replace the single label_org_name_uq with TWO partial unique indexes: uniqueIndex('label_org_name_global_uq').on(organizationId,name).where(team_id IS NULL) and uniqueIndex('label_team_name_uq').on(organizationId,teamId,name).where(team_id IS NOT NULL); keep index(organizationId). task_label join per §7.3: { task_id→task CASCADE, label_id→label CASCADE, organization_id→organization CASCADE, primaryKey(task_id,label_id), index(label_id) }. (2) DTOs: LabelOut{ id, name, color, group?, teamId? }, LabelCreate{ name, color, group?, teamId? } (color z.string().regex(/^#[0-9A-Fa-f]{6}$/)), LabelUpdate{ name?, color?, group? }. (3) labels router (orgs.route('/:orgId/labels', labels)): GET / (org:view→Page<LabelOut>), POST / (org:contribute→LabelOut), PATCH /:labelId (org:contribute→LabelOut), DELETE /:labelId (org:contribute→{id, removed:true} hard-delete).
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits label with the two partial unique indexes and the task_label join; `client.orgs[':orgId'].labels.$post` autocompletes; a Zod unit test rejects a non-#RRGGBB color; openapi lists label routes.

### `DA-comment-01` — comment entity: table + DTOs + comments routes (polymorphic subject) _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/comment.ts`, `apps/api/src/routes/comments.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** data-model.md §6.7, api-rpc-contract.md §3.8, RECONCILIATION.md (FROZEN: comment subject enum confirmed; hard-delete returns {removed:true})
- **build:** New entity (polymorphic, app-enforced subject integrity). (1) DB: `comment` per §6.7: id PK, organization_id NOT NULL CASCADE, author_id→actor SET NULL, subject_type comment_subject_type {task,project,program,initiative,cycle,update}, subject_id text, body text NOT NULL, parent_comment_id→comment CASCADE (self-FK), created_at default now(), edited_at?; indexes comment_subject_idx(org,subject_type,subject_id,created_at), comment_parent_idx(parent_comment_id). (2) DTOs: CommentOut{ id, authorId, subject:{type,id}, body, parentCommentId?, createdAt, editedAt? }, CommentCreate{ subjectType, subjectId, body, parentCommentId? }, CommentUpdate{ body }. (3) comments router (orgs.route('/:orgId/comments', comments)): GET / (org:view, {subjectType, subjectId}→Page<CommentOut>), POST / (org:comment→CommentOut, Idempotency-Key), PATCH /:commentId (org:comment author-only→CommentOut sets editedAt), DELETE /:commentId (org:comment author OR org:manage→{id, removed:true} hard-delete).
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the comment table with self-FK + subject index; `client.orgs[':orgId'].comments.$post` autocompletes with subjectType/subjectId; openapi lists comment routes (org:comment).

### `DA-update-01` — update entity: table + DTOs + updates routes (drives subject health) _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/update.ts`, `apps/api/src/routes/updates.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** data-model.md §6.2, api-rpc-contract.md §3.9, RECONCILIATION.md (FROZEN: update_subject_type stays project/program/initiative)
- **build:** New entity. (1) DB: `update` per §6.2: id PK, organization_id NOT NULL CASCADE, author_id→actor SET NULL, subject_type update_subject_type {project,program,initiative}, subject_id text, health health NOT NULL, body text NOT NULL, created_at default now(); index update_subject_idx(org,subject_type,subject_id,created_at). (2) DTOs: UpdateOut{ id, authorId, subject:{type,id}, health, body, createdAt }, UpdateCreate{ subjectType, subjectId, health:'on_track'|'at_risk'|'off_track', body }. (3) updates router (orgs.route('/:orgId/updates', updates)): GET / (org:view, {subjectType, subjectId}→Page<UpdateOut>), POST / (org:contribute→UpdateOut; recomputes the subject's current health column from the latest update, Idempotency-Key), GET /:updateId (org:view→UpdateOut), DELETE /:updateId (org:contribute author OR org:manage→{id, removed:true}; recomputes health to prior update).
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the update table with subject index; `client.orgs[':orgId'].updates.$post` autocompletes; openapi lists update routes; a unit test confirms POST recomputes subject health to the new value.

### `DA-agent-01` — agent entity: table + DTOs + agents routes (identity, grants, grant-requests) _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01, DA-grant-01
- **files:** `packages/db/src/schema/agents.ts`, `packages/types/src/agent.ts`, `apps/api/src/routes/agents.ts`
- **spec:** data-model.md §5.5, api-rpc-contract.md §3.10, RECONCILIATION.md (FROZEN: agent.approval_routing kept; grant-requests modeled as session_activity proposals, no new table/enum)
- **build:** New entity (1:1 with actor{kind:'agent'}). (1) DB: `agent` per §5.5: id PK, organization_id NOT NULL CASCADE, actor_id NOT NULL UNIQUE→actor CASCADE, connection jsonb $type<AgentConnection> NOT NULL, approval_policy approval_policy default 'suggest', accountable_owner_id→actor SET NULL, guidance text?, approval_routing jsonb $type<ApprovalRouting> (nullable, keep per FROZEN), created_at/updated_at; index agent_org_idx. (2) DTOs: AgentOut{ id, displayName, provider, status, grants: Capability[], approvalPolicy, accountableOwnerId, guidance? }, AgentCreate{ displayName, connection{endpoint,protocol,credentialsRef}, approvalPolicy?, accountableOwnerId, guidance? }, AgentUpdate{ displayName?, approvalPolicy?, accountableOwnerId?, guidance?, status? }, GrantOnRequest{ capabilities: Capability[], resource?: ResourceRef }. (3) agents router (orgs.route('/:orgId/agents', agents)): GET / (org:view→Page<AgentOut>), POST / (org:manage→AgentOut; creates the agent actor + agent row, starts read-only/empty grants, Idempotency-Key), GET /:agentId (org:view), PATCH /:agentId (org:manage), DELETE /:agentId (org:manage→{id, archivedAt}), PUT /:agentId/grants (org:manage→Grant writes agent-actor grant), GET /:agentId/grant-requests (org:view→Page modeled as session_activity proposals per FROZEN — type='action', approval_status='proposed', body.kind='grant_request'), POST /:agentId/grant-requests/:requestId/decide (org:manage→{id, status}; approve writes an actor-grant).
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the agent table with unique actor_id and approval_routing column; `client.orgs[':orgId'].agents.$post` autocompletes; openapi lists agent routes including grant-requests; grant-requests read maps to session_activity proposals (no grant_request table).

### `DA-session-01` — agent*session + session_activity entities: tables + DTOs + sessions routes (incl. SSE stream, approvals) *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01, DA-agent-01
- **files:** `packages/db/src/schema/agents.ts`, `packages/types/src/session.ts`, `apps/api/src/routes/sessions.ts`
- **spec:** data-model.md §5.6, §5.7, api-rpc-contract.md §3.11, RECONCILIATION.md (FROZEN: SSE-only transport, agentSessions router var, Session\* DTOs, table named agent_session)
- **build:** New entities. (1) DB: agent_session per §5.6 (table name agent_session, TS export agentSession): id PK, organization_id NOT NULL CASCADE, agent_id NOT NULL→agent CASCADE, task_id→task SET NULL, trigger session_trigger, status session_status default 'pending', external_run_ref?, started_at default now(), ended_at?; indexes agent_session_org_idx, agent_session_task_idx, agent_session_status_idx(org,status). session_activity per §5.7: id PK, session_id NOT NULL→agentSession CASCADE, type session_activity_type, body jsonb $type<SessionActivityBody> NOT NULL, approval_status approval_status?, applied_audit_event_id→auditEvent SET NULL, created_at default now(); indexes session_activity_session_idx(session_id,created_at), partial approval idx WHERE approval_status='proposed'. (2) DTOs (Session\* prefix per FROZEN): SessionOut{ id, agentId, taskId?, trigger, status, externalRunRef?, startedAt, endedAt? }, SessionStart{ agentId, taskId?, trigger, prompt? }, SessionActivityOut{ id, type, body, approval?, createdAt }, ApprovalDecide{ decision:'approve'|'reject', scope?:'this'|'all_in_session' }, SessionMessage{ body }. (3) sessions router exported as const agentSessions, mounted orgs.route('/:orgId/sessions', agentSessions): GET /, POST / (trigger_agent_session; 503 if agent runtime not configured), GET /:sessionId, GET /:sessionId/activity, GET /:sessionId/stream (SSE via Hono streamSSE — EXCLUDED from RPC typing, declares text/event-stream in describeRoute for OpenAPI; Last-Event-ID/?since resume; 25s heartbeat), POST /:sessionId/messages, POST /:sessionId/approvals/:activityId (org:assign; resolveApprovers), POST /:sessionId/pause|resume|cancel|takeover. All org-scoped capabilities per §3.11.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits agent_session (NOT 'session') and session_activity with the partial proposed index; `client.orgs[':orgId'].sessions.$post` autocompletes via AppType while the SSE stream route is excluded from typing but present in openapi.json as text/event-stream.

### `DA-integration-01` — integration entity: table + DTOs + integrations routes + oauth callback group _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/integration.ts`, `apps/api/src/routes/integrations.ts`, `apps/api/src/routes/oauth-callbacks.ts`, `apps/api/src/app.ts`
- **spec:** data-model.md §6.5, api-rpc-contract.md §3.12, RECONCILIATION.md (FROZEN: signed-state OAuth, credentialsRef, never reuse MCP token)
- **build:** New entity + public callback group. (1) DB: `integration` per §6.5 with auditColumns() + provider text NOT NULL, pattern integration_pattern, roles integration_role[] NOT NULL default '{}', connection jsonb $type<IntegrationConnection> NOT NULL, status integration_status default 'connected', config jsonb default {}, sync_mode sync_mode NOT NULL; index integration_org_idx. (2) DTOs: IntegrationOut{ id, provider, pattern, roles[], status, syncMode, account? }, IntegrationConnect{ provider, pattern, roles[] }, IntegrationUpdate{ roles?, config? }. (3) integrations router (orgs.route('/:orgId/integrations', integrations)): GET /, GET /directory, POST / (org:manage→{integrationId, authorizeUrl}; signed-state JWT over BETTER_AUTH_SECRET carrying {integrationId,orgId,nonce,actorId}, 10min TTL; NEVER reuses MCP token), GET /:integrationId, PATCH /:integrationId, DELETE /:integrationId (→{id, status:'disconnected'}), POST /:integrationId/import, POST /:integrationId/sync, GET /:integrationId/jobs/:jobId. (4) oauth-callbacks router mounted OUTSIDE /orgs as app.route('/integrations/oauth', oauthCallbacks): GET /:provider/callback (auth public; verifies signed state, exchanges code, stores credentialsRef, sets status connected, redirects to web settings). Add to app.ts chained composition.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the integration table with integration_role[] array column; `client.orgs[':orgId'].integrations.$post` autocompletes; openapi shows integrations routes plus the public oauth callback; the callback route is mounted outside /orgs.

### `DA-notification-01` — notification entity: table + DTOs + cross-org notifications routes _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/notification.ts`, `apps/api/src/routes/notifications.ts`, `apps/api/src/app.ts`
- **spec:** data-model.md §6.4, api-rpc-contract.md §3.13, RECONCILIATION.md (FROZEN: cross-org fan-out, per-item canActor filter, downstream org:assign on act)
- **build:** New entity, cross-org (no organization_id-as-tenant-key path; mounted top-level). (1) DB: `notification` per §6.4: id PK, user_id NOT NULL→user CASCADE, organization_id NOT NULL→organization CASCADE (which org it's from), type notification_type, subject_type audit_subject_type, subject_id text, body jsonb $type<NotificationBody>?, read_at?, acted_at?, created_at default now(); indexes notification_inbox_idx(user_id,read_at,acted_at,created_at), notification_org_idx. (2) DTOs: NotificationOut{ id, organizationId, type, subject:{type,id}, readAt?, actedAt?, createdAt }, NotificationCount{ unread, pendingApprovals, perOrg? }, NotificationAct{ action: string }. (3) notifications router mounted app.route('/notifications', notifications) OUTSIDE /orgs (sessionMiddleware only, no orgContextMiddleware): GET / (authenticated, {type?, organizationId?, unreadOnly?}→Page<NotificationOut> across caller's active actors, per-item canActor view-filtered, fan-out not cross-tenant join), GET /count (authenticated, {byOrg?}→NotificationCount), POST /:notificationId/read (authenticated owner→NotificationOut), POST /read-all (authenticated, {organizationId?, type?}→{updated:number}), POST /:notificationId/act (authenticated→resolves org from notification.organization_id, enforces org:assign downstream→NotificationOut; 404 if no actor in that org). Add to app.ts chain.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the notification table with the inbox index; `client.notifications.$get` autocompletes (top-level, not under orgs); openapi shows notifications routes as authenticated.

### `DA-dailyplan-01` — daily*plan_item entity: table + DTOs + cross-org dailyplan routes *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/dailyplan.ts`, `apps/api/src/routes/dailyplan.ts`, `apps/api/src/app.ts`
- **spec:** data-model.md §6.3, api-rpc-contract.md §3.14, RECONCILIATION.md (FROZEN: dailyPlanItem hubId owner + ref_organization_id chip, downstream org:view)
- **build:** New entity, Hub-scoped cross-org (no organization_id on the row). (1) DB: `daily_plan_item` per §6.3: id PK, hub_id NOT NULL→hub CASCADE, date date NOT NULL (no tz), ref_organization_id NOT NULL→organization CASCADE, ref_task_id NOT NULL→task CASCADE, sort integer default 0, status daily_plan_item_status default 'planned', timebox jsonb $type<{start,end}>?, created_at default now(); indexes daily_plan_item_hub_date_idx(hub_id,date), uniqueIndex daily_plan_item_uq(hub_id,date,ref_task_id). (2) DTOs: DailyPlanItemOut{ id, hubId, date, taskRef:{organizationId,taskId}, sort, status, timebox? }, DailyPlanItemCreate{ date, taskRef:{organizationId,taskId}, sort?, timebox? }, DailyPlanItemPatch{ sort?, status?, timebox?, date? }. (3) dailyplan router mounted app.route('/dailyplan', dailyplan) OUTSIDE /orgs (sessionMiddleware only): GET / (authenticated, {date}→grouped-by-org), POST /items (authenticated→DailyPlanItemOut; resolves ref org, enforces org:view on the referenced task; 404 if no actor in that org; owner = caller's hub), PATCH /items/:itemId (authenticated owner), DELETE /items/:itemId (authenticated owner→{id, removed:true}), POST /plan-day (authenticated, {date, suggest?}→{items}). Add to app.ts chain.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits daily_plan_item with hub_id+date index and the (hub_id,date,ref_task_id) unique; `client.dailyplan.items.$post` autocompletes (top-level); openapi shows dailyplan routes authenticated.

### `DA-saved-view-01` — saved*view entity: table + ViewFilter/Grouping/Sort types + saved-views routes *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01, DA-types-jsonb-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/saved-view.ts`, `apps/api/src/routes/saved-views.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** mcp-surface.md §4.1 (docket://{org}/view/{id}), run_view, permissions §7.4, RECONCILIATION.md (FROZEN: saved_view table shape, non-grantable, ViewFilter/Grouping/Sort types)
- **build:** New entity required by MCP run_view / docket://{org}/view/{id} (FROZEN gap-fill). (1) DB: `saved_view` per FROZEN data-model decision in crosscutting.ts: ...auditColumns() (id, organization_id CASCADE, created_by→actor SET NULL, created_at, updated_at, archived_at) + name text NOT NULL, scope view_scope {personal,team,organization} default 'personal', team_id→team CASCADE (nullable), owner_id→actor SET NULL, filter jsonb $type<ViewFilter> NOT NULL, grouping jsonb $type<ViewGrouping>?, sort jsonb $type<ViewSort>?, is_default boolean default false; indexes saved_view_org_idx, saved_view_owner_idx, saved_view_team_idx. NOT a grantable resource (no grant/visibility column). (2) DTOs: SavedViewOut{ id, name, scope, teamId?, ownerId?, filter, grouping?, sort?, isDefault }, SavedViewCreate{ name, scope?, teamId?, filter, grouping?, sort?, isDefault? }, SavedViewUpdate{ name?, scope?, teamId?, filter?, grouping?, sort?, isDefault? }. (3) saved-views router (orgs.route('/:orgId/views', savedViews)): GET / (org:view→Page<SavedViewOut>), POST / (org:contribute→SavedViewOut, Idempotency-Key), GET /:viewId (org:view), PATCH /:viewId (org:contribute), DELETE /:viewId (org:contribute→{id, archivedAt}). Results are re-scoped per requester at execution, so the table stores spec only.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the saved_view table with view_scope enum and the three indexes and no visibility/grant column; `client.orgs[':orgId'].views.$post` autocompletes; openapi shows saved-views routes.

### `DA-audit-01` — audit*event entity: table + DTOs + hub/activity feed route + write helper signature *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/types/src/audit.ts`, `apps/api/src/routes/hub.ts`, `apps/api/src/app.ts`
- **spec:** data-model.md §6.8, api-rpc-contract.md §3.15 (hub/activity), RECONCILIATION.md (FROZEN: audit_subject_type adds agent)
- **build:** New entity (org-wide activity feed; written by other handlers, read via hub). (1) DB: `audit_event` per §6.8: id PK, organization_id NOT NULL CASCADE, actor_id→actor SET NULL (principal), initiator_id→actor SET NULL (agent set-in-motion-by), subject_type audit_subject_type (incl. 'agent' per DA-enums-01), subject_id text, type audit_event_type, metadata jsonb $type<{before?,after?}>?, created_at default now(); indexes audit_event_org_idx(org,created_at), audit_event_subject_idx(org,subject_type,subject_id,created_at). (2) DTOs: AuditEventOut{ id, organizationId, actorId, initiatorId?, subject:{type,id}, type, createdAt }. (3) Route: GET /hub/activity (authenticated, ListQuery→Page<AuditEventOut> cross-org fan-out across caller's active actors, per-item canActor view-filtered, no cross-tenant join) on the hub router (DA-hub-01 owns the file; this ticket adds the activity route + the AuditEventOut type + the audit_event table by both extending the chained hub router). Provide a writeAudit() service helper signature in types (no business logic) so other tickets can record events.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits the audit_event table with subject_type containing 'agent'; `client.hub.activity.$get` autocompletes; openapi shows the cross-org activity feed authenticated.

### `DA-hub-01` — hub aggregation routes (today/inbox/portfolio/search) + cross-org DTOs _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-notification-01, DA-dailyplan-01
- **files:** `packages/types/src/hub.ts`, `apps/api/src/routes/hub.ts`, `apps/api/src/app.ts`
- **spec:** api-rpc-contract.md §3.15, RECONCILIATION.md (FROZEN: cross-org fan-out, per-item canActor view filtering, no cross-tenant join)
- **build:** New cross-org aggregation surface (no new table; reads notification/daily_plan_item/audit_event/work tables; fan-out per membership, server-merged, per-item canActor-filtered). (1) DTOs: HubTodayOut{ plan: DailyPlanGroup[], calendar: CalendarBlock[], needsAttention:{approvals,blocked,dueToday,inbox:number} }, HubPortfolioOut{ swimlanes: { organization: OrgChip, programs:{program,projects:ProjectBar[]}[] }[] }, SearchHit{ organizationId, type, id, title, snippet, url }, HubSearchOut{ results: SearchHit[], commands: CommandHit[] }. (2) hub router mounted app.route('/hub', hub) OUTSIDE /orgs (sessionMiddleware only): GET /hub/today (authenticated, {date}→HubTodayOut; per-item org:view enforced), GET /hub/inbox (authenticated, ListQuery & {unreadOnly?,type?}→Page<NotificationOut>), GET /hub/portfolio (authenticated, {from?,to?,initiativeId?}→HubPortfolioOut), GET /hub/search (authenticated, {q, scope?, types?, limit?}→HubSearchOut). The /hub/activity route is owned by DA-audit-01 in the same file — coordinate by both extending the chained hub router. Wire into app.ts chain (single .route('/hub', hub)).
- **accept:** `pnpm --filter apps/api typecheck` passes; `client.hub.today.$get` and `client.hub.search.$get` autocomplete; openapi shows all hub routes authenticated with per-item filtering documented; no cross-tenant SQL join is introduced (aggregation is fan-out).

### `DA-admin-tables-01` — Service-admin tables: staff*user, impersonation_session, lifecycle_hold, operator_audit_event *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/admin.ts`
- **spec:** data-model.md §8, RECONCILIATION.md (FROZEN: admin tables complete, org FKs intentional, lifecycle enum exact)
- **build:** New service-admin tables per data-model §8 (NOT tenant-scoped; excluded from org-lifecycle purge logic, but their org FKs cascade/set-null as written). staff_user: id PK, user_id NOT NULL UNIQUE→user CASCADE, role staff_role {support,finance,superadmin}, created_at; uniqueIndex(user_id). impersonation_session: id PK, staff_user_id NOT NULL→staffUser CASCADE, target_user_id→user CASCADE, target_org_id→organization CASCADE, reason NOT NULL, started_at default now(), expires_at NOT NULL; index(staff_user_id), check('impersonation_target_present', target_user_id IS NOT NULL OR target_org_id IS NOT NULL). lifecycle_hold: id PK, organization_id NOT NULL→organization CASCADE, reason NOT NULL, placed_by NOT NULL→staffUser RESTRICT, placed_at default now(), released_at? (active=NULL); partial index lifecycle_hold_org_idx WHERE released_at IS NULL. operator_audit_event: id PK, staff_user_id→staffUser SET NULL, action text NOT NULL, target_user_id→user SET NULL, target_org_id→organization SET NULL, metadata jsonb?, created_at default now(); index(created_at). Do NOT strip the org FKs (FROZEN: intentional).
- **accept:** `pnpm --filter @docket/db typecheck` passes; `db:generate` emits all four admin tables with staff_user.user_id unique, the impersonation target-present check, and the partial active-hold index.

### `DA-admin-routes-01` — admin router (staff-gated, top-level) + admin DTOs over the admin tables _(P6 · parallel)_

- **depends on:** DA-shared-01, DA-admin-tables-01
- **files:** `packages/types/src/admin.ts`, `apps/api/src/routes/admin.ts`, `apps/api/src/app.ts`
- **spec:** api-rpc-contract.md §3.16, data-model.md §8, RECONCILIATION.md (FROZEN: per-route staff allow-sets, OperatorAuditEvent on every mutation, impersonation runs as impersonated actor)
- **build:** New admin authorization plane (separate from tenant canActor). (1) DTOs: MetricsOut, AdminUserOut, AdminOrgOut, BillingActionInput, LifecycleHoldOut, ImpersonationSessionOut{ id, expiresAt, bannerToken }, OperatorAuditEventOut, StaffUserOut, StaffRoleSet{ role }. (2) admin router mounted app.route('/admin', admin) OUTSIDE /orgs, applying sessionMiddleware then staffGuard(allowSet) per FROZEN per-route allow-sets (reads/metrics/users/orgs/lifecycle/impersonation = {support,finance,superadmin}; billing-write/holds = {finance,superadmin}; audit/staff = {superadmin}): GET /admin/metrics, GET /admin/users, GET /admin/users/:userId, GET /admin/orgs, GET /admin/orgs/:orgId, POST /admin/orgs/:orgId/billing/:action, GET /admin/orgs/:orgId/lifecycle, POST /admin/orgs/:orgId/holds, DELETE /admin/orgs/:orgId/holds/:holdId, POST /admin/impersonations, DELETE /admin/impersonations/:sessionId, GET /admin/audit, GET /admin/staff, PUT /admin/staff/:userId. Every mutation writes an OperatorAuditEvent (never tenant audit_event). x-docket-capability = staff:<role>. Wire into app.ts chain. Impersonation still runs canActor as the impersonated actor (no super-grant).
- **accept:** `pnpm --filter apps/api typecheck` passes; `client.admin.metrics.$get` autocompletes via AppType; openapi shows /admin routes with staff:<role> annotations; a unit test confirms staffGuard rejects a support user on GET /admin/audit (superadmin-only).

### `DA-infra-idem-01` — idempotency*key table + idempotency middleware for POST create routes *(P6 · parallel)\_

- **depends on:** DA-shared-01, DA-enums-01
- **files:** `packages/db/src/schema/infra.ts`, `apps/api/src/middleware/idempotency.ts`, `packages/db/src/schema/index.ts`
- **spec:** api-rpc-contract.md §1.7, §3, RECONCILIATION.md #28 (FROZEN: Postgres idempotency_key table 24h TTL, user-scoped PK, request_hash)
- **build:** New service-infra table + middleware (FROZEN gap-fill; Postgres-only, no Redis). (1) DB: create schema/infra.ts with `idempotency_key`: key text NOT NULL, organization_id text→organization CASCADE (nullable), user_id text NOT NULL→user CASCADE, method text NOT NULL, path text NOT NULL, request_hash text NOT NULL, response_status integer?, response_body jsonb $type<unknown>?, status idempotency_status default 'in_progress', created_at default now(), expires_at NOT NULL; primaryKey(user_id, key); index idempotency_expires_idx(expires_at). TTL 24h (expires_at = created_at + interval '24 hours' set in app). (2) Middleware idempotency.ts: on a POST carrying Idempotency-Key, look up (user_id, key); if found AND request_hash (sha256 of method+path+canonical body) matches → return stored status+body verbatim; if found AND hash differs → 422 Problem code idempotency_key_reuse; if absent → execute in the same tx that inserts the row and store the final response. Header schema Idempotency-Key = z.string() (client token; documented separately from entity Id). Export the middleware so create-route tickets attach it. Register infra schema in db index.
- **accept:** `pnpm --filter apps/api typecheck` passes; `db:generate` emits idempotency_key with composite PK (user_id, key) and the expires index; a unit test confirms same-key+same-body replays the stored response and same-key+different-body returns 422 idempotency_key_reuse.

### `DA-relations-01` — Drizzle relations() + db index assembly for all new entities _(P6 · sequential)_

- **depends on:** DA-org-ext-01, DA-members-01, DA-role-01, DA-grant-01, DA-team-ext-01, DA-initiative-01, DA-initiative-links-01, DA-program-01, DA-project-ext-01, DA-milestone-01, DA-cycle-01, DA-task-ext-01, DA-task-deps-01, DA-label-01, DA-comment-01, DA-update-01, DA-agent-01, DA-session-01, DA-integration-01, DA-notification-01, DA-dailyplan-01, DA-saved-view-01, DA-audit-01, DA-admin-tables-01, DA-infra-idem-01
- **files:** `packages/db/src/relations.ts`, `packages/db/src/index.ts`
- **spec:** data-model.md §9, §10, RECONCILIATION.md preamble (data-model is the single SQL owner)
- **build:** Integration step (runs after all per-entity tables exist; NOT parallel — it touches relations.ts and index.ts which every table feeds). Author/extend packages/db/src/relations.ts with drizzle relations() for every new entity and join (initiative↔project/program, task↔label, task↔dependency, agent→agent_session→session_activity, hub→daily_plan_item, user→notification, organization→all work tables, etc.) per the §10 ER summary, and ensure packages/db/src/index.ts re-exports every new table and includes them in the exported `schema` object passed to drizzle(). Verify a single `db:generate` produces one coherent migration with no missing-table or dangling-FK errors.
- **accept:** `pnpm --filter @docket/db typecheck` passes; `pnpm --filter @docket/db db:generate` produces a single migration covering all new tables with no FK resolution errors; `pnpm --filter @docket/db db:migrate` applies cleanly against a local/Neon Postgres; `db:studio` shows every new table.

### `DA-app-compose-01` — app.ts composition: chain all org-scoped + cross-org routers into AppType, mount middleware, openapi/docs _(P6 · sequential)_

- **depends on:** DA-org-ext-01, DA-members-01, DA-role-01, DA-grant-01, DA-billing-01, DA-team-ext-01, DA-initiative-01, DA-initiative-links-01, DA-program-01, DA-project-ext-01, DA-milestone-01, DA-cycle-01, DA-task-ext-01, DA-task-deps-01, DA-label-01, DA-comment-01, DA-update-01, DA-agent-01, DA-session-01, DA-integration-01, DA-notification-01, DA-dailyplan-01, DA-saved-view-01, DA-audit-01, DA-hub-01, DA-admin-routes-01
- **files:** `apps/api/src/app.ts`, `apps/api/src/error.ts`, `packages/types/src/api.ts`
- **spec:** api-rpc-contract.md §1.2, §1.3, §1.6, §4, §6, RECONCILIATION.md (FROZEN: canonical Hono composition, orgContextMiddleware placement, auth/mcp/docs outside AppType, Problem onError + 402 BillingFrozen, @docket/types/api re-export)
- **build:** Integration step (NOT parallel — single composition root). Assemble apps/api/src/app.ts per FROZEN composition decision: orgs router chains all org-scoped children via orgs.route('/:orgId/teams', teams).route('/:orgId/initiatives', initiatives).route('/:orgId/programs', programs).route('/:orgId/projects', projects).route('/:orgId/cycles', cycles).route('/:orgId/tasks', tasks).route('/:orgId/comments', comments).route('/:orgId/updates', updates).route('/:orgId/agents', agents).route('/:orgId/sessions', agentSessions).route('/:orgId/integrations', integrations).route('/:orgId/labels', labels).route('/:orgId/views', savedViews) plus members/roles/grants/billing folded into orgs; apply orgs.use('/:orgId/_', orgContextMiddleware) BEFORE child .route() chains. Root: const app = new Hono<AppEnv>().basePath('/v1'); const routes = app.route('/orgs', orgs).route('/notifications', notifications).route('/dailyplan', dailyplan).route('/hub', hub).route('/admin', admin).route('/integrations/oauth', oauthCallbacks); export type AppType = typeof routes. Register /api/auth/_, /mcp, /.well-known/\*, GET /v1/openapi.json (openAPIRouteHandler), GET /v1/docs (Scalar), GET /v1/health on `app` OUTSIDE the `routes` const (not in AppType). In error.ts add shared onError mapping Problem statuses + the validator hook (ZodError→422, AuthError→401, CapabilityError→403, NotFoundError→404, ConflictError/CycleError→409, IdempotencyConflict→422, BillingFrozen→402, else 500). @docket/types/api re-exports `type AppType` from @docket/api. Never break method chaining.
- **accept:** `pnpm --filter apps/api build` (tsc→dist) succeeds and emits AppType in dist/.d.ts; a scratch `hc<AppType>` consumer autocompletes a nested route from every group (e.g. client.orgs[':orgId'].cycles[':cycleId'].close.$post) and does NOT see /api/auth, /mcp, or /v1/health in the typed client; `GET /v1/openapi.json` returns a valid 3.1 document listing every route group with its x-docket-capability and Problem responses; `GET /v1/docs` renders Scalar.

## permissions-auth-billing (39 tickets)

### `PAB-DB-01` — Permissions schema: grant + role + grant*effect enum (folded canonical shape) *(P6 · sequential)\_

- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/db/src/enums.ts`
- **spec:** permissions.md §3, permissions.md §4, RECONCILIATION.md #2, RECONCILIATION.md #3, data-model.md §6.1
- **build:** Author the canonical single `grant` and `role` tables (data-model names win; permissions §3/§4 columns folded in per RECONCILIATION #2/#3 and the frozen data-model decision). `grant`: id text PK $defaultFn(genId); organization_id text NOT NULL ->organization CASCADE; subject_kind grant_subject_kind {actor,role}; subject_id text (polymorphic, app-FK); resource_kind resource_kind {organization,team,program,project,task}; resource_id text; capabilities jsonb $type<GrantCapability[]> NOT NULL; effect grant_effect {allow,deny} NOT NULL DEFAULT 'allow'; cascades boolean NOT NULL DEFAULT true; visibility_override visibility (nullable); visibility visibility NOT NULL DEFAULT 'public'; expires_at timestamptz (nullable); created_by text->actor SET NULL; created_at timestamptz DEFAULT now() NOT NULL. Indexes: grant_org_idx(organization_id), grant_subject_idx, grant_resource_idx, uniqueIndex grant_subject_resource_effect_uq on (subject_kind, subject_id, resource_kind, resource_id, effect). `role`: id, organization_id NOT NULL CASCADE, key text NOT NULL, name text NOT NULL, is_system boolean NOT NULL DEFAULT false, capabilities jsonb $type<GrantCapability[]> NOT NULL DEFAULT [], base_capability grant_capability (nullable), default_visibility visibility NOT NULL DEFAULT 'public'; uniqueIndex role_org_key_uq(organization_id,key) + uniqueIndex (organization_id,name). Add pgEnums grant_effect, grant_subject_kind, resource_kind (if not already), visibility, grant_capability to enums.ts. DELETE any `permission_grant` table. No DB CHECK for polymorphic FK (app-enforced).
- **accept:** `pnpm --filter @docket/db typecheck` passes; `pnpm --filter @docket/db db:generate` emits a migration containing grant + role tables with grant_effect enum and the grant_subject_resource_effect_uq unique index; grep confirms no `permission_grant` symbol remains.

### `PAB-DB-02` — Containment-resolver columns: visibility + ancestor*path on team/program/project/task *(P6 · parallel)\_

- **files:** `packages/db/src/schema/identity.ts`, `packages/db/src/schema/work.ts`
- **spec:** RECONCILIATION.md #5, permissions.md §7.3, permissions.md §5, data-model.md §0.2
- **build:** Add to team, program, project, and task: `visibility visibility NOT NULL DEFAULT 'public'` and `ancestor_path text[] NOT NULL DEFAULT '{}'` (root->self prefixed tokens excluding self, e.g. ['org:<id>','team:<id>','program:<id>','project:<id>']). Add a GIN index per table: index('<table>\_ancestor_path_gin').using('gin', t.ancestorPath). Do NOT add these to organization (root), initiative, or cycle. ancestor_path is maintained transactionally in the app on parent move (separate ticket PAB-AZ-04 owns the maintenance helper); this ticket only ships the columns + GIN indexes in the initial migration so no later migration is needed.
- **accept:** `pnpm --filter @docket/db typecheck` passes; generated migration shows visibility + ancestor_path columns on exactly team/program/project/task each with a GIN index on ancestor_path; organization/initiative/cycle do not gain the columns.

### `PAB-DB-03` — invitation table (hand-built Docket invitation, not the BA plugin) _(P6 · parallel)_

- **files:** `packages/db/src/schema/identity.ts`, `packages/db/src/enums.ts`
- **spec:** RECONCILIATION.md (Decisions: invitations), docket-engineering-plan.md §2, api-rpc-contract.md §3.1 (members/invite)
- **build:** Add `invitation` to identity.ts and `invitation_status` pgEnum {pending,accepted,revoked,expired} to enums.ts. Columns: id text PK $defaultFn(genId); organization_id text NOT NULL ->organization CASCADE; email text NOT NULL; role_id text NOT NULL ->role RESTRICT; as_guest boolean NOT NULL DEFAULT false; token text NOT NULL; status invitation_status NOT NULL DEFAULT 'pending'; invited_by text ->actor SET NULL; expires_at timestamptz NOT NULL; created_at timestamptz DEFAULT now() NOT NULL; accepted_at timestamptz (nullable). Indexes: uniqueIndex invitation_token_uq(token); uniqueIndex invitation_org_email_pending_uq on (organization_id,email) WHERE status='pending'; index invitation_org_idx(organization_id). role referenced by id (not a BA role string).
- **accept:** `pnpm --filter @docket/db typecheck` passes; migration shows invitation table with invitation_status enum, the token unique index, and the partial pending unique index; role_id FK uses RESTRICT.

### `PAB-DB-04` — idempotency*key table (Postgres 24h TTL, user-scoped) *(P6 · parallel)\_

- **files:** `packages/db/src/schema/crosscutting.ts`, `packages/db/src/enums.ts`
- **spec:** RECONCILIATION.md (Decisions: Idempotency-Key), api-rpc-contract.md §1.7/§3, data-model.md (infra)
- **build:** Add `idempotency_key` table + `idempotency_status` pgEnum {in_progress,completed}. Columns: key text NOT NULL; organization_id text ->organization CASCADE (nullable); user_id text NOT NULL ->user CASCADE; method text NOT NULL; path text NOT NULL; request_hash text NOT NULL; response_status integer (nullable); response_body jsonb $type<unknown> (nullable); status idempotency_status NOT NULL DEFAULT 'in_progress'; created_at timestamptz DEFAULT now() NOT NULL; expires_at timestamptz NOT NULL. PK primaryKey({columns:[user_id,key]}). Index idempotency_expires_idx(expires_at) for the sweep. (Middleware + TTL sweep are PAB-API-08 and PAB-CRON-02.)
- **accept:** `pnpm --filter @docket/db typecheck` passes; migration shows idempotency_key with composite PK (user_id,key), idempotency_status enum, and the expires_at index.

### `PAB-DB-05` — approval*routing jsonb columns on organization + team (agent column already exists) *(P6 · parallel)\_

- **files:** `packages/db/src/schema/identity.ts`, `packages/db/src/types.ts`
- **spec:** RECONCILIATION.md (Deferred: approval-routing storage), permissions.md §9.2, data-model.md §5.5
- **build:** Add `approval_routing jsonb $type<ApprovalRouting>` (nullable) to BOTH organization and team (agent.approval_routing already exists per data-model §5.5). Define the ApprovalRouting Zod-derived $type in types.ts: { mode: 'assigner'|'specific_actor'|'team_assignees'; actorId?: Id; teamId?: Id }. No new table. (Resolver order is owned by PAB-AZ-06.)
- **accept:** `pnpm --filter @docket/db typecheck` passes; migration shows approval_routing on organization and team; ApprovalRouting type is exported from @docket/db/src/types.ts.

### `PAB-TYPES-01` — @docket/types capability + permission primitives (Capability, GrantCapability, Refs, GrantUpsert, RoleCreate flat) _(P6 · sequential)_

- **depends on:** PAB-DB-01
- **files:** `packages/types/src/capabilities.ts`, `packages/types/src/permissions.ts`, `packages/types/src/index.ts`
- **spec:** permissions.md §1, RECONCILIATION.md #3, RECONCILIATION.md #6, RECONCILIATION.md #7, api-rpc-contract.md §3.1
- **build:** Author the frozen capability/permission wire contract. capabilities.ts: CAPABILITIES=['view','comment','contribute','assign','manage'] as const; Capability=z.enum(CAPABILITIES); GrantCapability alias of Capability; CAPABILITY_RANK record; satisfies(held,required) helper. permissions.ts: ActorRef={kind:'actor',id:Id}, RoleRef={kind:'role',id:Id}, ResourceKind=z.enum(['organization','team','program','project','task']), ResourceRef={kind:ResourceKind,id:Id}; GrantUpsert={ subject: ActorRef|RoleRef, resource: ResourceRef, capabilities: Capability[].min(1), visibility: enum default 'public', cascades?: boolean default true, expiresAt?: ISO-datetime } (NO effect field, deny deferred); RoleOut={id,key,name,isSystem,baseCapability:Capability|null,defaultVisibility,capabilities:Capability[]}; RoleCreate={key?,name,baseCapability:Capability|null,defaultVisibility}; RoleUpdate={name?,baseCapability?,defaultVisibility?}; GrantOut. DELETE any CapabilityGrid type. Capability stored value is the bare literal (org:\* is annotation-only).
- **accept:** `pnpm --filter @docket/types typecheck` and `pnpm --filter @docket/types test` pass; a unit test asserts satisfies('manage','view')===true and satisfies('view','contribute')===false; grep confirms no `CapabilityGrid` export remains.

### `PAB-AZ-01` — @docket/authz package skeleton + capability primitives + ResolveResult _(P6 · sequential)_

- **depends on:** PAB-TYPES-01
- **files:** `packages/authz/package.json`, `packages/authz/tsconfig.json`, `packages/authz/src/index.ts`, `packages/authz/src/capabilities.ts`
- **spec:** permissions.md §12, permissions.md §1, RECONCILIATION.md (Decisions: DENY deferred)
- **build:** Create the compiled `@docket/authz` package (tsc->dist, main/types to dist per engineering compile policy) depending on @docket/db and @docket/types. Re-export capability helpers (CAPABILITIES, CAPABILITY_RANK, satisfies, maxCapability(grants[]), unionCapabilities, raiseOrReplace, subtract) from capabilities.ts. Export the ResolveResult type { allow:boolean; reason:string; effectiveCapability: Capability|null } and allow()/deny() constructors. Export a compile-time-dead `DENY_ENABLED=false` const (deny deferred from v1). No engine logic yet.
- **accept:** `pnpm --filter @docket/authz build` produces dist/index.d.ts; `pnpm --filter @docket/authz typecheck` passes; importing { satisfies, ResolveResult, DENY_ENABLED } from @docket/authz resolves in a consumer typecheck.

### `PAB-AZ-02` — Containment resolver: ancestorChain() + applicableGrants() + loadActor() _(P6 · sequential)_

- **depends on:** PAB-AZ-01, PAB-DB-02
- **files:** `packages/authz/src/resolve.ts`
- **spec:** permissions.md §2.1, permissions.md §6 (steps 0-3), permissions.md §7.3
- **build:** Implement loadActor(db, actorId) -> {kind, orgId, roleId, roleKey, roleBase, roleDefaultVisibility, status} via a single actor->role join; ancestorChain(db, target) returning ordered root->self ResourceRef[] using the FK walk (§2.1), preferring ancestor_path tokens when present and falling back to a bounded recursive CTE; orgOf(db, target); applicableGrants(db, actor, chain) -> non-expired grants where subject in {actorId}+{roleId} and resource in chain with (resource===target OR cascades=true), one indexed query. These are the read primitives consumed by canActor (PAB-AZ-03). No decision logic here.
- **accept:** `pnpm --filter @docket/authz typecheck` passes; `pnpm --filter @docket/authz test` passes a unit test (against a seeded test DB or mocked db reader) proving ancestorChain returns [org,team,project,task] for a project-parented task and [org,team,task] for a Triage task, and applicableGrants excludes expired grants.

### `PAB-AZ-03` — canActor() resolution algorithm (cascade walk, allow-only path, cross-org/suspended pre-checks) _(P6 · sequential)_

- **depends on:** PAB-AZ-02
- **files:** `packages/authz/src/resolve.ts`, `packages/authz/src/visibility.ts`
- **spec:** permissions.md §6, permissions.md §4.4, permissions.md §5, RECONCILIATION.md (Decisions: DENY deferred), permissions.md §13
- **build:** Implement canActor(actorId, required, target, db): ResolveResult. Pre-checks (unconditional, not grant-overridable): actor null OR actor.orgId!==orgOf(target) -> deny('cross-org-or-unknown-actor'); actor.status!=='active' -> deny('actor-suspended'); both apply to human and agent actors. Then: build chain, collect applicableGrants, seed effective from actor.roleBase, walk root->self replacing with allowMax per level (actor-subject wins ties over role-subject); the deny branch (subtract/denySet) exists but is guarded behind DENY_ENABLED=false so v1 ignores effect='deny' rows. If effective===null fall back to visibilityGrantsView for required==='view' only. Return satisfies(effective,required). visibility.ts: effectiveVisibility(target,db) (nearest ancestor-or-self visibility_override -> row visibility col NULL->public -> public) and visibilityGrantsView(actor,target,db) (guests roleDefaultVisibility='private' never by visibility). canActor must return effectiveCapability in one pass (no double resolution).
- **accept:** `pnpm --filter @docket/authz test` passes the §13 unit matrix subset: capability chain implication; cascade override at each level; actor-grant beats role-grant on tie; guest zero-access-without-grant then exact-subtree on grant; member sees public/blocked from manage; cross-org deny; suspended deny; expired grant inert; visibilityOverride flips access; deny rows ignored under DENY_ENABLED=false.

### `PAB-AZ-04` — ancestor*path maintenance helper + query-scoping predicate builder *(P6 · sequential)\_

- **depends on:** PAB-AZ-03
- **files:** `packages/authz/src/scope.ts`, `packages/authz/src/ancestor-path.ts`
- **spec:** permissions.md §7.2, permissions.md §7.3, permissions.md §7.4, RECONCILIATION.md #5
- **build:** ancestor-path.ts: rebuildAncestorPath(db, movedResource, tx) that, inside a caller-provided tx, recomputes ancestor_path for a moved subtree (bounded by subtree size) when team_id/program_id/project_id changes; exported for /move handlers (api lane) and grant cascade closure. scope.ts: buildVisiblePredicate(actor, resourceType, db) producing the Drizzle WHERE fragment for list endpoints per §7.2 — composes (member-tier AND effectiveVisibility='public') OR (id IN grant-reach closure via ancestor_path @> ARRAY[token]) minus deny-set (deny-set empty under DENY_ENABLED=false). Build the per-request grant-reach set once and reuse. For a Member with no grants it collapses to effectiveVisibility='public'; for a Guest it collapses to id IN (visible set) (empty until granted).
- **accept:** `pnpm --filter @docket/authz test` passes integration-style tests proving a Guest list query returns 0 rows pre-grant and only the granted subtree post-grant, and a Member list returns all public rows; rebuildAncestorPath rewrites a moved task's ancestor_path to its new project chain.

### `PAB-AZ-05` — Write-guards: last-owner guard + no-self-escalation + Owner-only privileged-action gate _(P6 · parallel)_

- **depends on:** PAB-AZ-03
- **files:** `packages/authz/src/write-guards.ts`
- **spec:** permissions.md §4.5, permissions.md §4.2, permissions.md §4.3
- **build:** Implement (a) lastOwnerGuard(db, orgId, op, tx): inside a SERIALIZABLE tx SELECT count(\*) FOR UPDATE of active human actors with role.key='owner'; reject (409 code 'last_owner_guard') any remove/suspend/archive/downgrade that drops count below 1 — used by member role/status change, member delete, transfer_ownership. (b) assertNoSelfEscalation(db, writer, target, grantedBaseCapabilityRank): canActor(writer,'manage',target) must allow AND granted rank <= writer effective rank on that resource, else 403 code 'self_escalation' — used by every grant/role write. (c) requireOwner(actor): assert role.key==='owner' for OWNER_ONLY actions {org.delete, org.transfer_ownership, org.change_billing_subject, role.modify_system_role}, layered BEFORE the manage check (Admin holds manage but is blocked). Export typed error classes for the api onError map.
- **accept:** `pnpm --filter @docket/authz test` passes: downgrading the last owner throws last_owner_guard; an Admin (manage) calling org.delete is rejected by requireOwner while an Owner passes; minting a grant above the writer's own capability throws self_escalation.

### `PAB-AZ-06` — Agent approval gate: gateAgentWrite() + resolveApprovers() + apply-time recheck _(P6 · parallel)_

- **depends on:** PAB-AZ-03, PAB-DB-05
- **files:** `packages/authz/src/agents.ts`
- **spec:** permissions.md §9, permissions.md §9.2, permissions.md §9.3, RECONCILIATION.md (Deferred: approval-routing storage)
- **build:** Implement gateAgentWrite(session, agentActor, write, db): step 1 capability axis via canActor(agentActor, requiredCapabilityFor(write), write.target) -> {outcome:'needs_grant'} on fail; step 2 approval axis on agentActor.approvalPolicy: autonomous->'apply', suggest->'propose_only', act_with_approval->{outcome:'awaiting_approval', approvers: resolveApprovers(...)}. resolveApprovers precedence (most-specific first): agent.approval_routing -> team.approval_routing(of task team) -> organization.approval_routing -> task assigner/delegator (latest 'assigned' audit_event) -> agent.accountable_owner_id -> org Owners; filter every candidate by canActor(candidate,'assign',target).allow, fall through empty layers; terminal layer = active org Owners (guaranteed non-empty by last-owner guard); defensively if still empty return {blocked:'no_eligible_approver'}. Apply-time semantics: on approve, re-run canActor(agentActor,...) at apply; on failure set approval_status='rejected' reason 'capability_revoked_at_apply', write audit_event type 'rejected', surface needs_grant notification — never apply. all_in_session binds only currently-proposed actions (each still re-checked at its own apply).
- **accept:** `pnpm --filter @docket/authz test` passes: capability-axis fail returns needs_grant; suggest never auto-applies; act_with_approval routes to an assign-holding approver and falls through approvers lacking assign down to org Owners; a grant revoked between propose and approve causes apply-time rejection (not application).

### `PAB-API-01` — orgContextMiddleware + capabilityGuard wired to canActor (404-vs-403 existence hiding) _(P6 · sequential)_

- **depends on:** PAB-AZ-03
- **files:** `apps/api/src/middleware/org-context.ts`, `apps/api/src/middleware/authorize.ts`
- **spec:** api-rpc-contract.md §1.6, permissions.md §7.1, permissions.md §7.5, permissions.md §10, RECONCILIATION.md #6
- **build:** orgContextMiddleware: applied on /orgs/:orgId/_ (via orgs.use('/:orgId/_', ...) before child routes); loads the human Actor for (session.user.id, orgId), 404 if no membership, sets c.var.actorCtx={orgId, actorId, role, capabilities}. organization_id is ALWAYS from this verified context, never the body. capabilityGuard(required: 'org:<cap>'|Capability, locate): parses 'org:contribute'->'contribute' bare literal, calls canActor(actorId, cap, target); on deny derive 404-vs-403 from the single ResolveResult.effectiveCapability (404 iff effectiveCapability===null OR !satisfies(effective,'view'), else 403) with NO second canActor call; sets c.var.authz=res. Owner-only privileged actions pass requireOwner (PAB-AZ-05) before the capability check.
- **accept:** `pnpm --filter @docket/api typecheck` passes; an integration test proves a non-member gets 404 from orgContextMiddleware, a member lacking view on a private task gets 404, and a member with view-but-not-contribute gets 403 on a PATCH; org_id is never read from the request body.

### `PAB-API-02` — Roles routes: GET/POST/PATCH/DELETE /orgs/:orgId/roles (flat capabilities) _(P6 · parallel)_

- **depends on:** PAB-API-01, PAB-TYPES-01
- **files:** `apps/api/src/routes/roles.ts`
- **spec:** api-rpc-contract.md §3.1 (roles), permissions.md §4, RECONCILIATION.md #3, permissions.md §4.3
- **build:** Chained Hono router for org roles using flat Capability[] (no CapabilityGrid). GET /roles -> RoleOut[] {id,key,name,isSystem,baseCapability,defaultVisibility,capabilities}; POST /roles (RoleCreate; server derives key slug if absent, rejects reserved owner/admin/member/guest) writes role with capabilities mirror = capabilitiesAtOrBelow(baseCapability); PATCH /roles/:roleId (RoleUpdate; key immutable); DELETE /roles/:roleId -> {id,removed:true}, 409 if is_system. All require org:manage via capabilityGuard. role.modify_system_role gated by requireOwner. assertNoSelfEscalation on create/update. Materialize the org-base role-grant at org root for owner/admin/member on create (guest gets none).
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration tests: creating a role with baseCapability='contribute' echoes capabilities ['view','comment','contribute']; deleting a system role returns 409; a Member (manage on no resource) cannot create a role above their own capability (self_escalation 403).

### `PAB-API-03` — Grants routes: GET /grants, PUT /grants (upsert), DELETE /grants/:grantId (allow-only) _(P6 · parallel)_

- **depends on:** PAB-API-01, PAB-TYPES-01
- **files:** `apps/api/src/routes/grants.ts`
- **spec:** api-rpc-contract.md §3.1 (grants), permissions.md §3, permissions.md §8, RECONCILIATION.md #2, RECONCILIATION.md (Decisions: DENY deferred)
- **build:** Chained Hono router for grants. GET /grants?subjectActorId&resourceKind&resourceId -> GrantOut[]. PUT /grants (GrantUpsert) maps subject.kind->subject_kind, subject.id->subject_id, resource.kind->resource_kind, resource.id->resource_id; server always writes effect='allow' (no effect field accepted, deny deferred); upserts on grant_subject_resource_effect_uq; returns GrantOut. DELETE /grants/:grantId -> {id,removed:true}. All org:manage. assertNoSelfEscalation against the granted capabilities max-rank on every PUT. Same router backs agent scopes (subject_kind='actor' on an agent actor) and grant-on-request approval (writes an actor-grant). expiresAt powers time-boxed grants.
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration tests: PUT a contribute grant for an actor on a project then canActor for that actor returns contribute on a child task; a writer cannot PUT a grant above their own effective capability (403 self_escalation); GrantUpsert rejects an `effect` field in the body (extra-key strict).

### `PAB-API-04` — Members + invitations: POST /members/invite, accept-invite, role/status PATCH, personal-org guard _(P6 · parallel)_

- **depends on:** PAB-API-01, PAB-DB-03, PAB-AZ-05
- **files:** `apps/api/src/routes/members.ts`, `apps/api/src/services/invitation.ts`
- **spec:** api-rpc-contract.md §3.1 (members/invite), RECONCILIATION.md (Decisions: invitations, personal space), permissions.md §4.5, docket-engineering-plan.md §2
- **build:** POST /orgs/:orgId/members/invite (json {email, role:RoleRef, asGuest?}) -> creates invitation row (token = 32-byte url-safe random, expires_at, status pending, role_id from RoleRef) returning {invitationId, email, role, status:'pending'}; org:manage; soft card-gate: if shared org + no active subscription + env DOCKET_REQUIRE_CARD_FOR_INVITE=true -> 402 code card_required {checkoutHint}, else allow. Accept-invite endpoint creates the human actor with role_id copied from the invitation, sets status accepted+accepted_at. PATCH /members/:actorId (role/status change) and DELETE /members/:actorId run lastOwnerGuard in a SERIALIZABLE tx. Personal-org guard: organization.is_personal=true rejects invite/extra-human-actor/non-owner-grant with 403 code personal_org_no_invites (agent actors allowed).
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration tests: inviting into a personal org returns 403 personal_org_no_invites; a duplicate pending invite for the same email/org violates the partial unique index; downgrading the last owner returns 409 last_owner_guard; accepting an invite creates an actor with the invitation's role_id.

### `PAB-API-05` — Seed 4 system roles + creator-becomes-Owner on org create; org-base role-grants _(P6 · sequential)_

- **depends on:** PAB-API-01, PAB-DB-01
- **files:** `apps/api/src/services/org-seed.ts`, `apps/api/src/routes/orgs.ts`
- **spec:** permissions.md §4.1, permissions.md §3 (role grants), build-sequence-and-slice.md §4.5, RECONCILIATION.md (Decisions: personal space)
- **build:** Extend the org-create transaction (POST /orgs) to seed the 4 system roles (owner base manage / admin base manage / member base contribute / guest base null) with is_system=true and default_visibility (owner/admin/member public, guest private), then materialize org-root role-grants {subject_kind:'role', resource_kind:'organization', resource_id:org.id, capabilities:[baseCapability], cascades:true, effect:'allow'} for owner/admin/member (guest gets none). Creator becomes the human actor with the owner role. For is_personal orgs seed exactly one human actor=owner, the default team, and reject additional human actors (delegates to PAB-API-04 guard). This is the full Phase-6 role-bundle materialization on top of the slice's minimal seed.
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration test: after POST /orgs the org has 4 is_system roles, the creator is an owner-role actor, and canActor(creator,'manage',{org}) allows while a fresh member-role actor gets contribute org-wide and no manage; guest role exists but has no org-root grant.

### `PAB-API-06` — Agent grant-on-request + approval routes wired to authz agent gate _(P6 · parallel)_

- **depends on:** PAB-API-03, PAB-AZ-06
- **files:** `apps/api/src/routes/agents.ts`, `apps/api/src/routes/sessions.ts`
- **spec:** permissions.md §8, permissions.md §9, api-rpc-contract.md §3.10/§3.11, RECONCILIATION.md (Deferred: approval-routing)
- **build:** PUT /orgs/:orgId/agents/:agentId/grants ({capabilities, resource?}) — grant-on-request: an actor with manage on the resource approves, writing a new actor-grant for the agent actor via the grants service (PAB-API-03); org:manage. POST /orgs/:orgId/sessions/:sessionId/approvals/:activityId — approve/reject an agent action: caller must be in resolveApprovers AND pass canActor(caller,'assign',target); on approve apply the write with apply-time canActor recheck for the agent (PAB-AZ-06), advancing approval_status proposed->approved->applied (or rejected on capability_revoked_at_apply); on reject set rejected, no write; both audited (audit_event). scope 'this'|'all_in_session' binds only currently-proposed actions.
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration tests: granting an agent contribute via grant-on-request lets a subsequent agent write pass canActor; approving as an actor lacking assign-on-target returns 403; approval applies the write and records an audit_event with actor_id=agent, initiator_id=approver.

### `PAB-API-07` — Problem (RFC 9457) error model + onError map + validator hook (incl. 402/403/409 authz codes) _(P6 · parallel)_

- **depends on:** PAB-AZ-05
- **files:** `apps/api/src/error.ts`, `packages/types/src/problem.ts`
- **spec:** api-rpc-contract.md §1.7, permissions.md §0.1, permissions.md §4.5
- **build:** Define Problem in @docket/types {type,title,status,detail?,code (closed enum),fieldErrors?} with application/problem+json. apps/api onError map: ZodError->422, AuthError->401, CapabilityError->403, NotFoundError->404, ConflictError->409, CycleError->409 (dependency_cycle), IdempotencyConflictError->422 (idempotency_key_reuse), BillingFrozenError->402 (lifecycle pending_deletion/past_due write-block, permissions §0.1), else->500. Closed code enum includes last_owner_guard, self_escalation, personal_org_no_invites, card_required. Shared validator hook flattens z.flattenError into fieldErrors and emits the same 422 Problem so validator and runtime errors are shape-identical. Attach Problem to every route's 4xx/5xx in describeRoute.
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/api test` pass; tests assert a forbidden write yields application/problem+json with status 403 + code, an invalid body yields 422 with fieldErrors, and a frozen-org write yields 402 BillingFrozen.

### `PAB-API-08` — Idempotency-Key middleware (Postgres table, replay + request*hash conflict) *(P6 · parallel)\_

- **depends on:** PAB-DB-04, PAB-API-07
- **files:** `apps/api/src/middleware/idempotency.ts`
- **spec:** api-rpc-contract.md §1.7/§3, RECONCILIATION.md (Decisions: Idempotency-Key), data-model.md (infra)
- **build:** Middleware applied to every POST create route. Header Idempotency-Key (optional). On request with a key: look up (user_id, key); if found AND request_hash (sha256 of method+path+canonical body) matches -> return stored response_status+response_body verbatim (no re-execution); if found AND hash differs -> 422 Problem code idempotency_key_reuse (IdempotencyConflictError); if not found -> execute in the same tx that inserts the row (status in_progress -> completed) storing the final response; expires_at = created_at + 24h. organization_id nullable (cross-org/admin POSTs). Absent header -> normal execution.
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration tests: replaying a create with the same key+body returns the identical stored response without creating a second row; same key + different body returns 422 idempotency_key_reuse; no header executes normally.

### `PAB-AUTH-01` — @docket/auth full plugin set: passkey + social(Google/GitHub/Linear) + sso + scim + oidcProvider + mcp + stripe _(P6 · sequential)_

- **files:** `packages/auth/src/index.ts`, `packages/auth/src/plugins.ts`, `packages/auth/package.json`
- **spec:** docket-engineering-plan.md §2, env-and-bootstrap.md §1.3, RECONCILIATION.md (Decisions: org plugin OFF, mount)
- **build:** Compose the single betterAuth() with the full plugin array (extending the slice's passkey+nextCookies): passkey({registration:{requireSession:false, resolveUser}}); socialProviders Google+GitHub+native Linear with account.accountLinking.trustedProviders minimal; sso(); scim() (Users only, no Groups); oidcProvider() (PKCE); mcp() built on oidcProvider; stripe() (wiring in PAB-BILL-01); nextCookies() LAST. drizzleAdapter(db,{provider:'pg'}) WITHOUT usePlural and WITHOUT useNumberId; advanced.database.generateId = () => genId() (shared ULID). organization plugin OFF. Pin better-auth + @better-auth/passkey + @better-auth/sso + @better-auth/scim + @better-auth/stripe all =1.6.14 exact via catalog; oidcProvider/mcp from core.
- **accept:** `pnpm --filter @docket/auth build` produces dist; `pnpm --filter @docket/auth typecheck` passes; a unit test boots the betterAuth() instance and asserts nextCookies is last in the plugin array and advanced.database.generateId returns a 26-char ULID; package.json pins all @better-auth/\* at 1.6.14 via catalog.

### `PAB-AUTH-02` — Better Auth CLI schema generation into @docket/db (auth.ts) + post-generate ULID codemod + migration _(P6 · sequential)_

- **depends on:** PAB-AUTH-01
- **files:** `packages/db/src/schema/auth.ts`, `packages/db/package.json`, `scripts/db-auth-generate.ts`
- **spec:** docket-engineering-plan.md §2 (schema generated into @docket/db), data-model.md §9, RECONCILIATION.md #1
- **build:** Add pnpm db:auth:generate that runs `npx @better-auth/cli@1.6.14 generate` emitting the full plugin schema into packages/db/src/schema/auth.ts (user/session/account/verification/passkey/oauthApplication/oauthAccessToken/oauthConsent/subscription/sso/scim/jwks). Wire a post-generate codemod: if any generated id column lacks the shared genId default (fallback path if BA ever ignores advanced.database.generateId), inject $defaultFn(genId) so the text-ULID invariant holds; never allow crypto.randomUUID(). Then `drizzle-kit generate` + `drizzle-kit migrate` over DATABASE_URL_UNPOOLED. Re-export the generated tables from @docket/db index.
- **accept:** `pnpm db:auth:generate` regenerates schema/auth.ts containing the oidcProvider + sso + scim + subscription tables; `pnpm --filter @docket/db db:generate` then `db:migrate` (against a Neon/local branch) applies cleanly; a smoke check confirms a created user.id is a 26-char ULID (not a UUID).

### `PAB-AUTH-03` — passkey resolveUser: transactional user+hub birth + passkey-intent HMAC endpoint _(P6 · parallel)_

- **depends on:** PAB-AUTH-01
- **files:** `packages/auth/src/resolve-user.ts`, `apps/api/src/routes/passkey-intent.ts`
- **spec:** docket-engineering-plan.md §2 (resolveUser transactional), build-sequence-and-slice.md §2.4/§2.7
- **build:** POST /v1/auth/passkey-intent (public; body {name,email}) returns a short-lived (5min) HMAC-signed context token = sign({name,email,nonce,exp}, BETTER_AUTH_SECRET). passkey resolveUser({context}) verifies the HMAC+exp, then in ONE Drizzle transaction inserts the user row (id via shared genId) AND its 1:1 hub row, returning {id,name,email}. Missing/invalid/expired context -> 400 (no anonymous registration). Guards the partial-identity failure mode (user with no hub).
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/auth typecheck` pass; tests: passkey-intent returns a verifiable token; resolveUser with a tampered/expired context returns 400; a successful registration creates exactly one user and one hub in the same tx (rollback on hub-insert failure leaves no orphan user).

### `PAB-AUTH-04` — Mount /api/auth/\* on Hono with CORS-first + trusted origins, same-origin rewrite contract _(P6 · parallel)_

- **depends on:** PAB-AUTH-01
- **files:** `apps/api/src/index.ts`, `apps/api/src/auth-mount.ts`, `apps/web/next.config.ts`
- **spec:** docket-engineering-plan.md §2 (one /api/auth owner), RECONCILIATION.md (Decisions: mount, same-origin), env-and-bootstrap.md §0/§1.2
- **build:** Mount auth at apps/api: app.on(['POST','GET'], '/api/auth/_', c => auth.handler(c.req.raw)). Register CORS middleware FIRST, exposing Authorization and WWW-Authenticate headers, allowlist = BETTER_AUTH_TRUSTED_ORIGINS. Keep /api/auth/_ OUTSIDE the typed AppType routes const (§4). apps/web next.config rewrites /api/_ -> ${API_URL}/_ (same-origin first-party cookie path); RPC client targets relative '/v1' in browser, absolute NEXT_PUBLIC_API_URL on server. BETTER_AUTH_URL == API_URL (no redirect_uri_mismatch).
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/web build` pass; a request to /api/auth/ok returns 200 with CORS headers including access-control-expose-headers: WWW-Authenticate; /api/auth/\* does not appear in the exported AppType.

### `PAB-AUTH-05` — Next auth client: createAuthClient + passkeyClient wiring across web/marketing/admin _(P6 · parallel)_

- **depends on:** PAB-AUTH-04
- **files:** `packages/auth/src/client.ts`, `apps/web/src/lib/auth-client.ts`, `apps/admin/src/lib/auth-client.ts`
- **spec:** docket-engineering-plan.md §2, RECONCILIATION.md (Decisions: mount/same-origin), build-sequence-and-slice.md §2.4
- **build:** Export a shared createAuthClient factory ({ baseURL, plugins:[passkeyClient(), ...] }) from @docket/auth/client. Web uses same-origin '/api/auth' baseURL in browser; admin (separate Vercel project, no rewrite) uses NEXT_PUBLIC_API_URL + '/api/auth' with credentials. Surface signIn.passkey(), addPasskey({name,context}) (consuming PAB-AUTH-03 intent), social signIn for Google/GitHub/Linear through Better Auth's native social linking route. Type-only, framework-coupled per app.
- **accept:** `pnpm --filter @docket/web typecheck` and `pnpm --filter @docket/admin typecheck` pass; the auth client exposes authClient.signIn.passkey and authClient.passkey.addPasskey with typed args; admin client targets the absolute API origin.

> **Superseded-by-shipped note (MCP tickets below):** these were written pre-implementation and are
> preserved as-authored. What actually shipped: `MCP_ISSUER_URL`/`MCP_RESOURCE_URL`/
> `OIDC_LOGIN_PAGE_URL` derive automatically from the required `API_URL`/`WEB_URL` (no manual
> per-var setup, and `OIDC_LOGIN_PAGE_URL` defaults to `${WEB_URL}/sign-in`, not
> `${NEXT_PUBLIC_WEB_URL}/oauth/consent`); `MCP_SESSION_STORE_URL` DOES exist (gated behind
> `MCP_TASKS_ENABLED`), contrary to the "remove from v1" line below. See
> `docs/engineering/specs/mcp-surface.md`, `docs/engineering/specs/env-and-bootstrap.md` §1.5, and
> `docs/WORKLOG.md` (`MCP-PROD-009`, `MCP-PROD-010`) for the authoritative as-shipped model.

### `PAB-MCP-01` — MCP env contract: add MCP*TASKS_ENABLED + MCP_CIMD_TRUST_ALLOWLIST/STRICT, rename MCP_CANONICAL_URL->MCP_RESOURCE_URL *(P6 · parallel)\_

- **files:** `packages/env/src/mcp.ts`, `packages/env/src/registry.ts`
- **spec:** env-and-bootstrap.md §1.5, mcp-surface.md §6.8, RECONCILIATION.md (Deferred: MCP, no Redis)
- **build:** Finalize the canonical MCP env slice (env-and-bootstrap §1.5 names authoritative): MCP_ISSUER_URL (=API_URL), MCP_RESOURCE_URL (=${API_URL}/mcp, the aud — supersedes MCP_CANONICAL_URL), MCP_ALLOWED_ORIGINS (CSV), OIDC_LOGIN_PAGE_URL (default ${NEXT_PUBLIC_WEB_URL}/oauth/consent), MCP_TASKS_ENABLED (coerced boolean, default false), MCP_CIMD_TRUST_ALLOWLIST (CSV host suffixes), MCP_CIMD_STRICT (boolean, default true). Remove MCP_SESSION_STORE_URL from v1 (no Redis). Add each as a VAR_REGISTRY row {name,slice,scope,targets,required,zod,where,sensitive} so bootstrap + env:check stay in lockstep.
- **accept:** `pnpm --filter @docket/env typecheck` passes; `pnpm env:check` validates the mcp slice with MCP_RESOURCE_URL present and MCP_CANONICAL_URL absent; MCP_TASKS_ENABLED defaults false; grep confirms MCP_SESSION_STORE_URL is not referenced in v1 env.

### `PAB-MCP-02` — MCP Resource-Server auth shims: resolveMcpPrincipal (aud/scope) + CIMD discovery override + SSRF guard _(P6 · sequential)_

- **depends on:** PAB-AUTH-02, PAB-MCP-01
- **files:** `apps/api/src/mcp/auth.ts`, `apps/api/src/mcp/principal.ts`, `apps/api/src/mcp/cimd.ts`
- **spec:** mcp-surface.md §2.4/§2.5/§2.6, docket-engineering-plan.md §4, RECONCILIATION.md (Deferred: MCP verify/shim)
- **build:** Single resolveMcpPrincipal(headers) resolving token->{userId,scopes,clientId,aud}: use auth.api.getMcpSession if it returns scopes else parse payload.scope (space-delimited) from verifyAccessToken({verifyOptions:{issuer:MCP_ISSUER_URL, audience:MCP_RESOURCE_URL}}). assertAudience: aud===MCP_RESOURCE_URL else 401 (fallback iss===MCP_ISSUER_URL + first-party client allowlist if aud absent). CIMD shim: serve /.well-known/oauth-authorization-server merging oAuthDiscoveryMetadata(auth) with client_id_metadata_document_supported:true; CIMD resolution fetches the URL-form client_id (https only; reject private/loopback/link-local/ULA IPs, re-check post-DNS, pin resolved IP; client_id===document URL byte-for-byte; validate redirect_uris; host suffix in MCP_CIMD_TRUST_ALLOWLIST; 5s timeout/64KB cap/no redirects/1-fetch-per-5min). All shims unit-tested against a stubbed AS; a boot probe logs native-vs-shim path.
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/api test` pass; tests: a token with wrong aud is rejected 401; scopes are read from payload.scope when getMcpSession lacks them; the well-known doc reports client_id_metadata_document_supported:true; a CIMD client_id resolving to 127.0.0.1 is rejected by the SSRF guard.

### `PAB-MCP-03` — MCP two-layer authz: global scope gate layered above canActor, with WWW-Authenticate challenges _(P6 · sequential)_

- **depends on:** PAB-MCP-02, PAB-AZ-03
- **files:** `apps/api/src/mcp/authorize.ts`
- **spec:** mcp-surface.md §2.2, permissions.md §10, docket-engineering-plan.md §4, RECONCILIATION.md (Decisions: global scopes)
- **build:** Implement the necessary-but-not-sufficient gate: the 4 flat global scopes (work:read, work:write, agents:run, connectors:link) gate the capability class first; then resolve the caller's Actor for (sub, org-from-docket-slug) and run canActor — both must pass. scope->capability map: work:read->view, work:write->contribute (or assign for assign-class tools), agents:run->assign+approver-eligibility, connectors:link->manage. Failure forms: 401 carries WWW-Authenticate Bearer resource_metadata=.../oauth-protected-resource/mcp; runtime 403 carries error=insufficient_scope naming required+granted scopes; resources/read no-grant AND not-found both map to JSON-RPC -32002 (never leak existence). Tool/grant failures (scope OK, canActor deny) return isError:true tool result; protocol violations -> JSON-RPC error. org is a call argument, never client-asserted (trust only verified sub).
- **accept:** `pnpm --filter @docket/api test` passes: a work:read token calling a write tool gets insufficient_scope step-up 403; a work:write token whose actor lacks contribute on the target gets isError tool result; resources/read of an unauthorized resource returns -32002; org context derives only from sub (a client-asserted org in the body is ignored).

### `PAB-MCP-04` — Stateless Streamable HTTP /mcp transport + capability advertisement (Tasks/prompts gating) _(P6 · parallel)_

- **depends on:** PAB-MCP-03
- **files:** `apps/api/src/mcp/server.ts`, `apps/api/src/mcp/index.ts`
- **spec:** mcp-surface.md §1.1/§5/§4.4, docket-engineering-plan.md §4, RECONCILIATION.md (Deferred: stateless v1, Tasks flag)
- **build:** Mount @modelcontextprotocol/sdk StreamableHTTPServerTransport at /mcp with sessionIdGenerator:undefined (stateless, no Redis, no resumable replay). Origin validation BEFORE auth (MCP_ALLOWED_ORIGINS, reject DNS-rebinding). Advertise capabilities: tools{listChanged:true}, resources{subscribe:true,listChanged:true}, completions{}, logging{}; tasks{...} ONLY when MCP_TASKS_ENABLED=true (otherwise omit and set per-tool execution.taskSupport='forbidden'); prompts NOT advertised. Subscribable resources (session/task/hub.inbox/hub.today) fan out notifications/resources/updated from the service-layer event bus (no Redis); subscriptions bound to the live connection, re-read on reconnect. Serve PRM (RFC 9728) at /.well-known/oauth-protected-resource and /.well-known/oauth-protected-resource/mcp.
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/api test` pass: initialize returns protocolVersion 2025-11-25 with tasks omitted when MCP_TASKS_ENABLED=false and present when true; prompts is never advertised; a request with a disallowed Origin is rejected before auth; PRM is served at both well-known paths.

### `PAB-BILL-01` — Stripe plugin wiring: org-keyed subscription, lookup-key price resolver, no-card trial, BILLING*ENABLED gate *(P6 · sequential)\_

- **depends on:** PAB-AUTH-02
- **files:** `packages/auth/src/stripe.ts`, `packages/env/src/stripe.ts`, `packages/env/src/registry.ts`
- **spec:** docket-engineering-plan.md §3, env-and-bootstrap.md §1.4/§3.5, RECONCILIATION.md (Decisions: billing split), data-model.md §3.3
- **build:** Configure stripe() plugin: referenceId = organization.id, authorizeReference hook asserts the caller's human Actor holds the right capability on that org, createCustomerOnSignUp=false (customer created at first shared-org creation), plan freeTrial.days=14 (no-card trial), enabledEvents = the 4 core + invoice.payment_failed/paid/payment_action_required + customer.subscription.trial_will_end via onEvent. Boot resolver: resolve plan priceId at boot via stripe.prices.list({lookup_keys:[DOCKET_PRICE_LOOKUP_TEAM]}) (and annual), with STRIPE_PRICE_TEAM as a hard override if set; pass resolved id to plans[].priceId/annualDiscountPriceId. env stripe slice superRefine: DOCKET_PRICE_LOOKUP_TEAM required unless STRIPE_PRICE_TEAM set; stripe vars required only when BILLING_ENABLED=true (coerced boolean, default false). Webhook path fixed at ${API_URL}/api/auth/stripe/webhook.
- **accept:** `pnpm --filter @docket/auth typecheck` and `pnpm --filter @docket/env typecheck` pass; a unit test (stubbed Stripe) proves the boot resolver returns a concrete price\_ id from a lookup key and prefers STRIPE_PRICE_TEAM when present; env:check fails when BILLING_ENABLED=true with no price configured and passes when BILLING_ENABLED=false.

### `PAB-BILL-02` — Org billing routes: GET /billing, POST /billing/checkout (embedded), POST /billing/portal _(P6 · parallel)_

- **depends on:** PAB-BILL-01, PAB-API-01
- **files:** `apps/api/src/routes/billing.ts`
- **spec:** api-rpc-contract.md §3.1 (billing), docket-engineering-plan.md §3 (embedded Checkout), env-and-bootstrap.md §1.4
- **build:** Chained Hono router under /orgs/:orgId/billing, all org:manage. GET /billing -> {lifecycleState, plan, status, currentPeriodEnd?, trialEndsAt?, exportReadyAt?, deleteAfterAt?} from the subscription mirror + org lifecycle columns. POST /billing/checkout ({plan,returnUrl}) -> {checkoutClientSecret} via embedded Checkout (ui_mode embedded), customer created/looked-up by org.id. POST /billing/portal ({returnUrl}) -> {url} via Stripe billing portal (STRIPE_BILLING_PORTAL_CONFIG_ID if set). Card-gate for shared-org checkout honors DOCKET_REQUIRE_CARD_FOR_INVITE/BILLING_ENABLED; personal orgs never hit Checkout.
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration tests (stubbed Stripe): GET /billing returns lifecycleState+plan for a trialing org; checkout returns a client secret for an org:manage caller; a non-manage caller gets 403; portal returns a url.

### `PAB-BILL-03` — Data-lifecycle state machine: onEvent transitions + lifecycle service (trial->past*due->export_window->pending_deletion) *(P6 · sequential)\_

- **depends on:** PAB-BILL-01
- **files:** `apps/api/src/services/lifecycle.ts`, `packages/auth/src/stripe.ts`
- **spec:** docket-engineering-plan.md §3, data-model.md §3.3, permissions.md §0.1, RECONCILIATION.md (Decisions: billing split)
- **build:** Implement the org lifecycle service over the organization columns lifecycle_state {trialing,active,past_due,export_window,pending_deletion,deleted}, export_ready_at, delete_after_at. Stripe onEvent hooks feed (never solely trust) transitions: invoice.payment_failed -> active->past_due; trial-end without payment / terminal past_due -> export_window (set export_ready_at, delete_after_at = now+14d, enqueue export job, email link); reactivation (paid/restored at any pre-deletion stage) -> active + clear delete_after_at. The lifecycle gate runs UPSTREAM of canActor (frozen org = 402/403 BillingFrozenError before authz). Expose transition functions reused by the cron sweep (PAB-CRON-01).
- **accept:** `pnpm --filter @docket/api test` passes: invoice.payment_failed moves an active org to past_due; trial expiry moves it to export_window with delete_after_at = +14d; a payment in export_window returns it to active and clears delete_after_at; a write to a pending_deletion org raises BillingFrozenError (402).

### `PAB-BILL-04` — Org work-layer export route + signed artifact (real v1, no stub) _(P6 · parallel)_

- **depends on:** PAB-BILL-01, PAB-API-01
- **files:** `apps/api/src/routes/export.ts`, `apps/api/src/services/export.ts`, `packages/env/src/ops.ts`
- **spec:** api-rpc-contract.md §3.1 (export), env-and-bootstrap.md §1.7, docket-engineering-plan.md §3, AGENTS.md (no-stubs)
- **build:** GET /orgs/:orgId/export?format=json|zip (org:manage) returns {downloadUrl, expiresAt}. Build a real export of the org work layer as JSON: organization, teams, roles, grants, actors (human+agent, NO credentials), initiatives, programs, projects, milestones, cycles, tasks (+labels, dependencies, comments, updates), integrations (metadata only, NO credentialsRef/tokens), notifications. EXCLUDE Stripe artifacts, secrets, other tenants. zip wraps the JSON + manifest.json (counts + schema version). downloadUrl = signed 1h-TTL URL to Vercel Blob (BLOB_READ_WRITE_TOKEN) or, in dev with no blob, a signed /v1/orgs/:orgId/export/download/:token streaming route. Add BLOB_READ_WRITE_TOKEN (optional) + EXPORT_BUCKET_URL/EXPORT_BUCKET_TOKEN override to the ops env slice. Reused by the lifecycle export_window step.
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/api test` pass: export of a seeded org returns a downloadUrl whose payload contains the org's tasks/projects but no credentialsRef/Stripe fields; zip format includes manifest.json; a non-manage caller gets 403.

### `PAB-CRON-01` — Lifecycle sweep cron: idempotent reconciliation, holds-respecting, Vercel Cron + CRON*SECRET *(P6 · sequential)\_

- **depends on:** PAB-BILL-03
- **files:** `apps/api/src/routes/lifecycle-sweep.ts`, `apps/api/vercel.json`
- **spec:** docket-engineering-plan.md §3 (idempotent cron sweep), env-and-bootstrap.md §1.7, data-model.md §3.3, RECONCILIATION.md (Decisions: org deletion CASCADE)
- **build:** GET /api/lifecycle/sweep guarded by Authorization: Bearer ${CRON_SECRET}. For each org indexed by (lifecycle_state, delete_after_at): load the LIVE Stripe subscription state and reconcile lifecycle_state against that truth (idempotent — running twice yields the same result; lost/out-of-order webhooks cannot corrupt state). SKIP any org with an active (released_at IS NULL) lifecycle_hold. When delete_after_at < now and pending_deletion: advance pending_deletion->deleted relying on ON DELETE CASCADE from organization, plus an app purge job for Stripe customer + external connector state. Reactivation observed at sweep returns org to active. Register a vercel.json crons entry (daily 0 3 \* \* \*) on the api project. Handler is fully idempotent and reconciliation-based (never trusts webhook ordering).
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/api test` pass: an unauthenticated sweep returns 401; running the sweep twice over an export_window org past delete_after_at produces a single deletion; an org under an active lifecycle_hold is skipped; reconciliation flips a paid org back to active even if no webhook fired.

### `PAB-CRON-02` — Idempotency-key TTL sweep (24h hard-delete) on the cron lane _(P6 · parallel)_

- **depends on:** PAB-DB-04, PAB-CRON-01
- **files:** `apps/api/src/routes/lifecycle-sweep.ts`
- **spec:** RECONCILIATION.md (Decisions: Idempotency-Key 24h), api-rpc-contract.md §1.7, data-model.md (infra)
- **build:** Extend the CRON_SECRET-guarded sweep cadence to hard-delete idempotency_key rows where expires_at < now() (using the idempotency_expires_idx). Lightweight, batched delete; idempotent. Runs on the same cron invocation/cadence as the lifecycle sweep to avoid a second cron entry.
- **accept:** `pnpm --filter @docket/api test` passes: after the sweep, idempotency_key rows older than 24h are removed and fresh rows remain; the delete is guarded by CRON_SECRET.

### `PAB-ADMIN-01` — Service-admin schema confirm + staffGuard (per-route allow-set, separate plane) _(P6 · parallel)_

- **depends on:** PAB-AUTH-02
- **files:** `apps/api/src/middleware/staff-guard.ts`, `packages/db/src/schema/admin.ts`
- **spec:** permissions.md §11, api-rpc-contract.md §3.16, data-model.md §8, docket-engineering-plan.md §5 (service-admin layer)
- **build:** Confirm admin.ts tables (staff_user {id, user_id unique ->user CASCADE, role staff_role {support,finance,superadmin}}, impersonation_session, lifecycle_hold, operator_audit_event) are excluded from the tenant lifecycle purge (their org FKs cascade/set-null intentionally, but they aren't tenant-owned). staffGuard(allowSet): runs sessionMiddleware then loads the StaffUser for session.user.id and asserts staffRole in the route's explicit allow-set (NOT a single ladder): reads/metrics/users/orgs/lifecycle/impersonation allow {support,finance,superadmin}; billing-write/holds allow {finance,superadmin}; audit/staff-management allow {superadmin}. 403 otherwise. The /admin plane does NOT use orgContextMiddleware/capabilityGuard (separate authorization plane); StaffUser never appears as a tenant Actor.
- **accept:** `pnpm --filter @docket/api typecheck` and `pnpm --filter @docket/db typecheck` pass; tests: a support staff hitting GET /admin/audit (superadmin-only) gets 403; finance hitting holds passes; a non-staff session gets 403; staff_user.user_id has a unique CASCADE FK to user.

### `PAB-ADMIN-02` — Admin routes: metrics/users/orgs/lifecycle/holds/billing-actions + OperatorAuditEvent on every mutation _(P6 · parallel)_

- **depends on:** PAB-ADMIN-01, PAB-BILL-03
- **files:** `apps/api/src/routes/admin/index.ts`, `apps/api/src/routes/admin/orgs.ts`, `apps/api/src/routes/admin/billing.ts`
- **spec:** api-rpc-contract.md §3.16, permissions.md §11, docket-engineering-plan.md §3/§5
- **build:** Mount /admin (in AppType, consumed by apps/admin) under sessionMiddleware + staffGuard(allowSet). Implement GET /admin/metrics, /admin/users(+:userId), /admin/orgs(+:orgId), /admin/orgs/:orgId/lifecycle ({support+}); POST /admin/orgs/:orgId/holds + DELETE holds/:holdId ({finance+}, pauses the sweep); POST /admin/orgs/:orgId/billing/:action (extend_trial|credit|change_plan|refund|pause_dunning, writes back to Stripe, {finance+}). Every /admin mutation writes an OperatorAuditEvent (never the tenant audit_event). Lifecycle/billing actions reuse PAB-BILL-03 transition functions.
- **accept:** `pnpm --filter @docket/api typecheck` passes; integration tests: placing a hold pauses the lifecycle sweep for that org; extend_trial writes back to Stripe (stubbed) and records an OperatorAuditEvent; a support-role staff is rejected on a billing action (finance-only).

### `PAB-ADMIN-03` — Impersonation: scoped/time-boxed ImpersonationSession; tenant calls run as impersonated Actor via canActor _(P6 · parallel)_

- **depends on:** PAB-ADMIN-01, PAB-AZ-03
- **files:** `apps/api/src/routes/admin/impersonations.ts`, `apps/api/src/middleware/impersonation.ts`
- **spec:** permissions.md §11, api-rpc-contract.md §3.16, docket-engineering-plan.md §5
- **build:** POST /admin/impersonations ({targetUserId?, targetOrgId?, reason, ttlMinutes}, {support+}) mints an ImpersonationSession {id, expiresAt, bannerToken}, reason-logged, time-boxed; DELETE /admin/impersonations/:sessionId ends it. Impersonation middleware: while a valid impersonation session is active, TENANT calls resolve the impersonated Actor and run through canActor AS THAT ACTOR (operators get NO tenant super-grant); every impersonation action writes an OperatorAuditEvent (never the tenant audit_event). StaffUser is never inserted as a tenant Actor in any grant graph.
- **accept:** `pnpm --filter @docket/api typecheck` passes; tests: an impersonating operator performing a tenant write is authorized exactly as the impersonated Actor (denied where that Actor would be denied); the impersonation is recorded in operator_audit_event and not in the tenant audit_event; an expired ImpersonationSession no longer impersonates.

### `PAB-TEST-01` — Authz coverage suite to >=80% (unit matrix + integration list-scoping + persona e2e gates) _(P6 · parallel)_

- **depends on:** PAB-AZ-04, PAB-AZ-05, PAB-AZ-06, PAB-API-03, PAB-API-04
- **files:** `packages/authz/tests/matrix.test.ts`, `apps/api/tests/authz-integration.test.ts`
- **spec:** permissions.md §13, docket-engineering-plan.md §0.4 (>=80% coverage), AGENTS.md (no skipped tests)
- **build:** Complete the permissions §13 test matrix to the 80% gate. Unit (@docket/authz): full capability-chain implication; cascade+override at each containment level; deny-rows-ignored-under-DENY_ENABLED=false (assert allow-only v1 behavior, no it.skip); actor-grant beats role-grant on tie; guest grant-only + cascades=false pins one row; member public/private/manage; owner-vs-admin privileged-action gate; expired grant inert; visibilityOverride flips; cross-org/suspended deny; agent grant-on-request + approval-axis independence. Integration (apps/api): list endpoints return only permitted rows (count-stable pagination), 404-vs-403 existence-hiding, scope-gate+canActor both required. Provide owner/member/guest persona helpers in @docket/test-utils for the invite-guest and delegate-task-approve e2e flows.
- **accept:** `pnpm --filter @docket/authz test --coverage` and `pnpm --filter @docket/api test` pass with @docket/authz line coverage >=80%; no it.skip/describe.skip present; the integration suite asserts a guest list returns 0 rows pre-grant and only the granted subtree post-grant.

## mcp (41 tickets)

> **Superseded-by-shipped note:** see the note above `PAB-MCP-01` earlier in this file — it applies
> to every env-contract reference in this section too (`MCP_ISSUER_URL`/`MCP_RESOURCE_URL`/
> `OIDC_LOGIN_PAGE_URL` are on-by-default derivations, not manually-set vars; `MCP_SESSION_STORE_URL`
> shipped rather than being removed).

### `MCP-00-env` — MCP env-var slice in @docket/env (issuer/resource/origins/CIMD/tasks flag) _(P6 · parallel)_

- **files:** `packages/env/src/slices/mcp.ts`, `packages/env/src/registry.ts`
- **spec:** env-and-bootstrap.md §1.5, mcp-surface.md §6 step 8
- **build:** Add the MCP env slice to @docket/env using the createEnv composition + the VAR_REGISTRY single-source pattern (per env-and-bootstrap frozen decision). Vars and Zod types: MCP_ISSUER_URL = z.url() (=API_URL, derived); MCP_RESOURCE_URL = z.url() (=${API_URL}/mcp, the canonical RS URI / audience — this is the value the spec called MCP_CANONICAL_URL, which is REMOVED); MCP_ALLOWED_ORIGINS = CSV string; OIDC_LOGIN_PAGE_URL = z.url().optional() default ${NEXT_PUBLIC_WEB_URL}/oauth/consent; MCP_TASKS_ENABLED = coerced boolean default false; MCP_CIMD_TRUST_ALLOWLIST = CSV string (comma-separated host suffixes); MCP_CIMD_STRICT = coerced boolean default true. DO NOT add MCP_SESSION_STORE_URL (no Redis in v1 — frozen). Add a superRefine error if MCP_TASKS_ENABLED=true (Tasks are disabled in v1; reject in env:check). Wire each var as a registry row {name, slice:'mcp', scope:'server', targets, required, zod, where} so bootstrap prompts and env:check derive from one declaration.
- **accept:** `pnpm --filter @docket/env build` and `pnpm --filter @docket/env typecheck` pass; `pnpm env:check` with MCP_ISSUER_URL/MCP_RESOURCE_URL/MCP_ALLOWED_ORIGINS set succeeds and with them missing throws a named error; setting MCP_TASKS_ENABLED=true makes env:check fail with a clear message.

### `MCP-01-transport` — Mount stateless StreamableHTTPServerTransport at /mcp in apps/api _(P6 · sequential)_

- **depends on:** MCP-00-env
- **files:** `apps/api/src/mcp/server.ts`, `apps/api/src/mcp/transport.ts`, `apps/api/src/index.ts`
- **spec:** mcp-surface.md §1.1, mcp-surface.md §6 step 1
- **build:** Mount the official @modelcontextprotocol/sdk McpServer over StreamableHTTPServerTransport at a single Hono route /mcp supporting POST (JSON-RPC, may upgrade to SSE) and GET (server->client SSE). Construct the transport STATELESS: sessionIdGenerator: undefined — NO Mcp-Session-Id persistence, NO Redis event store, NO Last-Event-ID resumability (frozen mcp-surface decision overrides spec §1.1). Forbid the deprecated two-endpoint HTTP+SSE transport. Honor MCP-Protocol-Version: 2025-11-25 on non-initialize requests (SDK handles; reject unknown -> HTTP 400). Register an Origin allowlist guard (from MCP_ALLOWED_ORIGINS) and CORS exposing Authorization, WWW-Authenticate, MCP-Protocol-Version BEFORE the handler and BEFORE Better Auth, per spec §1.1/§6. The /mcp route and discovery routes are registered on the root app OUTSIDE the typed RPC AppType const (per api-rpc-contract composition decision). server.ts creates the McpServer; tool/resource registration is added by later tickets via exported register functions.
- **accept:** `pnpm --filter @docket/api build` passes; a curl POST to /mcp with an Origin not in MCP_ALLOWED_ORIGINS is rejected before auth; a POST with method initialize returns a JSON-RPC initialize result advertising protocolVersion 2025-11-25; a request with an unknown MCP-Protocol-Version returns HTTP 400.

### `MCP-02-discovery` — Serve PRM (RFC 9728) + AS metadata (RFC 8414) discovery routes _(P6 · parallel)_

- **depends on:** MCP-01-transport
- **files:** `apps/api/src/mcp/discovery.ts`, `apps/api/src/index.ts`
- **spec:** mcp-surface.md §2.3, mcp-surface.md §6 step 2
- **build:** Serve RS discovery documents on apps/api (outside AppType). (1) GET /.well-known/oauth-protected-resource AND GET /.well-known/oauth-protected-resource/mcp via oAuthProtectedResourceMetadata(auth), returning resource=MCP_RESOURCE_URL (no trailing slash), authorization_servers=[MCP_ISSUER_URL], scopes_supported=[work:read,work:write,agents:run,connectors:link], bearer_methods_supported=[header]. (2) GET /.well-known/oauth-authorization-server (and /.well-known/openid-configuration) via oAuthDiscoveryMetadata(auth). MUST guarantee the doc contains code_challenge_methods_supported:[S256] and client_id_metadata_document_supported:true — if Better Auth 1.6.14 does not natively emit the CIMD flag, wrap the metadata response and force-set client_id_metadata_document_supported:true (frozen CIMD shim decision). registration_endpoint present (DCR MAY fallback).
- **accept:** `pnpm --filter @docket/api build` passes; GET /.well-known/oauth-protected-resource/mcp returns JSON with resource==MCP_RESOURCE_URL and the four scopes; GET /.well-known/oauth-authorization-server returns JSON where code_challenge_methods_supported includes "S256" and client_id_metadata_document_supported===true.

### `MCP-03-rs-auth` — RS token-validation middleware: bearer/aud/issuer/scope/principal + WWW-Authenticate _(P6 · sequential)_

- **depends on:** MCP-01-transport
- **files:** `apps/api/src/mcp/auth.ts`, `apps/api/src/mcp/principal.ts`, `apps/api/src/mcp/challenges.ts`
- **spec:** mcp-surface.md §2.5, mcp-surface.md §2.6, mcp-surface.md §6 step 3
- **build:** Wrap /mcp with withMcpAuth(auth, handler). Implement resolveMcpPrincipal(headers) as the SINGLE place token -> {userId, scopes:Set<string>, clientId, aud} is resolved (frozen shim decision), preferring auth.api.getMcpSession() and falling back to verifyAccessToken({verifyOptions:{issuer:MCP_ISSUER_URL, audience:MCP_RESOURCE_URL}}) parsing scopes from payload.scope/scopes. Enforce on EVERY request (MUST): (1) bearer present in Authorization header (never query) else 401; (2) aud === MCP_RESOURCE_URL else 401 (independent RS-side assertion, defense-in-depth — frozen audience decision; if aud absent entirely, hard-reject unless iss===MCP_ISSUER_URL AND clientId in pre-registered first-party allowlist); (3) iss === MCP_ISSUER_URL, NO token passthrough (never accept GitHub/Drive/Linear tokens, never forward downstream); (4) scope check per the §3.2 table; insufficient scope -> 403; (5) principal sub->User; org-scoped ops resolve human Actor for (user_id, org) else 403 then evaluate Grant cascade via @docket/authz canActor. Emit the two WWW-Authenticate forms (challenges.ts): 401 Bearer resource_metadata=... scope=work:read; 403 Bearer error=insufficient_scope scope="<needed> <granted>" resource_metadata=... error_description=...
- **accept:** `pnpm --filter @docket/api build` passes; unit test: a request with no Authorization header returns 401 with WWW-Authenticate containing resource_metadata; a token whose aud != MCP_RESOURCE_URL returns 401; a valid work:read token calling a work:write tool returns 403 with error="insufficient_scope" and the needed scope in the challenge.

### `MCP-04-cimd-ssrf` — CIMD resolution shim with SSRF guard + domain-trust allowlist _(P6 · parallel)_

- **depends on:** MCP-02-discovery
- **files:** `apps/api/src/mcp/cimd.ts`
- **spec:** mcp-surface.md §2.4, env-and-bootstrap.md §1.5
- **build:** Implement the unconditional CIMD client-resolution shim (frozen decision): when an authorize request presents an https:// URL-form client_id, before honoring it enforce IN ORDER (1) scheme MUST be https; (2) host MUST resolve to a public IP — reject RFC1918, loopback (127.0.0.0/8, ::1), link-local (169.254.0.0/16, fe80::/10), unique-local (fc00::/7), any non-global; re-check after DNS resolution and pin the resolved IP for the actual fetch (DNS-rebinding defense); (3) fetched document URL MUST equal the client_id byte-for-byte; (4) host MUST match MCP_CIMD_TRUST_ALLOWLIST (host-suffix match). Fetch is throttled (max 1/client_id/5min), 5s timeout, 64KB cap, redirects disabled. Validate redirect_uris against the same https+public-host rules. When MCP_CIMD_STRICT=true (default prod) reject unknown-but-valid public docs not in the allowlist. Failures return OAuth invalid_client.
- **accept:** `pnpm --filter @docket/api build` passes; unit tests: a client_id resolving to 127.0.0.1 or 10.x is rejected; an http:// client_id is rejected; a client_id whose document URL differs from the client_id string is rejected; with MCP_CIMD_STRICT=true an allowlisted host passes and a non-allowlisted public host is rejected.

### `MCP-05-shared-schemas` — Shared MCP Zod fragments + EntityRef using branded ULID Id _(P6 · sequential)_

- **depends on:** MCP-00-env
- **files:** `packages/types/src/mcp/fragments.ts`, `packages/types/src/mcp/index.ts`
- **spec:** mcp-surface.md §3.3, RECONCILIATION.md decision #7/#13
- **build:** Author the shared MCP Zod fragments in @docket/types, importing the single branded Id primitive (ULID-shaped, 26-char Crockford) from @docket/types/primitives — NEVER z.string().uuid() (frozen #7/#13: every Id and idempotency_key Id field uses the branded Id). Define: Slug = z.string().min(1).regex(/^[a-z0-9-]+$/); ActorRef = z.object({actor_id: Id}); Health = z.enum([on_track,at_risk,off_track]); Priority = z.enum([none,urgent,high,medium,low]); Idem = Id.optional(); EntityRef = z.object({id: Id, type: z.enum([task,project,program,initiative,cycle,team,update,comment,session,agent,view]), uri: z.string() (docket://{org}/{type}/{id}), url: z.string().url().optional()}). Export a barrel so every tool/resource ticket imports from one place. This is the frozen contract the per-tool tickets build against.
- **accept:** `pnpm --filter @docket/types build` and `pnpm --filter @docket/types typecheck` pass; a grep for `z.string().uuid()` in packages/types/src/mcp returns no matches; a unit test asserts EntityRef.parse rejects a 36-char UUID id and accepts a 26-char ULID id.

### `MCP-06-tool-registry` — Tool registration harness: outputSchema + structuredContent + scope/grant gating + isError contract _(P6 · sequential)_

- **depends on:** MCP-03-rs-auth, MCP-05-shared-schemas
- **files:** `apps/api/src/mcp/tools/registry.ts`, `apps/api/src/mcp/tools/define-tool.ts`, `apps/api/src/mcp/tools/errors.ts`
- **spec:** mcp-surface.md §3.1, mcp-surface.md §3.2, mcp-surface.md §6 step 4
- **build:** Build the defineDocketTool() harness every per-tool ticket uses: registers a tool on the McpServer with Zod inputSchema + outputSchema converted to JSON Schema 2020-12 (z.toJSONSchema or zod-to-json-schema), sets all four ToolAnnotations EXPLICITLY (readOnlyHint/destructiveHint/idempotentHint/openWorldHint per the §3.2 table — never relying on SDK defaults) plus a human title, and on success populates BOTH structuredContent AND a serialized-JSON TextContent block (spec §3.1). Three-tier error mapping (frozen decision): scope failure -> HTTP 403 at transport (handled by MCP-03, never reaches tool); per-resource grant failure + input/validation/business errors -> tool result isError:true + actionable text; unknown tool/malformed -> JSON-RPC protocol error. Per-tool scope gate (table §3.2) is asserted by the harness before the handler runs; per-resource grant via canActor. Idempotency: create-tools pass idempotency_key through to the Postgres idempotency_key table (24h TTL) so replays return the original result. The harness exposes registerAllTools(server) which the per-tool tickets append to.
- **accept:** `pnpm --filter @docket/api build` passes; unit test: a tool defined via defineDocketTool emits a tools/list entry whose annotations object has all four hints present and a title; a handler returning a domain error produces a CallToolResult with isError:true and a non-empty text block; the result contains both structuredContent and a JSON text block.

### `MCP-tool-create_task` — Tool: create*task *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/create-task.ts`, `apps/api/src/mcp/tools/create-task.ts`
- **spec:** mcp-surface.md §3.4 create_task, RECONCILIATION.md decision #10
- **build:** Register create_task (scope work:write; annotations readOnly:F destructive:F idempotent:F openWorld:F). Input {org:Slug, team(string key or Id), title min1, description?, priority Priority default none, assignee:ActorRef?, delegate:{agent_id:Id}?, project_id:Id?, program_id:Id?, milestone_id:Id?, cycle_id:Id?, parent_task_id:Id?, due_date date?, labels:Id[]?, idempotency_key:Idem}. Output {task:EntityRef, state:string}. Handler resolves org slug->id, requires canActor contribute on the team/target, sets state = team.workflow_states[0].key when omitted (frozen #10), enforces assignee kind {human,agent} and delegate kind {agent} app-side, calls the same service layer the REST POST /tasks uses. assignee/delegate kind violations -> isError.
- **accept:** `pnpm --filter @docket/api build` passes; a tools/call create_task with a valid work:write token returns structuredContent.task.uri matching docket://{org}/task/{id} and state==="backlog"; calling with a work:read-only token returns HTTP 403 insufficient_scope.

### `MCP-tool-update_task` — Tool: update*task *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/update-task.ts`, `apps/api/src/mcp/tools/update-task.ts`
- **spec:** mcp-surface.md §3.4 update_task, api-rpc-contract.md §3.7
- **build:** Register update_task (work:write; readOnly:F destructive:F idempotent:T openWorld:F). Input {org:Slug, task_id:Id, patch:{title min1?, description?, priority Priority?, due_date date|null?, estimate number|null?, labels:Id[]? full-replace}}. Output {task:EntityRef}. Patch is CONTENT-only — reparenting fields (project/program/milestone/cycle/parent) are NOT accepted here (they go through move_task per the api-rpc PATCH-vs-move decision). When patch.priority/etc change, run the same side-effects as the REST PATCH /:taskId. Requires canActor contribute on the task.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call update_task changing title returns task EntityRef; the inputSchema (tools/list) contains no project_id/program_id/milestone_id/cycle_id/parent_task_id keys.

### `MCP-tool-move_task` — Tool: move*task *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/move-task.ts`, `apps/api/src/mcp/tools/move-task.ts`
- **spec:** mcp-surface.md §3.4 move_task, api-rpc-contract.md §3.7
- **build:** Register move_task (work:write; readOnly:F destructive:F idempotent:T openWorld:F). Input {org:Slug, task_id:Id, to:{project_id:Id|null?, program_id:Id|null?, milestone_id:Id|null?, cycle_id:Id|null?, team:string?, triage:boolean?}}. Output {task:EntityRef}. This is the ONLY path that reparents — it recomputes ancestor_path transactionally over the moved subtree (permissions §7.3). Same target = idempotent no-op. Maps to the REST POST /:taskId/move handler. team move re-keys state to the new team's workflow_states. Requires canActor contribute on source and target.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call move_task setting to.triage=true returns the task EntityRef and a subsequent resources/read of the task shows project_id detached; calling move_task twice with the same target is a no-op (no error).

### `MCP-tool-set_task_assignee` — Tool: set*task_assignee *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/set-task-assignee.ts`, `apps/api/src/mcp/tools/set-task-assignee.ts`
- **spec:** mcp-surface.md §3.4 set_task_assignee, data-model.md §4.5
- **build:** Register set_task_assignee (work:write; readOnly:F destructive:F idempotent:T openWorld:F). Input {org:Slug, task_id:Id, assignee:ActorRef|null (null=unassign)}. Output {task:EntityRef}. assignee actor must be kind {human,agent} (app-enforced; violation -> isError). Requires canActor assign on the task. Writes audit_event type assigned.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call set_task_assignee with a human actor returns task EntityRef; passing assignee=null clears the assignee; passing an actor of kind team returns isError:true.

### `MCP-tool-set_task_delegate` — Tool: set*task_delegate *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/set-task-delegate.ts`, `apps/api/src/mcp/tools/set-task-delegate.ts`
- **spec:** mcp-surface.md §3.4 set_task_delegate
- **build:** Register set_task_delegate (work:write; readOnly:F destructive:F idempotent:T openWorld:F). Input {org:Slug, task_id:Id, delegate:{agent_id:Id}|null}. Output {task:EntityRef}. delegate must reference an actor of kind agent (app-enforced; else isError). Setting a delegate does NOT dispatch a session (use trigger_agent_session). Requires canActor assign on the task.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call set_task_delegate with a valid agent_id returns task EntityRef; delegate=null clears it; a non-agent actor id returns isError:true.

### `MCP-tool-set_task_state` — Tool: set*task_state *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/set-task-state.ts`, `apps/api/src/mcp/tools/set-task-state.ts`
- **spec:** mcp-surface.md §3.4 set_task_state, api-rpc-contract.md §3.7
- **build:** Register set_task_state (work:write; readOnly:F destructive:F idempotent:T openWorld:F). Input {org:Slug, task_id:Id, state:string}. Output {task:EntityRef, state:string}. state MUST be a key in the task's team.workflow_states (else isError, matching REST POST /:taskId/state 422). On transition to a terminal type sets completed_at/canceled_at; writes audit_event type state_changed. Requires canActor contribute.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call set_task_state with a valid state key returns {task, state}; an unknown state key returns isError:true with actionable text.

### `MCP-tool-add_subtask` — Tool: add*subtask *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/add-subtask.ts`, `apps/api/src/mcp/tools/add-subtask.ts`
- **spec:** mcp-surface.md §3.4 add_subtask
- **build:** Register add_subtask (work:write; readOnly:F destructive:F idempotent:F openWorld:F). Input {org:Slug, parent_task_id:Id, title min1, idempotency_key:Idem}. Output {subtask:EntityRef}. Creates a task with parent_task_id set, inheriting team/org from the parent; state = parent team's workflow_states[0].key. Requires canActor contribute on the parent.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call add_subtask returns subtask EntityRef of type task; resources/read of the parent shows the new subtask in its subtasks list.

### `MCP-tool-task_dependencies` — Tools: add*task_dependency + remove_task_dependency (acyclic) *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/task-dependency.ts`, `apps/api/src/mcp/tools/task-dependency.ts`
- **spec:** mcp-surface.md §3.4 add/remove_task_dependency, RECONCILIATION.md task_dependency acyclicity
- **build:** Register add_task_dependency (work:write; readOnly:F destructive:F idempotent:T openWorld:F) input {org:Slug, blocking_task_id:Id, blocked_task_id:Id} output {blocking:EntityRef, blocked:EntityRef}; and remove_task_dependency (work:write; readOnly:F destructive:T idempotent:T openWorld:F) same input, output {blocking:EntityRef, blocked:EntityRef}. add MUST reject an edge that would create a cycle -> isError:true (app-level recursive-CTE check in a SERIALIZABLE tx, UNIQUE(blocking,blocked), CHECK(blocking<>blocked) per RECONCILIATION). Org-wide, cross-project. Requires canActor contribute on both tasks.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call add_task_dependency creating a cycle (A blocks B, then B blocks A) returns isError:true; remove_task_dependency annotations show destructiveHint:true in tools/list.

### `MCP-tool-post_update` — Tool: post*update *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/post-update.ts`, `apps/api/src/mcp/tools/post-update.ts`
- **spec:** mcp-surface.md §3.4 post_update, data-model.md Update
- **build:** Register post_update (work:write; readOnly:F destructive:F idempotent:F openWorld:F). Input {org:Slug, subject:{type:enum[project,program,initiative], id:Id}, health:Health, body min1}. Output {update:EntityRef, subject_health:Health}. Latest update sets the subject's health (data-model Update). Requires canActor contribute on the subject. update_subject_type is constrained to project/program/initiative (frozen enum decision).
- **accept:** `pnpm --filter @docket/api build` passes; tools/call post_update on a project returns {update:EntityRef, subject_health} and a resources/read of that project shows the new health and latest update.

### `MCP-tool-add_comment` — Tool: add*comment *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/add-comment.ts`, `apps/api/src/mcp/tools/add-comment.ts`
- **spec:** mcp-surface.md §3.4 add_comment, data-model.md Comment
- **build:** Register add_comment (work:write; readOnly:F destructive:F idempotent:F openWorld:F). Input {org:Slug, subject:{type:enum[task,project,program,initiative], id:Id}, body min1, parent_comment_id:Id?, idempotency_key:Idem}. Output {comment:EntityRef}. Agents post as their own Actor. comment_subject_type is task/project/program/initiative/cycle/update (frozen) but tool exposes the four named. Requires canActor comment on the subject.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call add_comment on a task returns comment EntityRef; resources/read of the task shows the comment in the comment stream.

### `MCP-tool-projects` — Tools: create*project + update_project *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/project.ts`, `apps/api/src/mcp/tools/project.ts`
- **spec:** mcp-surface.md §3.4 create_project/update_project
- **build:** Register create_project (work:write; readOnly:F destructive:F idempotent:F openWorld:F) input {org:Slug, name min1, description?, lead:ActorRef?, program_id:Id?, team:string?, start_date date?, target_date date?, initiative_ids:Id[]?, idempotency_key:Idem} output {project:EntityRef}; and update_project (work:write; readOnly:F destructive:F idempotent:T openWorld:F) input {org:Slug, project_id:Id, patch:{name?, description?, status enum[planned,active,completed,canceled]?, lead:ActorRef|null?, program_id:Id|null?, start_date date|null?, target_date date|null?}} output {project:EntityRef}. Status defaults planned on create. Requires canActor contribute (create) / manage-or-contribute (update) on the org/project.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call create_project returns project EntityRef with type project; update_project changing status to active is reflected on resources/read.

### `MCP-tool-program` — Tool: create*program *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/create-program.ts`, `apps/api/src/mcp/tools/create-program.ts`
- **spec:** mcp-surface.md §3.4 create_program, data-model.md Program
- **build:** Register create_program (work:write; readOnly:F destructive:F idempotent:F openWorld:F). Input {org:Slug, name min1, description?, owner:ActorRef?, initiative_ids:Id[]?, idempotency_key:Idem}. Output {program:EntityRef}. Program status set is {active,paused,archived} — NO completed (programs never finish). Requires canActor contribute on the org.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call create_program returns program EntityRef of type program; resources/read of the program returns a health+flow snapshot with no percent bar.

### `MCP-tool-initiative` — Tools: create*initiative + link_initiative *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/initiative.ts`, `apps/api/src/mcp/tools/initiative.ts`
- **spec:** mcp-surface.md §3.4 create_initiative/link_initiative
- **build:** Register create_initiative (work:write; readOnly:F destructive:F idempotent:F openWorld:F) input {org:Slug, name min1, description?, owner:ActorRef?, target_date date?, idempotency_key:Idem} output {initiative:EntityRef}; and link_initiative (work:write; readOnly:F destructive:F idempotent:T openWorld:F) input {org:Slug, initiative_id:Id, target:{type:enum[project,program], id:Id}, action:enum[link,unlink] default link} output {initiative:EntityRef, target:EntityRef}. link_initiative writes/removes the m2m row (initiative_project / initiative_program); unlink is reversible so NOT flagged destructive. Requires canActor contribute on the initiative and target.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call create_initiative returns initiative EntityRef; link_initiative with action=link then action=unlink against the same target both succeed; remove leaves the initiative intact.

### `MCP-tool-link_external` — Tool: link*external (openWorld:true, no token passthrough) *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/link-external.ts`, `apps/api/src/mcp/tools/link-external.ts`
- **spec:** mcp-surface.md §3.4 link_external, api-rpc-contract.md §3.7 link-external
- **build:** Register link_external (scope connectors:link; readOnly:F destructive:F idempotent:T openWorld:TRUE). Input {org:Slug, subject:{type:enum[task,project], id:Id}, integration_id:Id, external_url:url, role:enum[work,context,signal,time,code] default context}. Output {subject:EntityRef, provenance:{source:literal linked, external_id?:string, external_url:url, sync_mode:enum[import,mirror]}}. v1 maps to the SINGLE inline provenance triple on task (frozen api-rpc decision): SETS source=linked, sync_mode=mirror; 409/isError if the subject is already linked to a different external resource (task_source_uq). The RS resolves metadata via the org's Integration.credentials_ref — NEVER the inbound MCP token (frozen no-passthrough). Requires canActor manage on the Integration + contribute on the subject.
- **accept:** `pnpm --filter @docket/api build` passes; link_external annotations in tools/list show openWorldHint:true; tools/call returns provenance.source==="linked"; linking a task that is already linked elsewhere returns isError:true; a code path search confirms the handler reads Integration.credentials_ref and never the request bearer token.

### `MCP-tool-start_connector_link` — Tool: start*connector_link (openWorld:true, signed-state OAuth) *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/start-connector-link.ts`, `apps/api/src/mcp/tools/start-connector-link.ts`
- **spec:** mcp-surface.md §3.4 start_connector_link, api-rpc-contract.md §3.12
- **build:** Register start_connector_link (connectors:link; readOnly:F destructive:F idempotent:F openWorld:TRUE). Input {org:Slug, provider:enum[github,google_drive,gmail,google_calendar,linear,jira,asana], pattern:enum[migration,connector]}. Output {integration_id:Id, authorize_url:url, status:enum[pending,connected]}. Creates an integration row (pending) and returns the provider authorize URL with a signed (HMAC over BETTER_AUTH_SECRET) short-TTL state JWT carrying {integrationId, orgId, nonce, actorId} — the SAME create+callback path as REST POST /integrations (frozen api-rpc decision). Returned via URL-mode elicitation. Never passes any Docket token to the provider. Requires canActor manage in the org.
- **accept:** `pnpm --filter @docket/api build` passes; openWorldHint:true in tools/list; tools/call returns an authorize_url whose state param is a verifiable HMAC-signed token containing the orgId; status==="pending".

### `MCP-tool-trigger_agent_session` — Tool: trigger*agent_session (openWorld:true, taskSupport flag-gated) *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/trigger-agent-session.ts`, `apps/api/src/mcp/tools/trigger-agent-session.ts`
- **spec:** mcp-surface.md §3.4 trigger_agent_session, mcp-surface.md §5 tasks
- **build:** Register trigger_agent_session (scope agents:run; readOnly:F destructive:F idempotent:F openWorld:TRUE). Input {org:Slug, agent_id:Id, trigger:enum[assignment,delegation,mention] default delegation, task_id:Id?, prompt?}. Output {session:EntityRef (type session), status:enum[pending,running,awaiting_input,awaiting_approval]}. Creates the agent_session and returns the handle (dispatch lives in agents-sessions). execution.taskSupport: when MCP_TASKS_ENABLED=false (v1 default, frozen) set taskSupport:'forbidden' (NOT optional) so the tool never advertises augmentation while the Tasks capability is unadvertised; only when the flag is true set 'optional'. Returns 503 'agent runtime not configured' if ATHENA_AGENT_ENDPOINT/KEY (and no ANTHROPIC_API_KEY) are unset (frozen agent-env decision). Requires canActor manage on the target + approver-eligibility.
- **accept:** `pnpm --filter @docket/api build` passes; with MCP_TASKS_ENABLED unset, the tool's execution.taskSupport is 'forbidden' and the initialize capabilities object omits 'tasks'; tools/call with agents:run returns session EntityRef + a status enum; with a work:write-only token returns 403.

### `MCP-tool-session_control` — Tools: respond*to_session, approve_action, reject_action, cancel_session *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/session-control.ts`, `apps/api/src/mcp/tools/session-control.ts`
- **spec:** mcp-surface.md §3.4 respond/approve/reject/cancel, permissions.md §9.3
- **build:** Register the four agents:run session-control tools. respond_to_session (readOnly:F destructive:F idempotent:F openWorld:F) input {org:Slug, session_id:Id, activity_id:Id, body min1} output {session:EntityRef, status:string}. approve_action (readOnly:F destructive:TRUE idempotent:T openWorld:F) input {org:Slug, session_id:Id, action_id:Id, note?} output {session:EntityRef, action_id:Id, result:enum[applied,queued]}. reject_action (readOnly:F destructive:TRUE idempotent:T openWorld:F) input {org:Slug, session_id:Id, action_id:Id, reason?} output {session:EntityRef, action_id:Id, result:literal rejected}. cancel_session (readOnly:F destructive:TRUE idempotent:T openWorld:F) input {org:Slug, session_id:Id, reason?} output {session:EntityRef, status:literal canceled}. approve/reject MUST verify the caller is in the resolved approver set AND canActor(approver,'assign',target); on approve, re-run canActor(agentActor, required, target) at apply time — if it now fails, do NOT apply, mark approval_status rejected with reason capability_revoked_at_apply (frozen permissions decision).
- **accept:** `pnpm --filter @docket/api build` passes; approve_action/reject_action/cancel_session show destructiveHint:true in tools/list; approve_action by a non-approver returns isError:true; approve_action whose agent lost capability between proposal and approval returns result NOT applied and marks the activity rejected.

### `MCP-tool-run_view` — Tool: run*view (read, permission-filtered, taskSupport flag-gated) *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/run-view.ts`, `apps/api/src/mcp/tools/run-view.ts`
- **spec:** mcp-surface.md §3.4 run_view, permissions.md §7.2/§7.4
- **build:** Register run_view (scope work:read; readOnlyHint:TRUE idempotent:T openWorld:F). Input {org:Slug, view: union({view_id:Id}, {query:{entity:enum[task,project,program,initiative,cycle], filters:record?, group_by:string?, sort:[{field,dir:enum[asc,desc]}]?, limit int max200 default50, cursor?}})}. Output {items:[{ref:EntityRef, fields:record}], group_by?:string, next_cursor?:string}. view_id reads the saved_view filter spec (frozen saved_view table) and executes it; results are ALWAYS re-scoped per requester via the §7.2 query-scoping predicate (a guest never sees hidden work). execution.taskSupport mirrors trigger_agent_session: 'forbidden' when MCP_TASKS_ENABLED=false else 'optional'. Honor cursor/next_cursor pagination.
- **accept:** `pnpm --filter @docket/api build` passes; run_view annotations show readOnlyHint:true; tools/call with an ad-hoc query returns items each carrying a ref EntityRef; a caller without view grant on hidden rows receives a filtered item set (rows they cannot view are absent); with MCP_TASKS_ENABLED=false taskSupport is 'forbidden'.

### `MCP-tool-search` — Tool: search (read, hub-global or org-local) _(P6 · parallel)_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/search.ts`, `apps/api/src/mcp/tools/search.ts`
- **spec:** mcp-surface.md §3.4 search, api-rpc-contract.md §3.15
- **build:** Register search (scope work:read; readOnlyHint:TRUE idempotent:T openWorld:F). Input {query min1, scope: union(literal hub, {org:Slug}) default hub, types:enum[...]? , limit int max50 default20}. Output {results:[{ref:EntityRef, title:string, snippet?:string, org:Slug}]}. scope=hub fans out across the principal's active Actor orgs server-merged (NO cross-tenant SQL join) and per-item canActor view filters; scope={org} is org-local. Personal space slug is personal.
- **accept:** `pnpm --filter @docket/api build` passes; search annotations show readOnlyHint:true; tools/call scope=hub returns results spanning >1 org each carrying its org slug; a result the caller cannot view in some org is absent.

### `MCP-tool-add_to_daily_plan` — Tool: add*to_daily_plan (Hub-scoped) *(P6 · parallel)\_

- **depends on:** MCP-06-tool-registry
- **files:** `packages/types/src/mcp/tools/add-to-daily-plan.ts`, `apps/api/src/mcp/tools/add-to-daily-plan.ts`
- **spec:** mcp-surface.md §3.4 add_to_daily_plan, data-model.md Daily Plan
- **build:** Register add_to_daily_plan (scope work:write; readOnly:F destructive:F idempotent:T openWorld:F). Input {org:Slug, task_id:Id, date date, timebox:{start:string, end:string}?}. Output {plan_item:{id:Id, date date, task:EntityRef, status:enum[planned,done]}}. Hub-scoped (NOT org-scoped) — authorized by sub ownership of the Hub; the daily_plan_item carries hubId (owner) + ref_organization_id (the org chip), no organization_id (frozen data-model). Downstream still requires canActor view on the referenced task in its org.
- **accept:** `pnpm --filter @docket/api build` passes; tools/call add_to_daily_plan returns plan_item with status planned and a task EntityRef; adding a task in an org the caller has no active Actor in returns isError/404-style not-found.

### `MCP-07-resource-reader` — Resource reader infra: docket:// scheme, resources/read, Zod read DTOs, -32002 contract _(P6 · sequential)_

- **depends on:** MCP-03-rs-auth, MCP-05-shared-schemas
- **files:** `apps/api/src/mcp/resources/reader.ts`, `apps/api/src/mcp/resources/uri.ts`, `packages/types/src/mcp/resources/dto.ts`
- **spec:** mcp-surface.md §4.1, mcp-surface.md §4.3 read contract
- **build:** Build the docket:// resource-read engine all resource-template tickets use. uri.ts parses/builds docket://{org}/{type}/{id} (org=slug, Personal=personal, Hub literal=hub) — RFC 3986; the URI is NEVER trusted for access, the RS re-authorizes from the token (resolve slug->id, load Actor, canActor view). reader.ts implements resources/read returning contents:[{uri, mimeType:'application/json', text:<JSON projection>}] where the projection is a per-type Zod read DTO from @docket/types (stable shape). Not-found -> JSON-RPC -32002; no-grant -> ALSO -32002 (frozen existence-hiding: never leak existence to unauthorized callers). Provide a registerResourceTemplate(type, dtoSchema, projector) harness the per-type tickets append to, plus dispatch by {type}.
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of a docket://{org}/task/{id} the caller can view returns contents[0].mimeType=='application/json' and a JSON body that parses against the Task read DTO; resources/read of a non-existent id and of an existing-but-unauthorized id BOTH return JSON-RPC error code -32002.

### `MCP-res-task` — Resource template: docket://{org}/task/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/task.ts`, `apps/api/src/mcp/resources/task.ts`
- **spec:** mcp-surface.md §4.3 Task
- **build:** Register the Task resource template (uriTemplate docket://{org}/task/{id}, name Task, mimeType application/json). Read DTO + projector returns: state, assignee, delegate, project/program/milestone/cycle refs, dependencies (blocking + blocked-by, each with the other task's project), subtasks, labels, provenance (inline triple), and the merged comment+activity stream. Permission-filtered via the reader (canActor view).
- **accept:** `pnpm --filter @docket/api build` passes; resources/templates/list includes docket://{org}/task/{id}; resources/read of a task returns a JSON projection parsing against the Task read DTO including dependencies and subtasks arrays.

### `MCP-res-project` — Resource template: docket://{org}/project/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/project.ts`, `apps/api/src/mcp/resources/project.ts`
- **spec:** mcp-surface.md §4.3 Project
- **build:** Register the Project resource template (name Project, mimeType application/json). Read DTO + projector returns: overview, weighted-progress %, health, milestones with their tasks, linked initiatives, latest update. Progress/health are COMPUTED (no stored columns).
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of a project returns a JSON projection with a numeric weighted-progress, a health value, a milestones array, and latestUpdate, parsing against the Project read DTO.

### `MCP-res-program` — Resource template: docket://{org}/program/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/program.ts`, `apps/api/src/mcp/resources/program.ts`
- **spec:** mcp-surface.md §4.3 Program
- **build:** Register the Program resource template (name Program, mimeType application/json). Read DTO + projector returns: health + flow snapshot, projects, ongoing tasks grouped by cycle, linked initiatives. NO percent bar (programs never finish).
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of a program returns a JSON projection containing a health/flow snapshot and projects array, with NO progress-percent field, parsing against the Program read DTO.

### `MCP-res-initiative` — Resource template: docket://{org}/initiative/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/initiative.ts`, `apps/api/src/mcp/resources/initiative.ts`
- **spec:** mcp-surface.md §4.3 Initiative
- **build:** Register the Initiative resource template (name Initiative, mimeType application/json). Read DTO + projector returns: auto-derived rolled-up health, child-distribution, associated projects/programs (the m2m links).
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of an initiative returns a JSON projection with a derived health, a child-distribution breakdown, and associatedProjects/associatedPrograms arrays, parsing against the Initiative read DTO.

### `MCP-res-cycle` — Resource template: docket://{org}/cycle/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/cycle.ts`, `apps/api/src/mcp/resources/cycle.ts`
- **spec:** mcp-surface.md §4.3 Cycle, api-rpc-contract.md §3.6
- **build:** Register the Cycle resource template (name Cycle, mimeType application/json). Read DTO + projector returns: window (starts/ends), burn-up (planned vs done), capacity, scope changes, carryover, tasks grouped by Project/Program. Burn-up/stats are COMPUTED from audit_event + task.cycle_id (no stored columns, frozen api-rpc decision).
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of a cycle returns a JSON projection with window, a burnup series, capacity, scopeChanges, carryover, and grouped tasks, parsing against the Cycle read DTO.

### `MCP-res-team` — Resource template: docket://{org}/team/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/team.ts`, `apps/api/src/mcp/resources/team.ts`
- **spec:** mcp-surface.md §4.3 Team
- **build:** Register the Team resource template (name Team, mimeType application/json). Read DTO + projector returns: workflow_states, cycles, triage queue summary, members (human Actors via team_member join).
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of a team returns a JSON projection with workflow_states array, cycles, triageSummary, and a members array of human actors, parsing against the Team read DTO.

### `MCP-res-update-comment` — Resource templates: docket://{org}/update/{id} + docket://{org}/comment/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/update.ts`, `packages/types/src/mcp/resources/comment.ts`, `apps/api/src/mcp/resources/update-comment.ts`
- **spec:** mcp-surface.md §4.3 Update/Comment
- **build:** Register two leaf resource templates. Update (name Update, application/json): author, subject ref, health, body, timestamp. Comment (name Comment, application/json): author Actor, subject ref, body, thread parent. Both permission-filtered via the reader.
- **accept:** `pnpm --filter @docket/api build` passes; resources/templates/list includes both docket://{org}/update/{id} and docket://{org}/comment/{id}; resources/read of each returns a JSON projection parsing against the respective read DTO (Update has health+body; Comment has author+body+parent).

### `MCP-res-session` — Resource template: docket://{org}/session/{id} (subscribable, no compute/cost) _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/session.ts`, `apps/api/src/mcp/resources/session.ts`
- **spec:** mcp-surface.md §4.3 Session, data-model.md §5.6/§5.7
- **build:** Register the Agent Session resource template (name Session, application/json). Read DTO + projector returns: status, agent ref, task ref, trigger, accountability (agent + initiator actors), and the Session Activity stream (thought/action/response/elicitation/error with per-action approval_status proposed|approved|rejected|applied). Explicitly EXCLUDE compute/cost (provider owns it, data-model). Mark this URI as subscribable (consumed by MCP-15).
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of a session returns a JSON projection with status, agent/task/initiator refs, and an activity array whose action entries carry approval_status, with NO compute/cost fields, parsing against the Session read DTO.

### `MCP-res-agent-view` — Resource templates: docket://{org}/agent/{id} + docket://{org}/view/{id} _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `packages/types/src/mcp/resources/agent.ts`, `packages/types/src/mcp/resources/view.ts`, `apps/api/src/mcp/resources/agent-view.ts`
- **spec:** mcp-surface.md §4.3 Agent/Saved View, data-model.md saved_view
- **build:** Register two resource templates. Agent (name Agent, application/json): provider connection (endpoint/protocol — NO credentials), grants[], approval_policy, accountable owner, guidance. Saved View (name View, application/json): the saved_view definition (filter/grouping/sort) permission-filtered — the RESULTS come from run_view, not here (saved_view is non-grantable; the read returns only the spec). Both via the reader.
- **accept:** `pnpm --filter @docket/api build` passes; resources/read of an agent returns connection metadata with NO credentials field and a grants array; resources/read of a view returns the filter/grouping/sort spec (not a result set), each parsing against its read DTO.

### `MCP-08-static-resources` — Static resources: docket://{org}, .../inbox, and Hub today/inbox/portfolio _(P6 · parallel)_

- **depends on:** MCP-07-resource-reader
- **files:** `apps/api/src/mcp/resources/static.ts`, `packages/types/src/mcp/resources/static-dto.ts`
- **spec:** mcp-surface.md §4.2
- **build:** Register the eagerly-listed static resources for resources/list (paginated, org-scoped to what the principal can see): docket://{org} (Organization summary + vocabulary skin + counts); docket://{org}/inbox (this org's unread/unacted Notifications); docket://hub/today (Daily Plan today, cross-org, literal hub slug, authorized purely by sub); docket://hub/inbox (cross-org Inbox); docket://hub/portfolio (cross-org Programs+Projects timeline). Hub resources resolve via sub (Hub 1:1 with User), fan-out per membership and server-merge — NO cross-tenant join (frozen). Each Zod read DTO in @docket/types.
- **accept:** `pnpm --filter @docket/api build` passes; resources/list returns at least the 5 static entries with cursor/nextCursor honored; resources/read of docket://hub/today returns cross-org daily-plan items authorized by sub; resources/read of docket://{org} returns the org summary + vocabulary + counts.

### `MCP-15-subscriptions` — resources/subscribe + updated/list*changed fan-out from the service event bus *(P6 · sequential)\_

- **depends on:** MCP-res-task, MCP-res-session, MCP-08-static-resources
- **files:** `apps/api/src/mcp/subscriptions.ts`, `apps/api/src/mcp/event-bus-bridge.ts`
- **spec:** mcp-surface.md §4.4
- **build:** Advertise resources.subscribe:true and resources.listChanged:true. Implement resources/subscribe for the subscribable URIs: docket://{org}/session/{id} (live agent activity), docket://{org}/task/{id}, and Hub inbox/today. On change emit notifications/resources/updated {uri} over the SUBSCRIBING connection's GET-SSE channel, sourced from the SAME service-layer entity-change event bus that drives web realtime — NOT Redis pub/sub (frozen no-Redis). Because transport is stateless/non-resumable, subscriptions are bound to the live connection: on drop the client re-subscribes + re-reads (no replay; acceptable because resources/read always returns current truth). Fire notifications/resources/list_changed (debounced) when the principal's visible-entity set changes materially.
- **accept:** `pnpm --filter @docket/api build` passes; initialize advertises resources.subscribe:true and resources.listChanged:true; an integration test: subscribe to a docket://{org}/session/{id}, mutate the session, and observe a notifications/resources/updated for that exact uri on the SSE channel; no Redis client is imported anywhere in the subscription path.

### `MCP-16-capabilities` — Capability negotiation: initialize advertise set + completion/logging/lifecycle, Tasks flag-gated, no prompts _(P6 · sequential)_

- **depends on:** MCP-06-tool-registry, MCP-07-resource-reader, MCP-15-subscriptions
- **files:** `apps/api/src/mcp/capabilities.ts`, `apps/api/src/mcp/completion.ts`, `apps/api/src/mcp/logging.ts`
- **spec:** mcp-surface.md §5, mcp-surface.md §6 step 6
- **build:** Wire the initialize capabilities object exactly: tools{listChanged:true}, resources{subscribe:true,listChanged:true}, completions{}, logging{}, and tasks{list,cancel,requests.tools.call} ONLY when MCP_TASKS_ENABLED=true (otherwise the tasks object is OMITTED — and per-tool taskSupport stays 'forbidden', frozen: both flip together). Do NOT advertise prompts (frozen — never add speculatively). serverInfo {name:docket, title:Docket, version:<build>}, protocolVersion 2025-11-25. tools.listChanged is principal/org-aware: a token lacking agents:run does not see agent tools; emit notifications/tools/list_changed when grants/connectors change. Implement completion/complete for resource-template {id} vars (matching visible entities by recent/active), {org} (the principal's org slugs), and tool enum args (team, state from team.workflow_states, provider). Implement logging notifications/message at info/warning/error (NEVER log tokens/credentials). Support ping, notifications/progress (with progressToken), notifications/cancelled. Honor cursor/nextCursor pagination on tools/list, resources/list, resources/templates/list.
- **accept:** `pnpm --filter @docket/api build` passes; with MCP_TASKS_ENABLED unset, the initialize result capabilities object has tools/resources/completions/logging but NO tasks and NO prompts keys; a completion/complete for an {org} var returns the caller's org slugs; a client whose token lacks agents:run does not see trigger_agent_session in tools/list; ping returns a pong.

### `MCP-17-e2e-flow` — E2E flow: connect -> discover -> CIMD -> consent -> read -> step-up write; and session subscribe -> approve _(P6 · sequential)_

- **depends on:** MCP-16-capabilities, MCP-tool-create_task, MCP-tool-session_control, MCP-tool-trigger_agent_session
- **files:** `apps/web-e2e/flows/mcp-connect-read-stepup.spec.ts`, `apps/web-e2e/flows/mcp-session-approve.spec.ts`, `apps/web-e2e/flow-registry.ts`
- **spec:** mcp-surface.md §6 step 9
- **build:** Author the two declared MCP e2e flows in the typed flow registry (engineering plan §6) so the coverage gate enforces them. Flow A (mcp-connect): connect an MCP client -> discover PRM + AS metadata -> CIMD register -> consent -> call a read tool (search or run_view) with work:read -> attempt a write tool (create_task) and receive 403 insufficient_scope -> run step-up authorization for work:write -> re-call create_task successfully. Flow B (mcp-session): trigger_agent_session -> resources/subscribe the docket://{org}/session/{id} resource -> observe a notifications/resources/updated -> approve_action. Register both flow ids in flow-registry.ts. Reuse the CDP virtual-authenticator fixture for the consent login.
- **accept:** `pnpm --filter @docket/web-e2e exec playwright test flows/mcp-connect-read-stepup.spec.ts flows/mcp-session-approve.spec.ts` is green in CI under lean capture; the flow-coverage gate passes with both new flow ids present; Flow A asserts the first create_task returns 403 and the post-step-up create_task succeeds.

## ui (23 tickets)

### `UI-00-pkg-tokens` — @docket/ui package scaffold + design tokens (globals.css) + tw-animate-css _(P5 · sequential)_

- **files:** `packages/ui/package.json`, `packages/ui/tsconfig.json`, `packages/ui/src/styles/globals.css`, `packages/ui/src/lib/utils.ts`, `packages/ui/src/index.ts`, `tooling/tailwind-config/globals.css`
- **spec:** design-system.md §0.1, design-system.md §1, design-system.md §1.5-1.8, design-system.md §2.4
- **build:** Scaffold JIT @docket/ui (raw TS, consumed via transpilePackages; NEVER compiled to dist) per design-system §0.1. Author packages/ui/src/styles/globals.css EXACTLY per §1: `@import "tailwindcss"; @import "tw-animate-css";` (tw-animate-css, NOT deprecated tailwindcss-animate, per frozen design-system decision), the full :root OKLCH primitive/semantic/health/state/priority/agent/session/org-accent/chart tokens plus dark browser media-query overrides, the `@theme inline --color-*` + `--radius-*` + `--font-sans/--font-mono` mapping, `@layer base` rules. RENAME the work-state color tokens per frozen design-system decision to key off WorkflowState.type taxonomy: `--state-backlog`, `--state-unstarted` (was --state-todo), `--state-started` (was --state-in-progress), `--state-completed` (was --state-done), `--state-canceled`, and their `--color-state-*` utilities. Add §1.5 `@theme inline --shadow-popover/--shadow-dialog/--shadow-card-hover`, §1.7 density `@layer base` `--row-py/--row-h` overrides keyed by `[data-density]`, and §1.8 motion `--ease-*/--duration-*` tokens with a `prefers-reduced-motion` collapse block. Re-export the file from `tooling/tailwind-config/globals.css`. Add `cn()` (clsx + tailwind-merge) in lib/utils.ts. Add deps per §2.4 frozen set (drop react-arborist; do not add next-themes or CSS-token parser deps).
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes; `pnpm --filter @docket/ui exec vite build` / consumer build resolves `@docket/ui/styles/globals.css`. Observable: importing globals.css into a Next app renders neutral surfaces in light and dark according to browser color scheme.

### `UI-01-shadcn-primitives` — Vendored shadcn new-york primitives + per-app components.json _(P5 · sequential)_

- **depends on:** UI-00-pkg-tokens
- **files:** `packages/ui/src/primitives/button.tsx`, `packages/ui/src/primitives/input.tsx`, `packages/ui/src/primitives/dialog.tsx`, `packages/ui/src/primitives/command.tsx`, `packages/ui/src/primitives/popover.tsx`, `packages/ui/src/primitives/dropdown-menu.tsx`, `packages/ui/src/primitives/tooltip.tsx`, `packages/ui/src/primitives/sheet.tsx`, `packages/ui/src/primitives/drawer.tsx`, `packages/ui/src/primitives/resizable.tsx`, `packages/ui/src/primitives/index.ts`, `apps/web/components.json`, `apps/admin/components.json`, `apps/marketing/components.json`
- **spec:** design-system.md §2.1, design-system.md §3.1
- **build:** Vendor the full shadcn new-york primitive set listed in design-system §3.1 into packages/ui/src/primitives (Button, Input, Textarea, Label, Checkbox, RadioGroup, Switch, Select, Command(cmdk), Dialog, Sheet, Drawer(vaul), Popover, DropdownMenu, ContextMenu, Tooltip, HoverCard, Tabs, Badge, Avatar, Separator, ScrollArea, Collapsible, Accordion, Progress, Skeleton, Sonner, Calendar, Resizable, Breadcrumb, Table, AlertDialog, Toggle, ToggleGroup). All committed (vendored, not a dependency); cssVariables:true so they consume our tokens; do NOT fork — extend via composition. Author components.json per app per §2.1 (web/admin: rsc:true; marketing rsc:true per frozen design-system decision) with aliases components->@docket/ui/components, ui->@docket/ui/primitives, utils->@docket/ui/lib/utils, hooks->@docket/ui/hooks, baseColor neutral, config "". Barrel-export from primitives/index.ts.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes; rendering `<Button variant="default">` in a consumer app shows `bg-primary` tokenized color; `npx shadcn@latest add <x>` lands new files in packages/ui/src/primitives. Observable: a Dialog opens with `shadow-dialog`, traps focus, restores on close.

### `UI-02-fonts-theme-density` — Font loading, CSS browser theme, density + org-accent + reduced-motion wiring _(P5 · sequential)_

- **depends on:** UI-00-pkg-tokens
- **files:** `packages/ui/src/hooks/use-density.ts`, `packages/ui/src/lib/org-accent.ts`, `apps/web/src/app/layout.tsx`, `apps/web/src/app/globals.css`
- **spec:** design-system.md §1.2, design-system.md §1.6, design-system.md §1.7, design-system.md §1.8, design-system.md §2.2, design-system.md §2.3
- **build:** Per frozen design-system theme decision: dark mode is CSS-native via `@media (prefers-color-scheme: dark)` in `@docket/ui/styles/globals.css`; do NOT add next-themes, a JS theme provider, a root `.dark` synchronizer, or `localStorage.theme`. Each app loads fonts via next/font in its own root layout: `Inter` variable -> `--font-sans`, `Geist_Mono` -> `--font-mono` on <html> className (@docket/ui must NOT import next/font). Implement use-density.ts (reads/writes Hub.preferences.density, sets data-density). Implement org-accent.ts per frozen design-system decision: ORG_ACCENT_PALETTE (8 contrast-safe OKLCH entries), `getOrgAccent(org)` = palette[hashToIndex(org.id, 8)]; Hub/personal use var(--primary). Each app's app/globals.css `@import "@docket/ui/styles/globals.css"; @source ...packages/ui/src/**...`.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable in apps/web: browser dark/light preference changes the app through CSS variables; `prefers-reduced-motion: reduce` collapses transition durations to 0ms; getOrgAccent returns a stable hue for a given org.id across reloads.

### `UI-03-vocabulary` — Vocabulary skin system (VocabularyProvider + useVocabulary + presets) _(P5 · parallel)_

- **depends on:** UI-00-pkg-tokens
- **files:** `packages/types/src/domain/vocabulary.ts`, `packages/ui/src/vocabulary/presets.ts`, `packages/ui/src/vocabulary/VocabularyProvider.tsx`, `packages/ui/src/hooks/use-vocabulary.ts`
- **spec:** design-system.md §6, design-system.md §0.3
- **build:** Implement the vocabulary skin system per design-system §6 + frozen design-system decisions. Source-of-truth Zod schema `VocabularySkin = { preset: 'startup'|'nonprofit'|'agency'; overrides?: Partial<Record<VocabKey,{singular,plural}>> }` with `VocabKey = 'initiative'|'program'|'project'|'cycle'|'task'|'milestone'|'team'|'triage'|'myWork'|'views'|'agents'|'settings'` in @docket/types/domain/vocabulary.ts. Export presetStartup (all-default English nouns; startup IS the default — rename design-system's 'Default' to startup to match OrgCreate.vocabulary enum), presetNonprofit, presetAgency as FULL Record<VocabKey,{singular,plural}> constants in @docket/ui. `program` is a first-class skinnable key (frozen): startup/nonprofit = {Program,Programs}, agency = {Retainer,Retainers}. VocabularyProvider (scoped to active org context; Hub uses presetStartup). `useVocabulary()` returns `t(key,{count})` resolving overrides[key] -> preset[key] -> presetStartup[key], pluralizing by count.
- **accept:** `pnpm --filter @docket/types exec tsc --noEmit` and `pnpm --filter @docket/ui exec tsc --noEmit` pass; `pnpm --filter @docket/types test` validates VocabularySkin Zod parse. Observable: `useVocabulary().t('initiative',{count:2})` returns 'Campaigns' under nonprofit and 'Initiatives' under startup; an unknown override key falls back to presetStartup.

### `UI-04-atoms` — Identity atoms (ActorAvatar, Health/Status/Priority, OrgChip, KeyHint, SessionStatusPill, etc.) _(P5 · parallel)_

- **depends on:** UI-01-shadcn-primitives
- **files:** `packages/ui/src/components/atoms/ActorAvatar.tsx`, `packages/ui/src/components/atoms/ActorChip.tsx`, `packages/ui/src/components/atoms/OrgChip.tsx`, `packages/ui/src/components/atoms/HealthPill.tsx`, `packages/ui/src/components/atoms/HealthDot.tsx`, `packages/ui/src/components/atoms/StatusIcon.tsx`, `packages/ui/src/components/atoms/PriorityIcon.tsx`, `packages/ui/src/components/atoms/LabelChip.tsx`, `packages/ui/src/components/atoms/KeyHint.tsx`, `packages/ui/src/components/atoms/IdTag.tsx`, `packages/ui/src/components/atoms/CountBadge.tsx`, `packages/ui/src/components/atoms/AttentionBadge.tsx`, `packages/ui/src/components/atoms/ProvenanceChip.tsx`, `packages/ui/src/components/atoms/SessionStatusPill.tsx`, `packages/ui/src/components/atoms/Timestamp.tsx`, `packages/ui/src/components/atoms/EmptyState.tsx`, `packages/ui/src/components/atoms/index.ts`
- **spec:** design-system.md §3.2, design-system.md §1.1, design-system.md §1.4, design-system.md §8
- **build:** Build all atoms in design-system §3.2 (atoms/_) with the EXACT normative names. ActorAvatar handles human (circle), org (rounded-lg squircle), agent (rounded-md + violet ring) + team shapes per §1.4 ring rules. StatusIcon selects color/shape by WorkflowState.type (backlog grey dashed / unstarted grey solid / started amber partial / completed green / canceled grey strike) per frozen design-system decision (NOT by state key). HealthPill/HealthDot encode health both chromatically (`_-subtle` bg + solid text/dot) AND non-chromatically (icon shape + label: on-track filled circle, at-risk half/triangle, off-track open/alert, no-update dashed) per §1.1/§8. PriorityIcon (none|urgent|high|medium|low). OrgChip = org-label chip used on Hub items (org-accent tint). KeyHint renders ⌘K / chord hints. IdTag mono ACME-128. AttentionBadge = rail unread+approval dot. SessionStatusPill (running pulse 2s loop disabled under reduced-motion / awaiting / paused / errored) using --session-\* tokens. Timestamp relative + hover-exact (Tooltip). ProvenanceChip linked-source tag. EmptyState. All keyboard-reachable, tabular-nums on counts, no raw hex.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable: StatusIcon for a `completed`-type state shows the green check shape in both themes; HealthPill at_risk shows amber subtle bg with dark-amber foreground (never white-on-amber) plus a triangle icon + 'At risk' label; SessionStatusPill 'running' pulses unless prefers-reduced-motion is set; atoms render with token utilities only (grep finds no `#` hex in component files).

### `UI-05-shell` — App Shell: GlobalRail, ContextSidebar, AppShell, ContextProvider (rebind) _(P5 · sequential)_

- **depends on:** UI-04-atoms, UI-03-vocabulary, UI-02-fonts-theme-density
- **files:** `packages/ui/src/components/shell/AppShell.tsx`, `packages/ui/src/components/shell/GlobalRail.tsx`, `packages/ui/src/components/shell/RailOrgAvatar.tsx`, `packages/ui/src/components/shell/RailButton.tsx`, `packages/ui/src/components/shell/AddOrgButton.tsx`, `packages/ui/src/components/shell/ContextSidebar.tsx`, `packages/ui/src/components/shell/SidebarNavItem.tsx`, `packages/ui/src/components/shell/SidebarSection.tsx`, `packages/ui/src/components/shell/SidebarOrgHeader.tsx`, `packages/ui/src/components/shell/ContextProvider.tsx`, `packages/ui/src/components/shell/InboxRailButton.tsx`, `packages/ui/src/components/shell/SearchRailButton.tsx`, `packages/ui/src/components/shell/index.ts`
- **spec:** design-system.md §4, design-system.md §1.6, design-system.md §6
- **build:** Build the App Shell per design-system §4 with normative names. AppShell (client) holds ContextProvider (active context = Hub | org id), density, and org-accent; three/four persistent independently-scrolling regions (rail 56px / sidebar 240px resizable 200-320 collapsible-0 / main flex-1 / properties optional) via shadcn Resizable; collapse+width persist to Hub.preferences (sidebarCollapsed/Width via PATCH /v1/hub/preferences). GlobalRail (bg-rail, dark in both themes, immutable across rebinds): Hub button, InboxRailButton (AttentionBadge cross-org), SearchRailButton (opens CommandPalette, ⌘K hint), Separator, one RailOrgAvatar per membership + Personal space (squircle, org-accent ring when active, per-org AttentionBadge, dnd-kit reorderable+pinnable persisted to Hub.preferences.railOrder/railPins), AddOrgButton. 3px bg-org-accent active-indicator bar. Right-side Tooltip + KeyHint per item. Selecting Hub/org REBINDS ContextProvider (swap sidebar+main, write --org-accent for org) as a 240ms cross-fade (collapsed to 0 under reduced-motion). ContextSidebar two skins: Hub (Today/Inbox/Portfolio/Search + Personal shortcut, fixed labels) and Org (SidebarOrgHeader + My Work/Triage/Initiatives/Programs/Projects/Cycles/Teams/Views/Agents/Settings, ALL vocabulary-skinned via useVocabulary; Agents+org AttentionBadge; Views/Teams Collapsible). Build against mock context data (no live API needed).
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable (Storybook/\_dev page): clicking an org avatar rebinds — sidebar swaps to org nav (labels skinned), --org-accent applies to ring/active bar, 240ms cross-fade (0ms under reduced-motion); rail order survives reorder via dnd-kit and persists; sidebar collapses via `[` and width is resizable within 200-320px; rail stays dark in light AND dark theme.

### `UI-06-hooks-keyboard` — Keyboard model: useGlobalHotkeys + useListKeyboard + KbarHelpDialog _(P5 · parallel)_

- **depends on:** UI-01-shadcn-primitives
- **files:** `packages/ui/src/hooks/use-global-hotkeys.ts`, `packages/ui/src/hooks/use-list-keyboard.ts`, `packages/ui/src/components/feedback/KbarHelpDialog.tsx`
- **spec:** design-system.md §7, design-system.md §5.1
- **build:** Implement the global keyboard registry per design-system §7. useGlobalHotkeys: scoped registry (List/editor contexts can claim keys) binding ⌘K (palette), ⌘0-9 / G-H (org/Hub switch), G-then-{I/T/P/R/A/V} go-to chords (org-local, vocab-aware), C (context create), [ / ] (toggle sidebar/properties), \ (toggle command scope), ? (help), Esc (always escapes overlay/selection/inline-edit). useListKeyboard (shared by all List instances): ↑/↓ or J/K selection, ←/→ collapse/expand group, Enter/O open, Space peek, X multi-select, Shift+↑↓ range, E edit title, C create-in-group, Cmd+↑↓ reorder, property hotkeys S/A/P/D/L/Shift+P; roving tabindex. KbarHelpDialog (?) lists everything with KeyHint. No conflicts with screen-reader/browser reserved combos.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes; unit test asserts the registry resolves a chord (G then I) and that scope-claiming suppresses a global key while a List owns focus. Observable: pressing ? opens KbarHelpDialog; Esc closes it; ⌘K invokes the registered palette callback.

### `UI-07-listview` — Shared component: ListView family (virtualized grouping/sub-grouping + inline edit + reorder) _(P5 · sequential)_

- **depends on:** UI-04-atoms, UI-06-hooks-keyboard, UI-03-vocabulary
- **files:** `packages/ui/src/components/views/ListView.tsx`, `packages/ui/src/components/views/ListGroup.tsx`, `packages/ui/src/components/views/ListSubGroup.tsx`, `packages/ui/src/components/views/ListRow.tsx`, `packages/ui/src/components/views/ListRowInlineEditor.tsx`, `packages/ui/src/components/views/GroupHeader.tsx`, `packages/ui/src/components/views/ViewToolbar.tsx`, `packages/ui/src/components/views/FilterBar.tsx`, `packages/ui/src/components/views/DisplayMenu.tsx`, `packages/ui/src/components/feedback/OptimisticBadge.tsx`, `packages/ui/src/components/feedback/LoadingRows.tsx`, `packages/ui/src/components/views/index.ts`
- **spec:** design-system.md §5.1, design-system.md §8.3, design-system.md §8
- **build:** Build the ListView workhorse per design-system §5.1 using a CUSTOM virtualized flattened tree on @tanstack/react-virtual ONLY (drop react-arborist per frozen design-system decision). Props: items, groupBy, subGroupBy?, sortBy, columns, density, selection + callbacks (onInlineEdit, onReorder, onOpen). Flatten group-header + sub-group-header + row into one virtual list; collapsed groups omit descendants from the flattened set. GroupHeader: skinned label (useVocabulary) + CountBadge + optional health/aggregate (e.g. Project progress) + chevron, Collapsible, collapse state persisted per view to Hub.preferences.viewState[viewKey] (viewKey '<context>:<screen>'). ListRow fixed --row-h: default Task cell order StatusIcon·PriorityIcon·IdTag·title(truncate)·LabelChips·SessionPill·spacer·ActorAvatar·due Timestamp·ProvenanceChip; hover row actions; selected = bg-surface-3 + left org-accent hairline. ListRowInlineEditor optimistic (OptimisticBadge) with toast+revert on failure; property hotkeys S/A/P/D/L/Shift+P via useListKeyboard. dnd-kit reorder ENABLED only when sortBy is manual; for computed sort hide drag handles + Tooltip 'Switch to manual sort to reorder' (frozen design-system decision). role=grid/row/gridcell, aria-selected, aria-expanded, aria-live=polite for optimistic updates. ViewToolbar/FilterBar/DisplayMenu (grouping/sub-grouping/density/properties-shown). This unblocks most screens.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes; unit/render test mounts ListView with 2000 mock tasks groupBy=project subGroupBy=state and asserts only viewport rows render (virtualization), collapsing a group removes its rows from the DOM, and an inline status edit fires onInlineEdit optimistically. Observable: keyboard ↑/↓ moves selection, ←/→ collapse/expand, E edits title; reorder handles hidden under a computed sort.

### `UI-08-detail-panel` — Shared component: DetailPage + PropertiesPanel + property editors _(P5 · sequential)_

- **depends on:** UI-07-listview, UI-05-shell
- **files:** `packages/ui/src/components/views/DetailPage.tsx`, `packages/ui/src/components/views/DetailHeader.tsx`, `packages/ui/src/components/views/PropertiesPanel.tsx`, `packages/ui/src/components/views/PropertyRow.tsx`, `packages/ui/src/components/views/PropertyEditor.tsx`
- **spec:** design-system.md §5.2, design-system.md §4.4, design-system.md §4.5
- **build:** Build DetailPage + PropertiesPanel per design-system §5.2 + §4.4. DetailHeader: inline-editable text-h1 title, optional HealthPill (Project/Program/Initiative), optional weighted Progress bar (Project only), target date, Breadcrumb, Tabs row (Overview / entity tabs / Updates). Body switches by tab. PropertiesPanel (right, surface-1, resizable 260-420 default 300, collapsible via `]`, persisted to Hub.preferences.propertiesCollapsed/Width): stacked PropertyRow (label left, PropertyEditor right). Universal PropertyEditor variants: assignee/lead/owner (ActorAvatar picker -> Human|Agent), status/state, priority, dates (Calendar), labels, program/project/initiative links, external links (ProvenanceChip rows), and the comments+activity feed at bottom. Responsive per §4.5: 768-1024px PropertiesPanel defaults collapsed and `]` opens it as a right shadcn Sheet overlay (frozen design-system decision); <768px it is a vaul bottom Drawer; >=1024px inline resizable. Detail content max-width 760px when no properties panel, full-bleed for List/Timeline.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable: a DetailPage renders header + Tabs + right PropertiesPanel; `]` toggles the panel (inline >=1024px, Sheet overlay 768-1024px, bottom Drawer <768px); editing a PropertyRow assignee opens the Human|Agent ActorAvatar picker; comments+activity feed renders at panel bottom.

### `UI-09-command-palette` — Shared component: CommandPalette (Cmd+K, scope toggle, action/nav/entity/org-switch groups) _(P5 · sequential)_

- **depends on:** UI-04-atoms, UI-06-hooks-keyboard
- **files:** `packages/ui/src/components/command/CommandPalette.tsx`, `packages/ui/src/components/command/CommandScopeToggle.tsx`, `packages/ui/src/components/command/CommandGroup.tsx`, `packages/ui/src/components/command/CommandActionItem.tsx`, `packages/ui/src/components/command/CommandEntityItem.tsx`, `packages/ui/src/components/command/CommandOrgSwitchItem.tsx`, `packages/ui/src/components/command/index.ts`
- **spec:** design-system.md §5.4, design-system.md §7
- **build:** Build the CommandPalette per design-system §5.4 on cmdk Command inside a Dialog. Opens on ⌘K/Ctrl+K from anywhere (via useGlobalHotkeys); shadow-popover, scale-in from 0.98 (collapsed under reduced-motion). CommandScopeToggle top-right: Hub-global vs org-local (toggle via `\`); org-local pre-filters to active org; Hub-global searches across orgs and each result carries an OrgChip. Result groups (CommandGroup): Actions (CommandActionItem 'Create task','Plan my day','Delegate to Athena…'), Go to (navigation), Entities (CommandEntityItem with StatusIcon/HealthPill), Switch org (CommandOrgSwitchItem triggers shell rebind). Each item shows KeyHint and (Hub scope) OrgChip. Sub-modes: `>` actions-only, `@` people/agents, `#` labels, nav verbs. Async results stream with LoadingRows. Focus trapped + restored. Data via injected async query callbacks (no direct API coupling in @docket/ui).
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable: ⌘K opens the palette with scale-in; `\` flips scope and Hub-global results show OrgChips; typing `>` filters to Actions; selecting CommandOrgSwitchItem fires the rebind callback; Esc closes and restores focus.

### `UI-10-session-view` — Shared component: SessionView + activity stream + inline approval + controls _(P5 · sequential)_

- **depends on:** UI-08-detail-panel, UI-04-atoms
- **files:** `packages/ui/src/components/session/SessionView.tsx`, `packages/ui/src/components/session/SessionActivityStream.tsx`, `packages/ui/src/components/session/SessionActivityItem.tsx`, `packages/ui/src/components/session/InlineApprovalCard.tsx`, `packages/ui/src/components/session/ElicitationReply.tsx`, `packages/ui/src/components/session/SessionChangesPanel.tsx`, `packages/ui/src/components/session/SessionAccountabilityCard.tsx`, `packages/ui/src/components/session/SessionControls.tsx`, `packages/ui/src/components/session/SessionPill.tsx`, `packages/ui/src/components/session/AgentsHereStrip.tsx`, `packages/ui/src/components/session/index.ts`
- **spec:** design-system.md §5.5, design-system.md §8.6
- **build:** Build the first-class agent SessionView per design-system §5.5 (two-column inside a DetailPage-like frame). Left SessionActivityStream: SessionActivityItem variants mapped to session_activity.type — thought (💭 muted), action (approval lifecycle proposed->approved/rejected->applied; under act_with_approval renders InlineApprovalCard with primary 'Approve & send' + secondary 'Review each'/'Reject' per the agent's approval_policy), response (💬), elicitation (❓ -> inline ElicitationReply textarea), error (--session-errored). Right column: SessionChangesPanel ('Changes this session'), SessionAccountabilityCard ('Athena · on behalf of you' from accountable_owner_id + initiator), SessionControls (Pause/Take over/Cancel session). Header: back link to task, OrgChip, agent ActorAvatar (violet ring), SessionStatusPill + elapsed Timestamp. Live: stream appends via an injected subscription (SSE per frozen api decision; component consumes an async iterator/onActivity callback, not a direct fetch), running shows pulse dot (off under reduced-motion), aria-live=polite announces new items. SessionPill = the live pill embedded in List rows (opens SessionView). AgentsHereStrip ('Agents here: Athena · last: … · ⚠ N appr'). InlineApprovalCard content reusable as ApprovalDigestItem/InboxItem (export the shared body).
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable: SessionView renders left stream + right changes/accountability/controls; a proposed action shows InlineApprovalCard with Approve & send; an elicitation shows an inline reply textarea; appending a mock activity to the stream announces via aria-live and the running pulse stops under prefers-reduced-motion.

### `UI-11-daily-plan` — Shared component: DailyPlan three-pane cockpit (Plan / Calendar / Needs-Attention) _(P5 · sequential)_

- **depends on:** UI-07-listview, UI-10-session-view, UI-04-atoms
- **files:** `packages/ui/src/components/hub/DailyPlan.tsx`, `packages/ui/src/components/hub/DailyPlanColumn.tsx`, `packages/ui/src/components/hub/DailyPlanGroup.tsx`, `packages/ui/src/components/hub/DailyPlanItem.tsx`, `packages/ui/src/components/hub/PullFromOrgPicker.tsx`, `packages/ui/src/components/hub/CalendarColumn.tsx`, `packages/ui/src/components/hub/NeedsAttentionColumn.tsx`, `packages/ui/src/components/hub/AttentionSection.tsx`, `packages/ui/src/components/hub/ApprovalDigestItem.tsx`, `packages/ui/src/components/hub/InboxList.tsx`, `packages/ui/src/components/hub/InboxItem.tsx`, `packages/ui/src/components/hub/ActivityFeed.tsx`, `packages/ui/src/components/hub/index.ts`
- **spec:** design-system.md §5.6, design-system.md §8.1
- **build:** Build the Daily Plan cockpit per design-system §5.6. Three panes: DailyPlanColumn (Plan) · CalendarColumn · NeedsAttentionColumn. Plan: DailyPlanGroup grouped by organization with OrgChip header, each DailyPlanItem org-chipped (status planned|done), backed by cross-org DailyPlan/DailyPlanItem; PullFromOrgPicker ('+ pull from any org…'); dnd-kit drag to reorder/timebox. CalendarColumn: day's events beside the plan (timebox drop target). NeedsAttentionColumn: AttentionSections — Approvals (ApprovalDigestItem, one-tap low-risk, deep into Session for full diff; reuse InlineApprovalCard body from UI-10), Blocked, Due today, Inbox count — sourced from Notification + session approvals. Split surfaces: InboxList (action items) vs ActivityFeed (passive awareness). Data via injected callbacks (no direct API in @docket/ui).
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable: DailyPlan renders three panes; Plan items grouped per-org each with an OrgChip; an ApprovalDigestItem one-tap fires the approve callback and 'deep into session' navigates to SessionView; dragging a plan item reorders it (dnd-kit); InboxList and ActivityFeed are distinct surfaces.

### `UI-12-timeline-roadmap` — Shared component: TimelineView/Roadmap + PortfolioRoadmap (org swimlanes) _(P5 · parallel)_

- **depends on:** UI-04-atoms, UI-07-listview
- **files:** `packages/ui/src/components/views/TimelineView.tsx`, `packages/ui/src/components/views/TimelineLane.tsx`, `packages/ui/src/components/views/TimelineBar.tsx`, `packages/ui/src/components/views/TimelineMilestoneMarker.tsx`, `packages/ui/src/components/views/TimeScaleHeader.tsx`, `packages/ui/src/components/hub/PortfolioRoadmap.tsx`, `packages/ui/src/components/hub/OrgSwimlane.tsx`
- **spec:** design-system.md §5.3, design-system.md §8.2
- **build:** Build the Roadmap primitives per design-system §5.3. TimeScaleHeader with adaptive granularity (day/week/month/quarter auto-picked from visible range + manual override) and a now line. Rows = TimelineLane. For Portfolio: nested OrgSwimlane -> Program lanes (container, NO bar — programs never end) -> TimelineBar per Project inside its program lane. TimelineBar: name + health tint (bg-health-_-subtle, border health-_) + TimelineMilestoneMarker (◆) + agent/approval signal glyph (🤖/⚠ -> SessionStatusPill). Initiatives = FilterBar chips that highlight/dim lanes (not drawn geometry). Drag to reschedule (dnd-kit) where the user has contribute; read-only otherwise (with a keyboard reschedule alternative per §8). Virtualized horizontally + vertically. PortfolioRoadmap composes TimelineView with org swimlanes.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable: PortfolioRoadmap renders org swimlanes each with program lanes (no bars) and project bars (health-tinted, milestone ◆); time scale auto-switches granularity on zoom; an Initiative filter chip dims non-member lanes; bars are read-only without contribute, draggable with it.

### `UI-13-feedback-util` — Feedback/util components: Toaster, ConfirmDialog, ErrorBoundaryFallback, KanbanBoard (de-emphasized) _(P5 · parallel)_

- **depends on:** UI-01-shadcn-primitives, UI-04-atoms
- **files:** `packages/ui/src/components/feedback/Toaster.tsx`, `packages/ui/src/components/feedback/ConfirmDialog.tsx`, `packages/ui/src/components/feedback/ErrorBoundaryFallback.tsx`, `packages/ui/src/components/views/KanbanBoard.tsx`, `packages/ui/src/components/views/KanbanColumn.tsx`, `packages/ui/src/components/views/KanbanCard.tsx`, `packages/ui/src/components/feedback/index.ts`
- **spec:** design-system.md §3.2, design-system.md §1.5
- **build:** Build the remaining feedback/util + de-emphasized board components per design-system §3.2. Toaster (sonner wrapper), ConfirmDialog (AlertDialog-based), ErrorBoundaryFallback. KanbanBoard + KanbanColumn + KanbanCard built but NOT default (kanban de-emphasized per product §7), drag-cards-between-columns via dnd-kit, shadow-card-hover on cards only. All token-driven, keyboard-reachable.
- **accept:** `pnpm --filter @docket/ui exec tsc --noEmit` passes. Observable: a sonner toast appears on a triggered action; ConfirmDialog blocks until confirm/cancel; a KanbanCard drags between columns and shows shadow-card-hover on hover; ErrorBoundaryFallback renders on a thrown child error.

### `UI-screen-today` — Screen: Hub / Today cockpit (apps/web) _(P6 · parallel)_

- **depends on:** UI-11-daily-plan, UI-05-shell, UI-09-command-palette
- **files:** `apps/web/src/app/(app)/today/page.tsx`, `apps/web/src/app/(app)/inbox/page.tsx`, `apps/web/src/app/(app)/layout.tsx`, `apps/web/src/lib/rpc.ts`, `apps/web/src/lib/hub-data.ts`
- **spec:** mvp-plan.md §8.1, design-system.md §5.6, design-system.md §4.3
- **build:** Compose the Hub Today cockpit screen per mvp-plan §8.1 + design-system §5.6 in the Hub-context shell. (app)/layout.tsx mounts AppShell in Hub context (GlobalRail with one RailOrgAvatar per `GET /v1/orgs` membership + Personal; Hub ContextSidebar Today/Inbox/Portfolio/Search). today/page.tsx (Server Component fetch + client DailyPlan): renders DailyPlan three-pane wired to cross-org data (GET /v1/dailyplan, GET /v1/hub/today, GET /v1/notifications) via the typed RPC client (hc<AppType>); Plan grouped by org with OrgChips; Needs-Attention from notifications + session approvals; PullFromOrgPicker -> POST /v1/dailyplan/items. inbox/page.tsx: InboxList (action items) split from ActivityFeed. Every item org-chipped. Per-item capability filtering is server-resolved (fan-out per active Actor); UI only renders returned items. CommandPalette wired Hub-global by default.
- **accept:** `pnpm --filter @athena/web build` (Next build) succeeds and `hc<AppType>` types resolve with no `any`; Playwright/manual: signing in and visiting /today shows the three-pane cockpit with real tasks pulled from >=2 orgs each org-chipped, an approval digest item, and PullFromOrgPicker adding a cross-org item that persists on reload.

### `UI-screen-portfolio` — Screen: Portfolio cross-org roadmap (apps/web) _(P6 · parallel)_

- **depends on:** UI-12-timeline-roadmap, UI-05-shell
- **files:** `apps/web/src/app/(app)/portfolio/page.tsx`, `apps/web/src/lib/portfolio-data.ts`
- **spec:** mvp-plan.md §8.2, design-system.md §5.3
- **build:** Compose the Portfolio screen per mvp-plan §8.2 + design-system §5.3 in Hub context. portfolio/page.tsx renders PortfolioRoadmap: org swimlanes (one per membership) -> Program lanes (containers, no bar) -> Project TimelineBars (health-tinted, milestone ◆, agent/approval 🤖 glyph). Initiatives rendered as FilterBar chips that highlight/dim lanes. Data fetched cross-org via the typed RPC client (programs/projects/milestones per org, server fan-out, never a cross-tenant join), each carrying organizationId for the swimlane. Adaptive time scale. Reschedule drag enabled only where the caller has contribute (server-provided capability), read-only otherwise.
- **accept:** `pnpm --filter @athena/web build` succeeds with no `any` from RPC. Manual/Playwright: /portfolio shows >=2 org swimlanes on one timeline, programs as lanes without bars, projects as health-tinted bars; toggling an Initiative chip dims non-member projects; data from different orgs never mixes within a lane.

### `UI-screen-org-work` — Screen: Org work views — My Work, Task list, Triage, Views (apps/web) _(P6 · parallel)_

- **depends on:** UI-07-listview, UI-05-shell, UI-09-command-palette
- **files:** `apps/web/src/app/(app)/orgs/[orgSlug]/layout.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/my-work/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/tasks/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/triage/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/views/page.tsx`, `apps/web/src/lib/org-work-data.ts`
- **spec:** mvp-plan.md §8.3, design-system.md §5.1, design-system.md §6
- **build:** Compose the org-scoped work views per mvp-plan §8.3 + design-system §5.1. orgs/[orgSlug]/layout.tsx rebinds AppShell into org context (writes --org-accent via getOrgAccent, mounts org ContextSidebar with VocabularyProvider for that org's skin). my-work/page.tsx = agent-aware split ListView: 'Assigned to me' vs 'Delegated to my agents / awaiting my approval'. tasks/page.tsx = the workhorse ListView default groupBy=project subGroupBy=state (skinned headers); agent-run rows show SessionPill opening the SessionView; a task with no project renders under 'No project / Triage'. triage/page.tsx = org-level aggregate, one newest-first ListView, each item source-tagged (ProvenanceChip), drills into per-Team queues. views/page.tsx = saved Views index (run a saved view -> ListView). All fetch via typed RPC (GET /v1/orgs/:orgId/tasks etc.); results are server permission-filtered (a guest never sees hidden work). state default = team.workflow_states[0].key (backlog) per frozen build-sequence decision.
- **accept:** `pnpm --filter @athena/web build` succeeds, RPC types resolve no `any`. Manual/Playwright: entering an org rebinds (accent + skinned sidebar); /tasks shows a List grouped by Project then sub-grouped by Status; an unparented task appears under 'No project / Triage'; My Work shows the assigned vs delegated split; agent-run row pill opens the SessionView.

### `UI-screen-effort-detail` — Screen: Project / Program / Initiative detail (apps/web) _(P6 · parallel)_

- **depends on:** UI-08-detail-panel, UI-12-timeline-roadmap, UI-10-session-view
- **files:** `apps/web/src/app/(app)/orgs/[orgSlug]/projects/[projectId]/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/programs/[programId]/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/initiatives/[initiativeId]/page.tsx`, `apps/web/src/lib/effort-detail-data.ts`
- **spec:** mvp-plan.md §8.4, design-system.md §5.2, design-system.md §5.3
- **build:** Compose the three effort-detail shapes per mvp-plan §8.4 + design-system §5.2. Project detail: overview-first DetailPage with weighted-progress bar + HealthPill in DetailHeader; tasks grouped into Milestone sections (ListView with Milestone groupBy); Updates as their own Tab; comments + agent activity in PropertiesPanel; AgentsHereStrip. Program detail: NO % bar — headline = health + flow snapshot; work list grouped by Cycle, segmented by Project. Initiative detail: timeline-first — TimelineView rollup of its Projects (Programs as always-on lanes) + auto-derived rolled-up health + child distribution bar (no manual upkeep). All labels vocabulary-skinned. Data via typed RPC (GET /v1/orgs/:orgId/projects/:id etc.); burnup/health computed server-side per frozen api decisions.
- **accept:** `pnpm --filter @athena/web build` succeeds, no `any` from RPC. Manual/Playwright: a Project detail shows weighted progress + health + Milestone-sectioned tasks + Updates tab + AgentsHereStrip in properties; a Program detail shows NO progress bar (health+flow snapshot, Cycle-grouped); an Initiative detail leads with a timeline rollup and auto-derived health.

### `UI-screen-cycle-task-detail` — Screen: Cycle detail + Task detail (apps/web) _(P6 · parallel)_

- **depends on:** UI-08-detail-panel, UI-07-listview, UI-10-session-view
- **files:** `apps/web/src/app/(app)/orgs/[orgSlug]/cycles/[cycleId]/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/tasks/[taskId]/page.tsx`, `apps/web/src/lib/cycle-task-data.ts`
- **spec:** mvp-plan.md §8.5, design-system.md §5.2
- **build:** Compose Cycle detail + Task detail per mvp-plan §8.5 + design-system §5.2. Cycle detail: a ListView (tasks grouped by Project/Program) topped by a collapsible stats banner — burn-up line (planned done vs remaining) + capacity + scope changes + carryover (all computed server-side from audit events per frozen api decision); cycle close walks each leftover task with keep/move/return-to-Triage (carryover review before roll) wired to POST /v1/orgs/:orgId/cycles/:id/close. Task detail: DetailPage where the agent session streams inline in the comment+activity feed (SessionActivityStream embedded in PropertiesPanel feed); subtasks = inline checklist under description; dedicated dependency-visualization section (each dependency shows the OTHER task's project, since deps are cross-project); external links as PropertyRows. PATCH content vs POST /:taskId/move for reparenting per frozen api decision. All skinned.
- **accept:** `pnpm --filter @athena/web build` succeeds, no `any` from RPC. Manual/Playwright: a Cycle shows the collapsible burn-up stats banner and a close flow that lists each leftover with keep/move/triage; a Task detail shows an inline subtasks checklist, a cross-project dependency section naming each dep's project, and (when delegated) the live agent session streaming inline in the activity feed.

### `UI-screen-agents-sessions` — Screen: Agents — sessions-first feed + Session view (apps/web) _(P6 · parallel)_

- **depends on:** UI-10-session-view, UI-07-listview, UI-05-shell
- **files:** `apps/web/src/app/(app)/orgs/[orgSlug]/agents/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/sessions/[sessionId]/page.tsx`, `apps/web/src/lib/sessions-data.ts`, `apps/web/src/lib/session-stream.ts`
- **spec:** mvp-plan.md §8.6, design-system.md §5.5
- **build:** Compose the Agents area per mvp-plan §8.6 + design-system §5.5. agents/page.tsx = a live, filterable feed of Sessions (filters: running / awaiting-approval / done / errored) rendered as a ListView of session rows with SessionStatusPill; each opens the Session view. sessions/[sessionId]/page.tsx = the full SessionView (left activity stream, right changes/accountability/controls) wired to the live SSE stream via session-stream.ts (a hand-typed streamSession(orgId, sessionId, { since }) helper consuming GET /v1/orgs/:orgId/sessions/:id/stream EventSource per frozen api decision — NOT via hc<AppType>; honors Last-Event-ID resume). Approvals (POST /:sessionId/approvals/:activityId), elicitation replies, Pause/Take over/Cancel wired to RPC. Pending approvals also mirror to Inbox/Today (shared InlineApprovalCard body). Provider chip is minor.
- **accept:** `pnpm --filter @athena/web build` succeeds, no `any`. Manual/Playwright: /agents shows a filterable session feed; opening a running session streams live activity over SSE (new items appear without reload, aria-live announces them); a proposed action's 'Approve & send' applies it and reflects in 'Changes this session'; an elicitation reply submits; the same pending approval appears in the Hub Inbox.

### `UI-screen-settings` — Screen: Settings (per-Organization) — Members & Access, Integrations wizard, Vocabulary (apps/web) _(P6 · parallel)_

- **depends on:** UI-08-detail-panel, UI-05-shell, UI-03-vocabulary
- **files:** `apps/web/src/app/(app)/orgs/[orgSlug]/settings/layout.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/settings/members/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/settings/integrations/page.tsx`, `apps/web/src/app/(app)/orgs/[orgSlug]/settings/vocabulary/page.tsx`, `apps/web/src/lib/settings-data.ts`
- **spec:** mvp-plan.md §8.7, design-system.md §6
- **build:** Compose per-org Settings per mvp-plan §8.7. members/page.tsx = one 'Members & Access' area, people-primary: each person shown with a plain-language role (Owner/Admin/Member/Guest); Guests in the same list with a 'Guest' badge (see nothing until granted); invite flow -> POST /v1/orgs/:orgId/members/invite (rejected with 403 personal_org_no_invites on a personal org per frozen decision); detailed capability grid behind an 'advanced' disclosure for custom roles (v1 = API-creatable, minimal UI per frozen permissions scope-cut); access editable per-person AND from each resource (a Share affordance writes an actor-grant via PUT /v1/orgs/:orgId/grants with flat Capability[]). integrations/page.tsx = categorized directory; connecting a tool is a wizard that picks Migration vs Connector up front with consequences spelled out -> POST /v1/orgs/:orgId/integrations returning { integrationId, authorizeUrl } -> redirect to provider. Vocabulary skins remain an internal compatibility field; Settings exposes no vocabulary picker.
- **accept:** `pnpm --filter @athena/web build` succeeds, no `any`. Manual/Playwright: Members lists people with plain-language role chips and a Guest badge; inviting on a shared org succeeds and on a personal org returns 403; the Integrations wizard forces a Migration-vs-Connector choice and produces an authorizeUrl; Settings exposes no vocabulary picker.

### `UI-screen-landing-onboarding` — Screen: Landing (apps/marketing) + Sign-up + Onboarding intent/vocabulary/import (apps/web) _(P6 · parallel)_

- **depends on:** UI-01-shadcn-primitives, UI-03-vocabulary, UI-02-fonts-theme-density
- **files:** `apps/marketing/src/app/page.tsx`, `apps/marketing/src/app/layout.tsx`, `apps/marketing/src/app/globals.css`, `apps/web/src/app/(auth)/sign-in/page.tsx`, `apps/web/src/app/(onboarding)/intent/page.tsx`, `apps/web/src/app/(onboarding)/vocabulary/page.tsx`, `apps/web/src/app/(onboarding)/connect/page.tsx`, `apps/web/src/app/(onboarding)/meet-athena/page.tsx`
- **spec:** mvp-plan.md §8.8, design-system.md §2.1, design-system.md §1.2
- **build:** Build the top-priority entry funnel per mvp-plan §8.8 + design-system §2.1. apps/marketing consumes @docket/ui token layer + text-display scale (rsc:true per frozen design-system decision; only token layer + Button/Badge/display type, no app shell): a Linear-grade landing making the calm command-center promise. sign-in/page.tsx (client): passkey-first sign-up/in (authClient.signIn.passkey() + addPasskey({name,context}) with the POST /v1/auth/passkey-intent context per frozen build-sequence decision), Google/Linear/GitHub as secondary options doubling as data-source links; no card for Personal/solo (card only on shared org/invite). Onboarding: intent fork ('a startup / a nonprofit / just me'), focused workspace naming with standard Docket terminology, connect-a-tool import so the first screen shows real work, and an optional skippable Meet-Athena step. Hub/multi-org is revealed progressively.
- **accept:** `pnpm --filter @docket/marketing build` and `pnpm --filter @athena/web build` succeed. Manual/Playwright (CDP virtual authenticator): the marketing landing renders with shared tokens + text-display; passkey sign-up creates a user (no password), no card prompt for solo; onboarding presents the intent fork, creates from the name step without vocabulary selection, then offers connect/import and the skippable Meet Athena step.

### `UI-screen-service-admin` — Screen: Service Admin dashboard (apps/admin) _(P6 · parallel)_

- **depends on:** UI-07-listview, UI-08-detail-panel, UI-01-shadcn-primitives
- **files:** `apps/admin/src/app/layout.tsx`, `apps/admin/src/app/(admin)/page.tsx`, `apps/admin/src/app/(admin)/users/[userId]/page.tsx`, `apps/admin/src/app/(admin)/orgs/[orgId]/page.tsx`, `apps/admin/src/app/(admin)/lifecycle/page.tsx`, `apps/admin/src/lib/admin-rpc.ts`
- **spec:** mvp-plan.md §8.9, permissions.md §11
- **build:** Build the operator back-office per mvp-plan §8.9 (consumes the SAME RPC AppType /admin group). Home = split metrics + queues; user-primary navigation (a person spans many orgs; reach orgs from a user). User detail fans out to their orgs. Inline common billing actions (extend trial, credit, change plan, refund, pause dunning) writing back to Stripe via /v1/admin/\* (gated by staffGuard per-route allow-sets per frozen api decision: reads = {support,finance,superadmin}, billing-write/holds = {finance,superadmin}, audit/staff = {superadmin}). Data-lifecycle pipeline (trialing -> export_window -> pending_deletion -> deleted) with first-class holds (place hold stops the clock) and a full audit trail (OperatorAuditEvent). 'View as'/impersonation is banner-wrapped, time-boxed, reason-logged, fully audited (runs canActor as the impersonated Actor — no tenant super-grant). Agent oversight = health signals only (aggregate volume, errors, stuck approvals) — never session contents. Uses ListView for queues, DetailPage for user/org records.
- **accept:** `pnpm --filter @docket/admin build` succeeds, RPC AppType resolves no `any`. Manual/Playwright (staff session): a non-staff user is 403'd; a support user sees reads but billing-write is gated to finance/superadmin and audit to superadmin; the lifecycle pipeline shows each org's stage and a hold stops the clock; impersonation shows a warning banner, requires a reason, and writes an OperatorAuditEvent.

## testing-infra-bootstrap (18 tickets)

> **Superseded-by-shipped note:** see the note above `PAB-MCP-01` in the permissions-auth-billing
> section — the MCP-env-derivation tickets below (the bootstrap `deriveDomains` step) describe the
> pre-implementation plan; `OIDC_LOGIN_PAGE_URL` actually derives `${WEB_URL}/sign-in`, and the
> derivation runs at API-boot time (`packages/env/src/api.ts`), not as a one-time bootstrap write.

### `BOOT-01-var-registry` — Typed VAR*REGISTRY single-source-of-truth for env + bootstrap *(P1 · sequential)\_

- **files:** `packages/env/src/registry.ts`, `packages/env/src/registry.test.ts`
- **spec:** env-and-bootstrap.md §1.1-§1.8, env-and-bootstrap.md §3.10, frozen:env-and-bootstrap single-source-of-truth
- **build:** Create the single declarative VAR_REGISTRY array (frozen decision: env-and-bootstrap area, 'single-source-of-truth mechanism'). Export `VAR_REGISTRY: VarSpec[]` where `VarSpec = { name: string; slice: 'shared'|'db'|'auth'|'stripe'|'mcp'|'agent'|'ops'; scope: 'server'|'client'|'bootstrap'; targets: ('dev'|'prod'|'preview')[]; required: boolean | ((ctx: { billingEnabled: boolean; mcpTasksEnabled: boolean }) => boolean); zod: z.ZodTypeAny; where: string; generate?: () => string; sensitive: boolean }`. Enumerate EVERY variable from env-and-bootstrap §1.1-§1.8 PLUS the frozen-decision additions: MCP_RESOURCE_URL (rename of MCP_CANONICAL_URL), MCP_SESSION_STORE_URL (optional), MCP_TASKS_ENABLED (default false), MCP_CIMD_TRUSTED_HOSTS/MCP_CIMD_TRUST_ALLOWLIST + MCP_CIMD_STRICT (optional CSV / default true), BILLING_ENABLED (default false), BLOB_READ_WRITE_TOKEN (optional). `generate` set for BETTER_AUTH_SECRET (openssl rand -base64 32 equivalent via crypto.randomBytes(32).toString('base64')) and CRON_SECRET (randomBytes(32).toString('hex')). `where` strings reused verbatim by env:check and bootstrap prompts. This is the ONLY declaration site; do not duplicate var lists elsewhere.
- **accept:** `pnpm --filter @docket/env test` passes a unit test asserting: (a) every VAR_REGISTRY name is unique; (b) MCP_CANONICAL_URL and EXPORT_BUCKET_TOKEN-as-primary are absent (renamed/demoted); (c) MCP_RESOURCE_URL, MCP_SESSION_STORE_URL, MCP_TASKS_ENABLED, BILLING_ENABLED, BLOB_READ_WRITE_TOKEN present with correct scope/optionality; (d) generate() returns a >=32-char string for BETTER_AUTH_SECRET and a 64-char hex for CRON_SECRET. `pnpm --filter @docket/env typecheck` clean.

### `BOOT-02-env-slices-from-registry` — Build @docket/env createEnv slices + env:check from VAR*REGISTRY *(P1 · sequential)\_

- **depends on:** BOOT-01-var-registry
- **files:** `packages/env/src/shared.ts`, `packages/env/src/db.ts`, `packages/env/src/auth.ts`, `packages/env/src/stripe.ts`, `packages/env/src/mcp.ts`, `packages/env/src/agent.ts`, `packages/env/src/ops.ts`, `packages/env/src/env.api.ts`, `packages/env/src/env.web.ts`, `packages/env/src/env.marketing.ts`, `packages/env/src/env.admin.ts`, `packages/env/package.json`, `scripts/env-check.ts`
- **spec:** env-and-bootstrap.md §2.1-§2.5, frozen:env-and-bootstrap superRefine rules + BILLING_ENABLED + MCP env set
- **build:** Assemble per-slice `createEnv` objects (@t3-oss/env-core for slices + Hono api; @t3-oss/env-nextjs for web/marketing/admin) by reading VAR*REGISTRY (filter by slice), per env-and-bootstrap §2.1-§2.3. Implement the frozen cross-field superRefine rules in env.api.ts composition: (1) ATHENA_AGENT_ENDPOINT<->ATHENA_AGENT_API_KEY all-or-nothing pair; (2) BILLING_ENABLED=true requires STRIPE_SECRET_KEY + (DOCKET_PRICE_LOOKUP_TEAM OR STRIPE_PRICE_TEAM); (3) STRIPE_PRICE_TEAM present => lookup optional; (4) MCP_TASKS_ENABLED=true requires MCP_SESSION_STORE_URL; (5) EXPORT_BUCKET_URL present => EXPORT_BUCKET_TOKEN required else BLOB_READ_WRITE_TOKEN path. NEXT_PUBLIC*\* referenced literally in runtimeEnv. Add `scripts/env-check.ts` wired as root `pnpm env:check` that loads each app target and runs its createEnv, printing the first failing var with its registry `where` hint and exiting non-zero on error / warning (not error) for optional agent/billing-off vars.
- **accept:** `pnpm env:check` with a complete dev env exits 0; with DATABASE_URL removed it exits non-zero and prints the DATABASE_URL `where` hint string. `pnpm env:check` with MCP_TASKS_ENABLED=true and MCP_SESSION_STORE_URL unset exits non-zero. With ATHENA_AGENT_ENDPOINT set but ATHENA_AGENT_API_KEY unset exits non-zero. `pnpm --filter @docket/env typecheck` clean.

### `BOOT-03-preflight` — Bootstrap Step 0 — CLI preflight (neonctl/stripe/vercel detect + auth) _(P2 · parallel)_

- **depends on:** BOOT-01-var-registry
- **files:** `scripts/bootstrap/preflight.ts`
- **spec:** env-and-bootstrap.md §3.1, env-and-bootstrap.md §3.0
- **build:** Implement the bootstrap preflight module per env-and-bootstrap §3.1: for each of neonctl, stripe, vercel detect presence via `<cli> --version` (execa), and on missing print the exact install command and offer to continue with that provider skipped. Detect auth state: Neon via `neonctl projects list --output json` (else `neonctl auth` or prompt NEON_API_KEY); Stripe via `stripe config --list` (else `stripe login`); Vercel via `vercel whoami` (else `vercel login` or prompt the operator for a bootstrap-only VERCEL_TOKEN). Export `preflight(): Promise<{ neon: 'ready'|'skipped'; stripe: ...; vercel: ... }>`. Use @clack/prompts for TTY, execa for CLI calls. No real network calls in unit scope — accept an injectable command runner so it is testable. Production CI uses Vercel's native Git integration and never consumes this credential.
- **accept:** `pnpm --filter ./ exec tsx scripts/bootstrap/preflight.ts --dry-run` (or a colocated vitest) runs against a stubbed command runner: returns 'skipped' when `<cli> --version` throws and 'ready' when version + auth probes succeed; never throws on a missing CLI. `pnpm typecheck` clean for the file.

### `BOOT-04-domains-secrets` — Bootstrap Steps 1-3 — target/domains/secrets derivation _(P2 · parallel)_

- **depends on:** BOOT-01-var-registry
- **files:** `scripts/bootstrap/domains.ts`, `scripts/bootstrap/secrets.ts`, `scripts/bootstrap/domains.test.ts`
- **spec:** env-and-bootstrap.md §3.2, env-and-bootstrap.md §0, frozen:env-and-bootstrap MCP_RESOURCE_URL derivation
- **build:** Implement target multiselect (dev and/or prod, dev default) and domain confirm/override using the §0 domain table defaults. From the API origin DERIVE: BETTER_AUTH_URL=API_URL, MCP_ISSUER_URL=API_URL, MCP_RESOURCE_URL=`${API_URL}/mcp`, OIDC_LOGIN_PAGE_URL=`${NEXT_PUBLIC_WEB_URL}/oauth/consent`, BETTER_AUTH_TRUSTED_ORIGINS and MCP_ALLOWED_ORIGINS CSVs from web/marketing/admin origins, BETTER_AUTH_PASSKEY_RP_ID=localhost (dev) or apex of prod web origin. Secrets: generate BETTER_AUTH_SECRET (crypto.randomBytes(32).toString('base64')) and CRON_SECRET (randomBytes(32).toString('hex')) via the registry `generate()` fns, DIFFERENT per target, generated once and never regenerated on re-run unless explicitly asked (warn that regenerating BETTER_AUTH_SECRET invalidates sessions). Export `deriveDomains(input)` and `ensureSecrets(target, existing)` as pure-ish functions.
- **accept:** `pnpm --filter ./ test scripts/bootstrap/domains.test.ts` asserts: deriveDomains('http://localhost:8787', webUrl, mktUrl, admUrl) yields MCP_RESOURCE_URL ending in '/mcp', MCP_ISSUER_URL===API_URL, RP_ID==='localhost' for localhost web, and the apex (e.g. 'docket.app') for 'https://app.docket.app'; ensureSecrets returns existing secrets unchanged on re-run. `pnpm typecheck` clean.

### `BOOT-05-neon` — Bootstrap Step 4 — Neon provisioning + migrate over unpooled URL _(P2 · parallel)_

- **depends on:** BOOT-03-preflight
- **files:** `scripts/bootstrap/neon.ts`
- **spec:** env-and-bootstrap.md §3.3, frozen:build-sequence DATABASE_URL_UNPOOLED required for migrate
- **build:** Implement Neon provisioning per env-and-bootstrap §3.3: reuse-or-create `docket` project (`neonctl projects list --output json`; create with `neonctl projects create --name docket --region-id <prompted> --output json --set-context`); ensure `production` + `dev` branches; capture pooled DATABASE_URL (`neonctl connection-string <branch> --pooled --output json`) and unpooled DATABASE_URL_UNPOOLED (`neonctl connection-string <branch> --output json`); dev target -> dev branch, prod -> production branch. Run migrations over the UNPOOLED string only: export DATABASE_URL_UNPOOLED then `pnpm --filter @docket/db db:generate` (if needed) and `pnpm --filter @docket/db db:migrate`. List-before-create idempotency. Inject the command runner for testability; do not seed tenant data.
- **accept:** `pnpm --filter ./ exec tsx -e "import {provisionNeon} from './scripts/bootstrap/neon'"` resolves (import smoke). Colocated vitest with a stubbed runner asserts: reuses an existing `docket` project rather than creating; migrate command is invoked with DATABASE_URL_UNPOOLED set and never with the pooled URL. `pnpm typecheck` clean.

### `BOOT-06-oauth-stripe-athena` — Bootstrap Steps 5-7 — OAuth prompts, Stripe products/prices/webhooks, Athena _(P2 · parallel)_

- **depends on:** BOOT-03-preflight, BOOT-02-env-slices-from-registry
- **files:** `scripts/bootstrap/oauth.ts`, `scripts/bootstrap/stripe.ts`, `scripts/bootstrap/athena.ts`
- **spec:** env-and-bootstrap.md §3.4-§3.6, frozen:env-and-bootstrap Stripe lookup-key resolution + eight webhook events + createCustomerOnSignUp=false
- **build:** OAuth (§3.4): print exact dev+prod redirect URIs from env-and-bootstrap §1.3 for Google/GitHub/Linear and collect id/secret via masked prompts, validating each against the authEnv slice. Stripe (§3.5): list-before-create product `Docket Team`; create prices with stable lookup keys team*monthly (-> DOCKET_PRICE_LOOKUP_TEAM, capture price* id -> STRIPE_PRICE_TEAM fallback) and optional team_annual; per frozen decision resolve concrete priceId at boot via lookup unless STRIPE_PRICE_TEAM override present. Webhooks: DEV prints `stripe listen --forward-to localhost:8787/api/auth/stripe/webhook` + capture via `stripe listen --print-secret`; PROD `stripe webhook_endpoints create --url ${API_URL}/api/auth/stripe/webhook --enabled-events checkout.session.completed,customer.subscription.created,customer.subscription.updated,customer.subscription.deleted,invoice.payment_failed,invoice.paid,invoice.payment_action_required,customer.subscription.trial_will_end --output json` capturing the signing secret. Set createCustomerOnSignUp=false implication note. Athena (§3.6): prompt ATHENA_AGENT_ENDPOINT + ATHENA_AGENT_API_KEY (+ optional ANTHROPIC_API_KEY), skippable with a warning that sessions return 503 until set.
- **accept:** Colocated vitest with a stubbed Stripe/prompt runner asserts: the prod webhook create call includes exactly the eight enabled-events; Stripe step lists products before creating; OAuth step rejects a pasted GOOGLE_CLIENT_ID of empty string (authEnv min(1)); Athena skip path returns optional vars unset without throwing. `pnpm typecheck` clean.

### `BOOT-07-write-env-vercel` — Bootstrap Steps 8-9 — per-app .env writing + Vercel env push + Neon preview branching _(P2 · parallel)_

- **depends on:** BOOT-04-domains-secrets, BOOT-02-env-slices-from-registry
- **files:** `scripts/bootstrap/write-dev-env.ts`, `scripts/bootstrap/configure-vercel.ts`, `scripts/bootstrap/neon-preview.ts`, `.gitignore`
- **spec:** env-and-bootstrap.md §3.7-§3.8, frozen:env-and-bootstrap Neon per-PR branch strategy, frozen:env-and-bootstrap BLOB_READ_WRITE_TOKEN
- **build:** Step 8 (§3.7): assemble validated values and write per-app .env files atomically (write .env.tmp then rename) using the VAR*REGISTRY scope/slice to pick each app's subset: apps/api/.env (all server slices + API_URL/PORT/NODE_ENV=development), apps/web/.env (NEXT_PUBLIC*_), apps/marketing/.env, apps/admin/.env. Never overwrite existing .env without diff+confirm. Emit/update redacted apps/_/.env.example. Step 9 (§3.8): for each Vercel project (api/web/marketing/admin) `vercel link --project docket-<app> --yes`, then for each var pipe value via stdin `printf '%s' <value> | vercel env add <NAME> <target> --force` with --sensitive for _\_SECRET/**KEY/DATABASE_URL\* and --no-sensitive (production+preview) for NEXT_PUBLIC**; pull-back diff via `vercel env pull`. Neon preview branching (frozen): wire the Neon-Vercel native integration / `neonctl branches create preview/pr-<n>` so preview deploys get a branched DATABASE_URL/DATABASE_URL_UNPOOLED (same var names). Ensure .gitignore covers apps/_/.env, .env.vercel._, playwright/.auth/_.
- **accept:** Colocated vitest asserts: write-dev-env never writes a server secret into apps/web/.env (scope filtering); writes are atomic (tmp+rename observed via stubbed fs); configure-vercel marks DATABASE_URL --sensitive and NEXT_PUBLIC_API_URL --no-sensitive on both production and preview. `git check-ignore apps/api/.env playwright/.auth/x` returns those paths. `pnpm typecheck` clean.

### `BOOT-08-bootstrap-orchestrator` — Bootstrap orchestrator + verify/summary (scripts/bootstrap.ts) + root pnpm bootstrap _(P3 · sequential)_

- **depends on:** BOOT-03-preflight, BOOT-04-domains-secrets, BOOT-05-neon, BOOT-06-oauth-stripe-athena, BOOT-07-write-env-vercel
- **files:** `scripts/bootstrap.ts`, `package.json`
- **spec:** env-and-bootstrap.md §3.0, env-and-bootstrap.md §3.9-§3.10, build-sequence-and-slice.md §2.6, frozen:build-sequence one-script-subset bootstrap
- **build:** Compose the §3.0 flow end-to-end in scripts/bootstrap.ts importing the step modules: preflight -> target/domains/secrets -> neon -> oauth/stripe/athena -> write dev env -> configure vercel -> verify -> summary. Step 10 verify (§3.9): re-run `pnpm env:check` per app target; smoke checks (SELECT 1 over DATABASE_URL, `stripe prices retrieve`/list by lookup key, fetch `${MCP_ISSUER_URL}/.well-known/oauth-authorization-server` and `${MCP_RESOURCE_URL}` PRM once API up) — non-fatal where the service is not yet running, fatal on env validation failure (exit non-zero). Step 11 summary: print created Neon/Stripe ids, exact OAuth redirect URIs, Vercel keys set per target, next commands (pnpm dev, stripe listen, db:studio). Add root package.json script `"bootstrap": "tsx scripts/bootstrap.ts"`. Idempotent + masked secrets + never commit .env (§3.10).
- **accept:** `pnpm bootstrap --help` (or `tsx scripts/bootstrap.ts --help`) prints the 11-step flow and exits 0. A dry-run/`--target dev --non-interactive` against stubbed runners completes the full ordered flow and calls env:check at the verify step. `pnpm typecheck` clean; no `// TODO`, no stub `throw new Error('Not implemented')` in any bootstrap file.

### `E2E-01-package-skeleton` — apps/web-e2e package skeleton + playwright.config.ts (two capture modes) _(P2 · sequential)_

- **files:** `apps/web-e2e/package.json`, `apps/web-e2e/playwright.config.ts`, `apps/web-e2e/tsconfig.json`
- **spec:** build-sequence-and-slice.md §2.7, engineering plan §6 'Two capture modes', frozen:build-sequence two-mode capture/sharding posture
- **build:** Create the dedicated apps/web-e2e workspace member (engineering plan §6) pinned to Playwright >=1.60, Chromium-only by default. playwright.config.ts implements the two capture modes from the SAME source via the CAPTURE_ALL env flag: lean PR mode (trace 'on-first-retry', screenshot 'only-on-failure', video 'retain-on-failure') vs CAPTURE_ALL=1 film mode (trace/screenshot/video all 'on', viewport 1280x800, deviceScaleFactor 2 retina, reducedMotion 'reduce'). Configure projects: a `setup` auth project (BOOT into via E2E-03 dependency placeholder), then a `chromium` test project that `dependsOn: ['setup']` and uses storageState. webServer config to start apps/api + apps/web for local runs. reporter: list locally, 'blob' under CI. Add to pnpm-workspace via apps/\* glob (already covered).
- **accept:** `pnpm --filter @docket/web-e2e exec playwright --version` prints >=1.60. `CAPTURE_ALL=1 pnpm --filter @docket/web-e2e exec playwright test --list` resolves config without error and shows film-mode options (viewport 1280x800) when inspected; default `pnpm --filter @docket/web-e2e exec playwright test --list` resolves lean mode. `pnpm --filter @docket/web-e2e typecheck` clean.

### `E2E-02-virtual-authenticator-fixture` — CDP virtual authenticator + flow-recorder fixtures _(P3 · parallel)_

- **depends on:** E2E-01-package-skeleton
- **files:** `apps/web-e2e/fixtures/virtual-authenticator.ts`, `apps/web-e2e/fixtures/flow-recorder.ts`, `apps/web-e2e/fixtures/index.ts`
- **spec:** build-sequence-and-slice.md §2.7, engineering plan §6 'CDP virtual authenticator', frozen:build-sequence CDP virtual authenticator only, no session-seed
- **build:** virtual-authenticator.ts: a Playwright fixture that opens a CDP session and sends WebAuthn.enable then WebAuthn.addVirtualAuthenticator with EXACTLY { protocol: 'ctap2', transport: 'internal', hasResidentKey: true, hasUserVerification: true, isUserVerified: true, automaticPresenceSimulation: true } (frozen decision, no session-seed fallback exists or is built). flow-recorder.ts: a `step(name, fn)` fixture that writes a named screenshot per step and attaches the video to the HTML report so each flow self-documents with chapter markers (engineering plan §6 'flow-recorder fixture'). index.ts: merge both into the typed test fixtures via Playwright `test.extend`.
- **accept:** `pnpm --filter @docket/web-e2e typecheck` clean. A smoke spec using the fixture (or the existing slice spec from E2E-05) runs the WebAuthn.addVirtualAuthenticator call with the exact six options (assert via a unit/snapshot of the options object). grep confirms NO session-seed/bypass endpoint helper exists anywhere under apps/web-e2e.

### `E2E-03-auth-setup-personas` — Auth setup project: per-persona storageState (owner/member/guest) _(P3 · parallel)_

- **depends on:** E2E-02-virtual-authenticator-fixture
- **files:** `apps/web-e2e/setup/auth.setup.ts`, `apps/web-e2e/playwright.config.ts`
- **spec:** engineering plan §6 'per-persona storageState and per-worker tenants', build-sequence-and-slice.md §2.7
- **build:** Implement the setup project (engineering plan §6 'per-persona storageState') that logs in each persona — owner, member, guest — once via the real passkey path (CDP virtual authenticator) and persists each authenticated context to a storageState file under playwright/.auth/<persona>.json (gitignored). Wire the config `setup` project to run these and downstream chromium project to select storageState by persona. Owner persona registers + creates its org; member/guest are seeded into that org via @docket/test-utils helpers (depends_on edge to TEST-01 for invite/seed helpers is satisfied at runtime, not import-blocking here — use direct DB-read/seed helpers).
- **accept:** `pnpm --filter @docket/web-e2e exec playwright test --project=setup` produces playwright/.auth/owner.json, member.json, guest.json (when run against a live api+db via webServer). `pnpm --filter @docket/web-e2e typecheck` clean. `git check-ignore playwright/.auth/owner.json` returns the path.

### `TEST-01-test-utils` — @docket/test-utils — per-worker tenant, DB-read, session/seed helpers _(P2 · parallel)_

- **files:** `packages/test-utils/src/tenant.ts`, `packages/test-utils/src/db-read.ts`, `packages/test-utils/src/seed.ts`, `packages/test-utils/src/index.ts`, `packages/test-utils/package.json`
- **spec:** build-sequence-and-slice.md §2.1 (@docket/test-utils), build-sequence-and-slice.md §2.7, engineering plan §6 'per-worker tenant helper and DB-read helper'
- **build:** Build @docket/test-utils (build-sequence §2.7 / engineering plan §6): (1) tenant.ts — per-worker tenant helper that creates a fresh Organization per Playwright worker (keyed by testInfo.workerIndex) so parallel shards don't collide; (2) db-read.ts — direct Postgres read helper (over DATABASE_URL) used by real-stack assertions to confirm task.organization_id and task.team_id without going through the UI; (3) seed.ts — helpers to seed member/guest actors into a tenant and to create cross-org fixtures. No session-seed-bypass helper (frozen: passkey via CDP only). Consumed JIT by apps/web-e2e.
- **accept:** `pnpm --filter @docket/test-utils typecheck` clean and `pnpm --filter @docket/test-utils build` (if compiled) succeeds. A unit test asserts the per-worker tenant helper derives a unique org name/slug per workerIndex. grep confirms no exported `mintSession`/`seedSession`-style WebAuthn bypass.

### `E2E-04-flow-registry-coverage` — Typed flow registry + coverage spec (CI-failing gate) _(P3 · sequential)_

- **depends on:** E2E-01-package-skeleton
- **files:** `apps/web-e2e/flow-registry.ts`, `apps/web-e2e/coverage.spec.ts`
- **spec:** engineering plan §6 'typed flow registry' + 'coverage spec', build-sequence-and-slice.md §2.7
- **build:** flow-registry.ts: a TYPED registry naming the required flows as real symbols (engineering plan §6): signup, onboarding, create-org, connect-integration, plan-project, delegate-task-approve, cross-org-today, invite-guest. Export `FLOW_IDS` as a const tuple + `FlowId` type + a `registerFlow(id)` mechanism specs call to declare coverage. coverage.spec.ts: the enforcement gate that scans apps/web-e2e/flows for a spec registering each declared FlowId and FAILS the test (red CI) when a declared flow has no corresponding registered spec. The slice flow registers under id `create-org` (extended to project+task).
- **accept:** `pnpm --filter @docket/web-e2e exec playwright test coverage.spec.ts` FAILS while any declared flow lacks a registered spec, and PASSES once all flows are registered (verify by temporarily removing a registration -> red). `pnpm --filter @docket/web-e2e typecheck` clean; FlowId is a literal union, not string.

### `E2E-05-slice-flow-spec` — create-org-project-task slice flow spec (real-stack assertions) _(P4 · parallel)_

- **depends on:** E2E-02-virtual-authenticator-fixture, E2E-03-auth-setup-personas, TEST-01-test-utils, E2E-04-flow-registry-coverage
- **files:** `apps/web-e2e/flows/create-org-project-task.spec.ts`
- **spec:** build-sequence-and-slice.md §2.5-§2.8, engineering plan §6, frozen:build-sequence task default state = workflow_states[0].key (backlog)
- **build:** The verifying slice flow (build-sequence §2.7), registered under flow id `create-org`. Steps wrapped in flow-recorder step(): (1) sign up with passkey (CDP virtual authenticator) -> assert redirect into app shell + session cookie; (2) create organization 'Acme' -> assert avatar in global rail + GET /organizations returns it + default Team exists; (3) create project 'Paid Launch'; (4) create task 'Draft launch post'; (5) see grouped list -> assert task under group 'Paid Launch' -> sub-group 'Backlog', and a second project-less task lands under a 'No project / Triage' group. Real-stack assertions via @docket/test-utils: direct DB read confirms task.organization_id==created org and task.team_id==default team; a GET /tasks?organizationId=<other-tenant> with this session returns no rows / 403 (Phase-4.5 isolation). storageState persisted after step 1; per-worker tenant.
- **accept:** `pnpm --filter @docket/web-e2e exec playwright test flows/create-org-project-task.spec.ts` is GREEN in lean mode against live api+db; `CAPTURE_ALL=1 ...` produces a 1280x800 reduced-motion film with per-step screenshots. coverage.spec.ts passes (create-org registered). `pnpm --filter @docket/web-e2e typecheck` clean; no it.skip, no stub.

### `E2E-06-additional-flow-specs` — Remaining declared-flow specs (signup, onboarding, plan-project, delegate-task-approve, cross-org-today, invite-guest, connect-integration) _(P6 · parallel)_

- **depends on:** E2E-02-virtual-authenticator-fixture, E2E-03-auth-setup-personas, TEST-01-test-utils, E2E-04-flow-registry-coverage
- **files:** `apps/web-e2e/flows/signup.spec.ts`, `apps/web-e2e/flows/onboarding.spec.ts`, `apps/web-e2e/flows/plan-project.spec.ts`, `apps/web-e2e/flows/delegate-task-approve.spec.ts`, `apps/web-e2e/flows/cross-org-today.spec.ts`, `apps/web-e2e/flows/invite-guest.spec.ts`, `apps/web-e2e/flows/connect-integration.spec.ts`
- **spec:** engineering plan §6 'flow registry' + 'mocking external OAuth', build-sequence-and-slice.md §2.7
- **build:** One spec per remaining declared FlowId, each registering its FlowId in the registry and using flow-recorder step() boundaries. signup/onboarding: real passkey via CDP. plan-project: project+initiative/cycle planning surface. delegate-task-approve: assign to agent -> proposed session_activity -> approver approves (permissions §9 approver-eligibility). cross-org-today: Hub /today aggregation across the caller's active Actors with per-item canActor filtering (no cross-tenant join; assert a guest cannot see hidden work). invite-guest: hand-built invitation flow (member/invite) then guest persona accepts. connect-integration: MUST mock/sandbox external OAuth (Google/GitHub/Linear) — assert Docket's callback handling/account-linking/token storage deterministically, never hitting live providers (engineering plan §6). Each spec is a real-stack test using per-worker tenant + persona storageState; mark long-running/agent and billing-gated paths behind their feature flags where applicable.
- **accept:** `pnpm --filter @docket/web-e2e exec playwright test flows/` runs all flow specs green against live api+db (connect-integration uses the mocked OAuth double, not live providers — assert no network call to accounts.google.com/github.com/linear.app). coverage.spec.ts PASSES (every declared FlowId now registered). `pnpm --filter @docket/web-e2e typecheck` clean; no it.skip.

### `CI-01-pr-e2e-workflow` — GitHub Actions PR workflow — typecheck/lint/build + sharded e2e (lean) with blob merge _(P5 · parallel)_

- **depends on:** E2E-04-flow-registry-coverage, E2E-05-slice-flow-spec
- **files:** `.github/workflows/ci.yml`, `.github/workflows/e2e.yml`
- **spec:** engineering plan §6 'CI: sharding, blob merge', engineering plan §6 'Two capture modes' (lean PR), frozen:env-and-bootstrap Neon preview branch migrate-before-e2e
- **build:** Replace/author the PR CI for the @docket/_ tree. ci.yml: pnpm install, `pnpm typecheck`, `pnpm lint`, `pnpm build` (turbo), `pnpm env:check` with SKIP_ENV_VALIDATION for the secret-less image-build matrix where appropriate. e2e.yml (PR-triggered, lean mode): provision a Neon preview branch (frozen Neon-Vercel/neonctl strategy) and run `pnpm --filter @docket/db db:migrate` over DATABASE_URL_UNPOOLED before the suite; run Playwright SHARDED across runners emitting the `blob` reporter from each shard; a merge job combines blobs into one unified HTML report (engineering plan §6 'sharding, blob merge'); upload videos/traces as artifacts; ensure playwright/.auth/_ never committed. Run the coverage gate as part of the suite. Inject operator CLI creds (NEON_API_KEY, etc.) from GitHub secrets, never app .env.
- **accept:** `act` or a pushed PR runs ci.yml green (typecheck/lint/build/env:check) and e2e.yml green: shards emit blob reports, the merge job produces a single HTML report artifact, coverage gate passes. YAML validates (`actionlint .github/workflows/*.yml` clean). Workflow uses the lean capture mode (no forced video on pass).

### `CI-02-film-workflow` — Scheduled e2e-flow-films workflow (CAPTURE*ALL film run) *(P5 · parallel)\_

- **depends on:** E2E-05-slice-flow-spec, E2E-06-additional-flow-specs
- **files:** `.github/workflows/e2e-flow-films.yml`
- **spec:** engineering plan §6 'the film run' + 'e2e-flow-films', frozen:design-system reduced-motion film runs, frozen:build-sequence CAPTURE_ALL=1 scheduled film posture
- **build:** Author the scheduled `e2e-flow-films` GitHub workflow (engineering plan §6 'the film run') — NOT on PR. Runs on a cron schedule (and workflow_dispatch) with CAPTURE_ALL=1 so video/screenshot/trace are all forced ON, rendering at 1280x800 retina, reduced motion, against seeded data, producing marketing-grade reproducible films of every declared flow. Uploads the rendered films + per-step screenshots + merged HTML report as artifacts. Films must never gate a PR (separate schedule, frozen two-mode posture).
- **accept:** `actionlint .github/workflows/e2e-flow-films.yml` clean; the workflow declares a `schedule:` cron trigger and sets CAPTURE_ALL=1 in the e2e job env. A manual `workflow_dispatch` run produces film video artifacts at 1280x800. The PR pipeline (CI-01) does NOT reference this workflow (no PR coupling).

### `CI-03-lifecycle-cron` — Vercel Cron config for the CRON*SECRET-guarded lifecycle sweep *(P6 · parallel)\_

- **depends on:** BOOT-02-env-slices-from-registry
- **files:** `apps/api/vercel.json`, `scripts/bootstrap/cron.ts`
- **spec:** engineering plan §3 'idempotent cron sweep', env-and-bootstrap.md §1.7 CRON_SECRET, frozen:build-sequence cron infra = Vercel Cron
- **build:** Per frozen decision (cron infra = Vercel Cron): add a `crons` entry to apps/api/vercel.json invoking the lifecycle sweep on a daily schedule (e.g. `0 3 * * *`) at the route the billing lane owns (GET /api/lifecycle/sweep), guarded by `Authorization: Bearer ${CRON_SECRET}`. This ticket owns ONLY the cron wiring + the bootstrap step that ensures CRON_SECRET is generated (openssl rand -hex 32 via registry generate()) and written to Vercel; the sweep HANDLER itself is the Billing lane's responsibility (out of scope — do not implement the sweep logic). scripts/bootstrap/cron.ts ensures the schedule is registered/idempotent and warns when MCP_SESSION_STORE_URL/Fluid-Compute notes apply (informational only).
- **accept:** apps/api/vercel.json validates as JSON and contains a `crons` array with a daily schedule pointing at the sweep path. `pnpm typecheck` clean for cron.ts. A unit test asserts the cron entry path matches `/api/lifecycle/sweep` and that bootstrap writes CRON_SECRET to the api target. No sweep business logic present in these files (boundary respected).
