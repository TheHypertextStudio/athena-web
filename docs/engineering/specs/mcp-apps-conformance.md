<!-- GENERATED FILE. Regenerate with:
     pnpm --filter @docket/integrations exec tsx tests/mcp/emit-conformance-matrix.ts -->

# MCP Apps stable normative conformance manifest

**Extension:** `io.modelcontextprotocol/ui`
**Version:** `2026-01-26`
**Source:** https://apps.extensions.modelcontextprotocol.io/api/
**Spec copy:** `docs/engineering/specs/vendor/mcp-apps-2026-01-26.mdx`
**Spec SHA-256:** `ee452a7d1b9b7fb900acfeb4d6932d3963375b0f3f37d196a4b93eb80312af0e`
**Retrieved:** 2026-08-02

This generated manifest accounts for all 81 uppercase RFC 2119 occurrences
in the committed stable prose. `MAY` features are recorded but unsupported optional behavior
remains unadvertised. Applicable rows name production code and a behavioral test; conditional
or unsupported rows retain a concrete reason and omission evidence.

## Host requirements

| ID | Level | Source | Applicability | Implementation | Behavioral test | Reason |
| --- | --- | --- | --- | --- | --- | --- |
| `HOST-001` | **MAY** | UI Resource Format L170 `b2c8b9d1406c8e83`<br>* Hosts MAY honor these by setting appropriate iframe `allow` attributes. | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewPermissionsAllow` | `mcp-apps-host.test.ts :: grants only the permissions the resource asked for` |  |
| `HOST-002` | **MUST** | Host Behavior: L274 `9fa81c3e52c742c3`<br>- **CSP Enforcement:** Host MUST construct CSP headers based on declared domains | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewCsp` | `mcp-apps-host.test.ts :: adds exactly the origins the resource declared and nothing else` |  |
| `HOST-003` | **MUST** | Host Behavior: L275 `fc191c471fc083fb`<br>- **Restrictive Default:** If `ui.csp` is omitted, Host MUST use: | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewCsp` | `mcp-apps-host.test.ts :: adds exactly the origins the resource declared and nothing else` |  |
| `HOST-004` | **MAY** | Host Behavior: L286 `206c1711ff883b57`<br>- **No Loosening:** Host MAY further restrict but MUST NOT allow undeclared domains | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewCsp` | `mcp-apps-host.test.ts :: adds exactly the origins the resource declared and nothing else` |  |
| `HOST-005` | **MUST NOT** | Host Behavior: L286 `8d8e265b528fc512`<br>- **No Loosening:** Host MAY further restrict but MUST NOT allow undeclared domains | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewCsp` | `mcp-apps-host.test.ts :: adds exactly the origins the resource declared and nothing else` |  |
| `HOST-006` | **SHOULD** | Host Behavior: L287 `152d4e206d0c7a77`<br>- **Audit Trail:** Host SHOULD log CSP configurations for security review | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | CSP values are enforced and covered without retaining untrusted provider policy in application logs. |
| `HOST-007` | **MUST** | Behavior: L391 `4f41cb6c146ea11f`<br>- Host MUST use `resources/read` to fetch the referenced resource URI | applicable | `packages/integrations/src/mcp-connector.ts :: captureMcpAppPresentation` | `mcp-apps-real-connector.test.ts :: snapshots valid base64 widget HTML beside the one raw tool result` |  |
| `HOST-008` | **MAY** | Behavior: L392 `fb67f10142c8bfb5`<br>- Host MAY prefetch and cache UI resource content for performance optimization | applicable | `packages/integrations/src/mcp-connector.ts :: captureMcpAppPresentation` | `mcp-apps-real-connector.test.ts :: snapshots valid base64 widget HTML beside the one raw tool result` |  |
| `HOST-009` | **MUST NOT** | Visibility: L400 `6ef0259f9ae24490`<br>- **tools/list behavior:** Host MUST NOT include tools in the agent's tool list when their visibility does not include `"model"` (e.g., `visibility: ["app"]`) | applicable | `apps/api/src/agents/toolbox.ts :: model-visible remote tool catalog` | `apps/api/tests/agent/toolbox.test.ts :: surfaces absent, model-only, and dual-visible tools while keeping app-only helpers out of the model catalog` |  |
| `HOST-010` | **MUST** | Visibility: L401 `8e47217157f2409d`<br>- **tools/call behavior:** Host MUST reject `tools/call` requests from apps for tools that don't include `"app"` in visibility | applicable | `apps/api/src/mcp/apps/host.ts :: runWidgetTool` | `apps/api/tests/mcp/mcp-apps-host-routes.test.ts :: refuses a model-only tool when a view asks for it, though the model may call it` |  |
| `HOST-011` | **MUST** | Sandbox proxy L472 `cf5d809524b6c3b9`<br>If the Host is a web page, it MUST wrap the View and communicate with it through an intermediate Sandbox proxy. | applicable | `apps/web/src/components/athena/mcp-app-view.tsx :: McpAppView` | `apps/web/tests/athena/mcp-app-view.test.tsx :: gives the proxy an origin and the widget none` |  |
| `HOST-012` | **MUST** | Sandbox proxy L474 `f976b1b6db870055`<br>1. The Host and the Sandbox MUST have different origins. | applicable | `apps/web/src/components/athena/mcp-app-view.tsx :: McpAppView` | `apps/web/tests/athena/mcp-app-view.test.tsx :: gives the proxy an origin and the widget none` |  |
| `HOST-013` | **MUST** | Sandbox proxy L477 `915e00d6144c2577`<br>4. Once the Sandbox is ready, the Host MUST send the raw HTML resource to load in a `ui/notifications/sandbox-resource-ready` notification. | applicable | `apps/web/src/components/athena/mcp-app-view.tsx :: McpAppView` | `apps/web/tests/athena/mcp-app-view.test.tsx :: gives the proxy an origin and the widget none` |  |
| `HOST-014` | **MUST NOT** | Sandbox proxy L485 `543f3f9599e303b6`<br>6. The Sandbox MUST forward messages sent by the Host to the View, and vice versa, for any method that doesn't start with `ui/notifications/sandbox-`. This includes lifecycle messages, e.g., `ui/initialize` request & `ui/notifications/initialized` notification both sent by the View. The Host MUST NOT send any request or notification to the View before it receives an `initialized` notification. | applicable | `apps/web/src/components/athena/mcp-app-view.tsx :: McpAppView` | `apps/web/tests/athena/mcp-app-view.test.tsx :: gives the proxy an origin and the widget none` |  |
| `HOST-015` | **MAY** | Sandbox proxy L487 `7acc73cb2cb34705`<br>8. The Host MAY forward any message from the View (coming via the Sandbox) to the MCP Apps server, for any method that doesn't start with `ui/`. While the Host SHOULD ensure the View's MCP connection is spec-compliant, it MAY decide to block some messages or subject them to further user approval. | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | Athena forwards only explicitly advertised and authorized stable server methods, not the generic optional channel. |
| `HOST-016` | **SHOULD** | Sandbox proxy L487 `101c0d5c3a4201f0`<br>8. The Host MAY forward any message from the View (coming via the Sandbox) to the MCP Apps server, for any method that doesn't start with `ui/`. While the Host SHOULD ensure the View's MCP connection is spec-compliant, it MAY decide to block some messages or subject them to further user approval. | applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` |  |
| `HOST-017` | **MAY** | Sandbox proxy L487 `729855eafca085f7`<br>8. The Host MAY forward any message from the View (coming via the Sandbox) to the MCP Apps server, for any method that doesn't start with `ui/`. While the Host SHOULD ensure the View's MCP connection is spec-compliant, it MAY decide to block some messages or subject them to further user approval. | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | Athena has no additional approval channel for generic forwarded messages because that optional channel is absent. |
| `HOST-018` | **SHOULD** | Host Context in `McpUiInitializeResult` L533 `d90c088450cbe3cc`<br>When the View sends an `ui/initialize` request to the Host, the Host SHOULD include UI-specific context in the `McpUiInitializeResult`'s `hostContext` field: | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-019` | **MUST** | Host Behavior L718 `a913c0bdd3e0363c`<br>When using flexible dimensions (no fixed `height` or `width`), hosts MUST listen for `ui/notifications/size-changed` notifications from the View and update the iframe dimensions accordingly: | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-020` | **MUST NOT** | Requirements L786 `2a15799d4a485092`<br>- Host MUST NOT switch the View to a display mode that does not appear in its `appCapabilities.availableDisplayModes`, if set. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-021` | **MUST** | Requirements L787 `36351519afa4511e`<br>- Host MUST return the resulting mode (whether updated or not) in the response to `ui/request-display-mode`. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-022` | **SHOULD** | Requirements L788 `83188a959380935f`<br>- If the requested mode is not available, Host SHOULD return the current display mode in the response. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-023` | **MAY** | Requirements L789 `efe085fad33eb6e6`<br>- Host MAY decline display mode requests from Views that did not declare said modes in their capabilities. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-024` | **SHOULD** | Requests (View → Host) L996 `3c2e6540a2f72b08`<br>Host SHOULD open the URL in the user's default browser or a new tab. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-025` | **SHOULD** | Requests (View → Host) L1033 `6ffa627842ae6d3e`<br>* Host SHOULD add the message to the conversation context, preserving the specified role. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-026` | **MAY** | Requests (View → Host) L1034 `69f18f2d5080889d`<br>* Host MAY request user consent. | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | Text-only ui/message is routed directly to canonical chat; no separate consent mode is advertised. |
| `HOST-027` | **SHOULD** | Requests (View → Host) L1097 `2a80e3bdfc16b038`<br>- SHOULD provide the context to the model in future turns | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | ui/update-model-context is unsupported and updateModelContext is not advertised. |
| `HOST-028` | **MAY** | Requests (View → Host) L1098 `29212344d68dc5d5`<br>- MAY overwrite the previous model context with the new update | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | ui/update-model-context is unsupported and updateModelContext is not advertised. |
| `HOST-029` | **MAY** | Requests (View → Host) L1099 `1112f4558cd5dc16`<br>- MAY defer sending the context to the model until the next user message (including `ui/message`) | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | ui/update-model-context is unsupported and updateModelContext is not advertised. |
| `HOST-030` | **MAY** | Requests (View → Host) L1100 `568f488043c64aed`<br>- MAY dedupe identical `ui/update-model-context` calls | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | ui/update-model-context is unsupported and updateModelContext is not advertised. |
| `HOST-031` | **SHOULD** | Requests (View → Host) L1101 `df85f74bdbb93555`<br>- If multiple updates are received before the next user message, Host SHOULD only send the last update to the model | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | ui/update-model-context is unsupported and updateModelContext is not advertised. |
| `HOST-032` | **MAY** | Requests (View → Host) L1102 `5273986e39fa6bd4`<br>- MAY display context updates to the user | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | ui/update-model-context is unsupported and updateModelContext is not advertised. |
| `HOST-033` | **MUST** | Notifications (Host → View) L1106 `de5ad378011c738d`<br>`ui/notifications/tool-input` - Host MUST send this notification with the complete tool arguments after the View's initialize request completes. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-034` | **MAY** | Notifications (Host → View) L1120 `3efabbe2ed0f6931`<br>`ui/notifications/tool-input-partial` - Host MAY send this notification zero or more times while the agent is streaming tool arguments, before `ui/notifications/tool-input` is sent. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-035` | **MAY** | Notifications (Host → View) L1134 `31f8b801291b4176`<br>- MAY parse the agent's partial JSON output by closing unclosed brackets/braces | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: deliverToolInputPartial` | `mcp-apps-host.test.ts :: stops streaming partial arguments once the complete set is sent` | Athena accepts already-recovered partial argument objects; it does not parse partial JSON text. |
| `HOST-036` | **MAY** | Notifications (Host → View) L1135 `ea55b72598138f8a`<br>- MAY send recovered arguments as they become available during streaming | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: deliverToolInputPartial` | `mcp-apps-host.test.ts :: stops streaming partial arguments once the complete set is sent` | No producer currently streams recovered arguments into the host facade. |
| `HOST-037` | **MUST** | Notifications (Host → View) L1136 `84733a41887e8c76`<br>- MUST stop sending once `ui/notifications/tool-input` is sent with complete arguments | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-038` | **MUST** | Notifications (Host → View) L1155 `2e68bda03f79138f`<br>Host MUST send this notification when tool execution completes (if the View is displayed during tool execution). | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-039` | **MUST** | Notifications (Host → View) L1169 `0cdde0b95ffcfbfe`<br>Host MUST send this notification if the tool execution was cancelled, for any reason (which can optionally be specified), including user action, sampling error, classifier intervention, etc. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-040` | **MUST** | Notifications (Host → View) L1201 `f015a38ba124b0eb`<br>Host MUST send this notification before tearing down the UI resource, for any reason, including user action, resource re-allocation, etc. The Host MAY specify the reason. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-041` | **MAY** | Notifications (Host → View) L1201 `1b64b788ca631e39`<br>Host MUST send this notification before tearing down the UI resource, for any reason, including user action, resource re-allocation, etc. The Host MAY specify the reason. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-042` | **SHOULD** | Notifications (Host → View) L1202 `76ea38b65b78d222`<br>Host SHOULD wait for a response before tearing down the resource (to prevent data loss). | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-043` | **MAY** | Notifications (Host → View) L1229 `5c96e66e7e266461`<br>Host MAY send this notification when any context field changes (e.g., theme toggle, display mode change, device orientation change, window/panel resize). This notification contains partial updates - the View SHOULD merge received fields with its current context state. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `HOST-044` | **REQUIRED** | Client (Host) Capabilities L1522 `e99bcd73b3fe0f30`<br>- `mimeTypes`: Array of supported content types (REQUIRED, e.g., `["text/html;profile=mcp-app"]`) | applicable | `packages/integrations/src/mcp-connector.ts :: MCP_UI_CLIENT_CAPABILITY` | `mcp-apps-conformance.test.ts :: declares the ui extension with the profile mimeType` |  |
| `HOST-045` | **MUST** | 1. Iframe Sandboxing L1698 `ec1020d755533a12`<br>All View content MUST be rendered in sandboxed iframes with restricted permissions. | applicable | `apps/web/src/components/athena/mcp-app-view.tsx :: McpAppView` | `apps/web/tests/athena/mcp-app-view.test.tsx :: gives the proxy an origin and the widget none` |  |
| `HOST-046` | **MUST** | 4. Content Security Policy Enforcement L1725 `2c524d654b45131a`<br>Hosts MUST enforce Content Security Policies based on resource metadata. | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewCsp` | `mcp-apps-host.test.ts :: adds exactly the origins the resource declared and nothing else` |  |
| `HOST-047` | **MUST** | 4. Content Security Policy Enforcement L1756 `d78fa78cbe09a04b`<br>- Host MUST block connections to undeclared domains | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewCsp` | `mcp-apps-host.test.ts :: adds exactly the origins the resource declared and nothing else` |  |
| `HOST-048` | **SHOULD** | 4. Content Security Policy Enforcement L1757 `c3b346520880adfe`<br>- Host SHOULD warn users when UI requires external domain access | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | Athena visibly contains apps in sandbox chrome and enforces declarations; it does not add a separate external-domain warning. |
| `HOST-049` | **MAY** | 4. Content Security Policy Enforcement L1758 `565f888ad0f06eef`<br>- Host MAY implement global domain allowlists/blocklists | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | Athena uses exact per-resource declarations and has no global domain list feature. |

## Sandbox requirements

| ID | Level | Source | Applicability | Implementation | Behavioral test | Reason |
| --- | --- | --- | --- | --- | --- | --- |
| `SANDBOX-001` | **MUST** | Sandbox proxy L475 `d7eae745f110fb68`<br>2. The Sandbox MUST have the following permissions: `allow-scripts`, `allow-same-origin`. | applicable | `packages/integrations/src/mcp-apps-sandbox.ts :: sandboxProxyDocument` | `mcp-apps-sandbox.test.ts :: forwards host messages down to the view and view messages up to the host` |  |
| `SANDBOX-002` | **MUST** | Sandbox proxy L476 `58048a3c84045329`<br>3. The Sandbox MUST send a `ui/notifications/sandbox-proxy-ready` notification to the host when it's ready to process an `ui/notifications/sandbox-resource-ready` notification. | applicable | `packages/integrations/src/mcp-apps-sandbox.ts :: sandboxProxyDocument` | `mcp-apps-sandbox.test.ts :: forwards host messages down to the view and view messages up to the host` |  |
| `SANDBOX-003` | **MUST** | Sandbox proxy L478 `bd3b0be78ece8419`<br>5. The Sandbox MUST load the raw HTML of the View with CSP settings that: | applicable | `packages/integrations/src/mcp-apps-sandbox.ts :: sandboxProxyDocument` | `mcp-apps-sandbox.test.ts :: forwards host messages down to the view and view messages up to the host` |  |
| `SANDBOX-004` | **MAY** | Sandbox proxy L484 `de2ed9663fe73f1c`<br>- If `permissions` is declared, the Sandbox MAY set the inner iframe's `allow` attribute accordingly | applicable | `packages/integrations/src/mcp-apps-host.ts :: buildViewPermissionsAllow` | `mcp-apps-host.test.ts :: grants only the permissions the resource asked for` |  |
| `SANDBOX-005` | **MUST** | Sandbox proxy L485 `24822d78b2fdd443`<br>6. The Sandbox MUST forward messages sent by the Host to the View, and vice versa, for any method that doesn't start with `ui/notifications/sandbox-`. This includes lifecycle messages, e.g., `ui/initialize` request & `ui/notifications/initialized` notification both sent by the View. The Host MUST NOT send any request or notification to the View before it receives an `initialized` notification. | applicable | `packages/integrations/src/mcp-apps-sandbox.ts :: sandboxProxyDocument` | `mcp-apps-sandbox.test.ts :: forwards host messages down to the view and view messages up to the host` |  |
| `SANDBOX-006` | **SHOULD NOT** | Sandbox proxy L486 `db3b2162c1b604d8`<br>7. The Sandbox SHOULD NOT create/send any requests to the Host or to the View (this would require synthesizing new request ids). | applicable | `packages/integrations/src/mcp-apps-sandbox.ts :: sandboxProxyDocument` | `mcp-apps-sandbox.test.ts :: forwards host messages down to the view and view messages up to the host` |  |

## Server requirements

| ID | Level | Source | Applicability | Implementation | Behavioral test | Reason |
| --- | --- | --- | --- | --- | --- | --- |
| `SERVER-001` | **MUST** | UI Resource Format L64 `2bfa91d727eff917`<br>* MUST use the `ui://` URI scheme to distinguish UI resources from other | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-002` | **SHOULD** | UI Resource Format L95 `15999262f0aa84bf`<br>* SHOULD be `text/html;profile=mcp-app` for HTML-based UIs in the initial MVP. | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-003` | **MUST** | UI Resource Format L206 `b9ff1969224cf01f`<br>* determined by each host. Servers MUST consult host-specific documentation | not-applicable | `apps/api/src/mcp/apps/index.ts :: widget resource metadata` | `apps/web/tests/athena/mcp-app-view.test.tsx :: renders the widget through a proxy on the API origin, not the app origin` | Athena resources do not declare ui.domain; the API-origin sandbox is host-controlled. |
| `SERVER-004` | **MUST** | UI Resource Format L240 `5ea525d3c49c6b97`<br>mimeType: "text/html;profile=mcp-app"; // MUST be "text/html;profile=mcp-app" | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-005` | **MUST** | Content Requirements: L267 `177a09f26b8b78ec`<br>- URI MUST start with `ui://` scheme | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-006` | **MUST** | Content Requirements: L268 `5c97c7c9edcb1fe5`<br>- `mimeType` MUST be `text/html;profile=mcp-app` (other types reserved for future extensions) | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-007` | **MUST** | Content Requirements: L269 `14eb096e92aef753`<br>- Content MUST be provided via either `text` (string) or `blob` (base64-encoded) | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-008` | **MUST** | Content Requirements: L270 `7020222c5779a235`<br>- Content MUST be valid HTML5 document | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-009` | **MUST** | Behavior: L390 `05e8e1269d28eb8c`<br>- Resource MUST exist on the server | applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` |  |
| `SERVER-010` | **MAY** | Behavior: L393 `7b9eb959446688a7`<br>- Since UI resources are primarily discovered through tool metadata, Servers MAY omit UI-only resources from `resources/list` and `notifications/resources/list_changed` | not-applicable | `apps/api/src/mcp/apps/index.ts :: registerAppResources` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` | Athena lists its own stable UI resources; it does not exercise the optional omission. |
| `SERVER-011` | **SHOULD** | Server Behavior L1531 `3763365bf9766368`<br>Servers SHOULD check client capabilities before registering UI-enabled tools. The SDK provides the `getUiCapability` helper for this: | not-applicable | `packages/integrations/src/mcp-connector.ts :: textual tool-result fallback` | `mcp-apps-real-connector.test.ts :: preserves a successful textual result when its app resource cannot be read` | Athena always registers meaningful text fallback and UI metadata remains ignorable by non-UI clients. |
| `SERVER-012` | **SHOULD** | Server Behavior L1558 `c8c4b25b5d3cdae9`<br>- Servers SHOULD provide text-only fallback behavior for all UI-enabled tools | applicable | `packages/integrations/src/mcp-connector.ts :: textual tool-result fallback` | `mcp-apps-real-connector.test.ts :: preserves a successful textual result when its app resource cannot be read` |  |
| `SERVER-013` | **MUST** | Server Behavior L1559 `0204cb2df93c1ff8`<br>- Tools MUST return meaningful content array even when UI is available | applicable | `packages/integrations/src/mcp-connector.ts :: textual tool-result fallback` | `mcp-apps-real-connector.test.ts :: preserves a successful textual result when its app resource cannot be read` |  |
| `SERVER-014` | **MAY** | Server Behavior L1560 `bf69a1147461a1b4`<br>- Servers MAY register different tool variants based on host capabilities | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | Athena registers one tool variant with meaningful text fallback, not capability-specific variants. |

## View requirements

| ID | Level | Source | Applicability | Implementation | Behavioral test | Reason |
| --- | --- | --- | --- | --- | --- | --- |
| `VIEW-001` | **SHOULD NOT** | UI Resource Format L171 `573c3a0e2874903e`<br>* Apps SHOULD NOT assume permissions are granted; use JS feature detection as fallback. | not-applicable | `apps/api/src/mcp/apps/runtime.ts :: permission-free runtime` | `apps/api/tests/mcp/mcp-apps.test.ts :: serves a document that can boot with no network at all` | Athena widgets request no permission-dependent browser API, so feature-detection fallback is not exercised. |
| `VIEW-002` | **MUST** | App Capabilities in `ui/initialize` L512 `a7bed610ff928163`<br>When the View sends an `ui/initialize` request to the Host, it MUST include its capabilities in the `appCapabilities` field: | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-003` | **MUST** | Requirements L781 `10fa2a0572b0e08b`<br>- View MUST declare all display modes it supports in `appCapabilities.availableDisplayModes` during initialization. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-004` | **MUST** | Requirements L782 `42ffc1178e4709b0`<br>- View MUST check if the requested mode is in `availableDisplayModes` from host context before requesting a mode change. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-005` | **MUST** | Requirements L783 `506540c0ef05df46`<br>- View MUST handle the response mode differing from the requested mode. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-006` | **MAY** | Requests (View → Host) L1093 `14a2a10748db383a`<br>The View MAY send this request to update the Host's model context. This context will be used in future turns. Each request overwrites the previous context sent by the View. | not-applicable | `packages/integrations/src/mcp-apps-host.ts :: truthful hostCapabilities` | `mcp-apps-conformance.test.ts :: advertises only end-to-end host capabilities` | ui/update-model-context is unsupported and updateModelContext is not advertised. |
| `VIEW-007` | **MAY** | Notifications (Host → View) L1140 `7553d87ffe23b44e`<br>- MAY ignore these notifications entirely | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-008` | **MAY** | Notifications (Host → View) L1141 `70b28eacd98143d7`<br>- MAY render progressive loading/streaming states based on available fields | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-009` | **MUST NOT** | Notifications (Host → View) L1142 `819da869a246b145`<br>- MUST NOT rely on partial arguments for critical operations | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-010` | **SHOULD** | Notifications (Host → View) L1143 `0bb16720f40a7052`<br>- SHOULD gracefully handle missing or changing fields between notifications | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-011` | **SHOULD** | Notifications (Host → View) L1217 `046f65bc220ebdf4`<br>The View SHOULD send this notification when rendered content body size changes (e.g. using ResizeObserver API to report up to date size). | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |
| `VIEW-012` | **SHOULD** | Notifications (Host → View) L1229 `dfa0d4b6f2d785a8`<br>Host MAY send this notification when any context field changes (e.g., theme toggle, display mode change, device orientation change, window/panel resize). This notification contains partial updates - the View SHOULD merge received fields with its current context state. | applicable | `packages/integrations/src/mcp-apps-host.ts :: createMcpAppHost` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |  |

## Advertised optional capability evidence

| Capability | End-to-end test |
| --- | --- |
| `openLinks` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |
| `serverTools` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |
| `message.text` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |
| `sandbox.csp` | `apps/web/tests/athena/mcp-app-view.test.tsx :: gives the proxy an origin and the widget none` |
| `sandbox.permissions` | `apps/web/tests/athena/mcp-app-view.test.tsx :: gives the proxy an origin and the widget none` |
| `hostContext.theme` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |
| `hostContext.sizing` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |
| `displayMode.inline` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |
| `displayMode.fullscreen` | `mcp-apps-official-compat.test.ts :: interoperates through the official App across the complete stable lifecycle` |
