{
  "name": "@docket/billing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "Server-side billing lifecycle and provider behavior.",
  "exports": {
    "./contracts": "./src/contracts.ts",
    "./adapters/in-memory": "./src/adapters/in-memory.ts",
    "./adapters/stripe": "./src/adapters/stripe.ts",
    "./application/lifecycle": "./src/application/lifecycle.ts",
    "./application/entitlement": "./src/application/entitlement.ts",
    "./application/discounts": "./src/application/discounts.ts",
    "./application/provider-state": "./src/application/provider-state.ts",
    "./provision": "./src/provision.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf .turbo *.tsbuildinfo",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@docket/db": "workspace:*",
    "drizzle-orm": "catalog:",
    "stripe": "catalog:"
  },
  "devDependencies": {
    "@docket/eslint-config": "workspace:*",
    "@docket/tsconfig": "workspace:*",
    "vite": "catalog:",
    "vitest": "catalog:"
  }
}
