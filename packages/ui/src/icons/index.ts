/**
 * `@docket/ui/icons` — curated lucide-react icon set.
 *
 * @remarks
 * Re-exports the handful of `lucide-react` icons the app shell and ListView family need,
 * so feature code imports icons from one stable subpath
 * (`import { ChevronRight } from '@docket/ui/icons'`) rather than reaching into
 * `lucide-react` directly. Add new glyphs here as slices require them.
 */
export {
  Check,
  CheckCircle2,
  ChevronDown,
  ChevronLeft,
  ChevronRight,
  ChevronUp,
  Circle,
  CircleDashed,
  CircleDot,
  Ellipsis,
  Filter,
  FolderKanban,
  Home,
  Inbox,
  LayoutGrid,
  LogOut,
  type LucideIcon,
  Menu,
  MoreHorizontal,
  Plus,
  RefreshCw,
  Search,
  Settings,
  Sparkles,
  Users,
  User,
  X,
  XCircle,
} from 'lucide-react';
