/**
 * `@docket/ui` — Vitest setup.
 *
 * @remarks
 * Registers the `@testing-library/jest-dom` matchers (e.g. `toBeInTheDocument`,
 * `toHaveClass`) on Vitest's `expect` for every component test.
 */
import '@testing-library/jest-dom/vitest';
